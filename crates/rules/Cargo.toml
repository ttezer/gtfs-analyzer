[package]
name = "gtfs-rules"
version = "0.9.7"
edition = "2021"
license.workspace = true
description = "Rule registry for the GTFS Analyzer validator. Internal API, no stability guarantee."
repository = "https://github.com/ttezer/gtfs-analyzer"
keywords = ["gtfs", "transit", "validator", "rules"]
categories = ["data-structures"]

[dependencies]
gtfs-core = { version = "0.9.7", path = "../core" }

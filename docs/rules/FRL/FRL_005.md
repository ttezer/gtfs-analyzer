# FRL_005 — contains_id geçersiz

> **Karar:** `fare_rules.txt.contains_id` dolu ama `stops.txt.zone_id` kümesinde yok. KRİTİK, spec hatası. **Düzeltme:** Geçerli bir `zone_id` kullanın.
> ⚠️ `contains_id`, yolculuğun içinden geçtiği zone id koşuludur; stop id veya route id ile karıştırılmamalıdır.

| Alan | Değer |
|---|---|
| Grup | FRL — Fare Rules |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Row |
| Kimlik alanı | `fare_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_fare_rules()` içinde:
```
fare_rules.contains_id dolu
VE map.zone_ids içinde yok
→ FRL_005
```
- `map.zone_ids`, `stops.txt.zone_id` değerlerinden kurulur.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Boş `contains_id` FRL_005 üretmez.
- **Yanlış pozitif:** Stop id gibi verilen değer zone id değilse hata doğrudur.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **FRL_003** | Kalkış zone referansı yok | `origin_id` | KRİTİK · Spec · K4 |
| **FRL_004** | Varış zone referansı yok | `destination_id` | KRİTİK · Spec · K4 |
| **FRL_005** | İçinden geçilen zone referansı yok | `contains_id` | KRİTİK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** contains_id geçersiz
- **Çözüm önerisi:** `contains_id` için geçerli bir `zone_id` kullanın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"`contains_id '<zone_id>' stops.txt'te tanımlı bir zone_id değil.`"*
- **Çözüm önerisi:** `Geçerli bir zone_id kullanın.`

## Ölçülen alan / değer
- **Dosya/alan:** `fare_rules.txt` → `contains_id`
- **Gözlenen değer:** `stops.txt.zone_id` içinde bulunmayan değer
- **Beklenen değer:** Tanımlı `zone_id`

## Kod değiştirirken minimum test matrisi
- Var olmayan `contains_id` zone değeri → FRL_005 çıkmalı.
- Geçerli zone değeri → FRL_005 çıkmamalı.
- Boş `contains_id` → FRL_005 çıkmamalı.
- `origin_id` ve `destination_id` hatalarıyla rule id karışmamalı.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- Spec sınıfı ve `VS_K` görünürlüğü nedeniyle kritik rapor görünürlüklerine girer.

### Hangi raporlarda görünür
`report_views = VS_K` → R1, R2, R5, R9.

### Bağımlılık (bunu maskeleyen root cause'lar)
`stops.txt.zone_id` alanları eksikse zone tabanlı fare rule referansları yanlış yorumlanabilir.

### Dış araç eşleşmesi
2026-06-03 doğrulandı: MobilityData kuralları sayfasında birebir FRL_005 adı görünmedi; GTFS Fares v1 dokümantasyonu `contains_id` alanını zone id referansı olarak tanımlar.

Kaynaklar: [MobilityData GTFS Validator Rules](https://gtfs-validator.mobilitydata.org/rules.html), [GTFS Fares Feature Guide](https://gtfs.org/getting-started/features/fares/)

### GTFS spec referansı
[GTFS Schedule Reference - fare_rules.txt](https://gtfs.org/documentation/schedule/reference/#fare_rulestxt): `contains_id`, `stops.zone_id` değerine referans verir.

### Kod referansı
- Emit: [k4_cross_ref.rs](../../../crates/pipeline/src/k4_cross_ref.rs)


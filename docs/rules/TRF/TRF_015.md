# TRF_015 — in-seat aktarma geçersiz

> **Karar:** `transfer_type=4/5` (linked-trip) aktarmasında `from_stop_id`/`to_stop_id` bir istasyon (`location_type=1`); linked-trip transfer için durak (location_type=0) referansı beklenir. YÜKSEK, Quality. **Düzeltme:** Linked-trip transfer için durak (location_type=0) referansı kullanın.
> ⚠️ Yalnızca `transfer_type ∈ {4,5}` satırlarında, referans durağın `location_type=1` olması durumunda tetiklenir.

| Alan | Değer |
|---|---|
| Grup | TRF — Transfers |
| Önem | Yüksek |
| Sınıf | Quality |
| Otorite kaynağı | PROJECT_QUALITY |
| Aşama | K4 |
| Varlık | Row |
| Kimlik alanı | - |
| Skor tabanı | 2 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_transfers()` içinde:
```
transfer_type ∈ {4,5}
from/to_stop_id'nin stops.txt location_type == 1 (istasyon)
→ TRF_015 (satır bazlı, alan başına notice)
```
- Kapsam birimi: satır (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `transfer_type` 4/5 değilse değerlendirilmez.
- **Yanlış pozitif:** Yok; in-seat aktarma durak düzeyinde tanımlanmalıdır.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRF_015** | type 4/5 stop bir istasyon | location_type | YÜKSEK · Quality · K4 |
| **TRF_013** | type 4/5 için trip_id eksik | bağlam | KRİTİK · Quality · K4 |
| **TRF_014** | in-seat seferin stop_times'ı yok | stop_times | YÜKSEK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** in-seat aktarma geçersiz
- **Çözüm önerisi:** Linked-trip transfer için durak (location_type=0) referansı kullanın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"{alan} '{stop_id}' bir istasyon (location_type=1); linked-trip transfer'da durak (0) olmalı."*

## Ölçülen alan / değer
- **Dosya/alan:** `transfers.txt` → `from_stop_id`/`to_stop_id`; `stops.txt` → `location_type`.
- **Gözlenen değer:** `location_type=1` durak.
- **Beklenen değer:** `location_type != 1` (durak).

## Kod değiştirirken minimum test matrisi
- type=4 + istasyon stop_id → TRF_015 çıkmalı.
- type=4 + durak (location_type=0) stop_id → notice yok.
- type=2 + istasyon → TRF_015 çıkmamalı.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 3.0** (YÜKSEK).
- **R1 blocker değil:** Quality sınıfı R1 üretmez (R1 yalnız `GtfsSpec` + Spec + Kritik).
- **R5 genel skor:** Quality sınıf penaltısına girer (ağırlık 0.2).

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-07-09 doğrulandı:** MobilityData rules sayfasında "in-seat aktarma stop location_type=station kısıtı" için birebir notice karşılığı tespit edilmedi; proje-özel kalite kuralıdır.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- **Doğrudan normatif karşılık yok:** Linked-trip aktarmada station yerine platform kullanma beklentisi proje-özel kalite kuralı olarak tutulur (2026-07 otorite denetimi).
- 🔗 [GTFS Reference — transfers.txt](https://gtfs.org/documentation/schedule/reference/#transferstxt) · [stops.txt](https://gtfs.org/documentation/schedule/reference/#stopstxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:1761](../../../crates/pipeline/src/k4_cross_ref.rs#L1761) (`check_transfers`)

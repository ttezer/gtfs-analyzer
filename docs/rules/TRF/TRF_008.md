# TRF_008 — from_route_id bulunamadı

> **Karar:** `transfers.txt::from_route_id` `routes.txt`'te tanımlı değil. KRİTİK, Spec. **Düzeltme:** Geçerli bir `route_id` kullanın.
> ⚠️ Satır bazlı (Row); yalnızca dolu `from_route_id` için FK kontrolü.

| Alan | Değer |
|---|---|
| Grup | TRF — Transfers |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Row |
| Kimlik alanı | `from_route_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_transfers()` içinde:
```
from_route_id Some VE map.routes İÇERMİYOR
→ TRF_008 (satır bazlı notice)
```
- Kapsam birimi: satır (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `from_route_id` boşsa atlanır.
- **Yanlış pozitif:** Yok.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRF_008** | from_route_id routes.txt'te yok | FK (route) | KRİTİK · Spec · K4 |
| **TRF_009** | to_route_id routes.txt'te yok | FK (route) | KRİTİK · Spec · K4 |
| **TRF_017** | trip↔route uyumsuzluğu | tutarlılık | YÜKSEK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** from_route_id bulunamadı
- **Çözüm önerisi:** Geçerli bir `route_id` kullanın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"from_route_id '{route_id}' routes.txt'te tanımlı değil."*

## Ölçülen alan / değer
- **Dosya/alan:** `transfers.txt` → `from_route_id`; karşılaştırma `routes.txt`.
- **Gözlenen değer:** Tanımsız `route_id`.
- **Beklenen değer:** `routes.txt`'te kayıtlı `route_id`.

## Kod değiştirirken minimum test matrisi
- routes.txt'te olmayan from_route_id → TRF_008 çıkmalı.
- `from_route_id` boş → notice yok.
- Geçerli `from_route_id` → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** Spec + KRİTİK → yayın engelleyici.
- **R5 kalite skoru:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `foreign_key_violation` (`transfers.txt::from_route_id` → `routes.txt::route_id`).
  - https://gtfs-validator.mobilitydata.org/rules.html
- GTFS Guru tarafında bu kontrolün canlı doğrulanmış birebir eşleşmesi tespit edilmedi.

### GTFS spec referansı
- GTFS `transfers.txt::from_route_id` → `routes.txt::route_id` foreign key kuralı.
- 🔗 [GTFS Reference — transfers.txt](https://gtfs.org/documentation/schedule/reference/#transferstxt) · [routes.txt](https://gtfs.org/documentation/schedule/reference/#routestxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:1139](../../../crates/pipeline/src/k4_cross_ref.rs#L1139) (`check_transfers`)

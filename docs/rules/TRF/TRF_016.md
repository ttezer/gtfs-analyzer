# TRF_016 — Aktarma koşulu çelişkili

> **Karar:** Aynı (`from_stop`, `to_stop`, `from_trip`, `to_trip`, `from_route`, `to_route`) kombinasyonu birden fazla satırda tanımlı; hangi kuralın geçerli olduğu (öncelik) belirsiz. ORTA, Interop. **Düzeltme:** Çakışan transfer satırlarından birini kaldırın veya alanları farklılaştırın.
> ⚠️ Tam altı-alanlı anahtar üzerinde çalışır; bağlamsız basit durak çifti tekrarları TRF_012 kapsamındadır.

| Alan | Değer |
|---|---|
| Grup | TRF — Transfers |
| Önem | Kritik |
| Sınıf | Spec |
| Otorite kaynağı | MOBILITYDATA_PARITY |
| Aşama | K4 |
| Varlık | Row |
| Kimlik alanı | - |
| Skor tabanı | 2 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_transfers()` içinde:
```
key = "from_stop|to_stop|from_trip|to_trip|from_route|to_route"
key daha önce görüldü (seen_keys)
→ TRF_016 (satır bazlı, ilk görünüm satırı referanslı)
```
- Kapsam birimi: satır (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Alanlardan biri farklıysa anahtar farklı sayılır, çakışma algılanmaz.
- **Yanlış pozitif:** Yok; aynı altı-alanlı anahtarın tekrarı kesin çelişkidir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRF_016** | Tam anahtar (stop+trip+route) çakışması | bağlamlı anahtar | ORTA · Interop · K4 |
| **TRF_012** | Bağlamsız (stop-only) çift tekrarı | basit durak çifti | ORTA · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** Aktarma koşulu çelişkili
- **Çözüm önerisi:** Çakışan transfer satırlarından birini kaldırın veya alanları farklılaştırın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"Aynı (from_stop, to_stop, trip, route) kombinasyonunda çakışan transfer kaydı (ilk görünüm: satır {n}); öncelik belirsiz."*

## Ölçülen alan / değer
- **Dosya/alan:** `transfers.txt` → altı-alanlı birleşik anahtar.
- **Gözlenen değer:** Tekrarlanan anahtar.
- **Beklenen değer:** Her anahtar için tek satır.

## Kod değiştirirken minimum test matrisi
- Aynı altı-alanlı anahtarlı iki satır → TRF_016 çıkmalı.
- Bağlamsız çift tekrarı → TRF_016 değil TRF_012.
- Farklı anahtarlı satırlar → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (ORTA).
- **R1 blocker değil:** Interop sınıfı R1 üretmez (R1 yalnız `GtfsSpec` + Spec + Kritik).
- **R8 dış uyum:** MobilityData paritesi görünümüne girer.

### Hangi raporlarda görünür
`report_views = VI = [R2, R5, R8, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-07-27 DÜZELTİLDİ:** MD karşılığı `duplicate_key` (ERROR); Entur Norway (`mdb-1078`) feed'inde MD 10, bu proje 10 → birebir parite. Ancak sınıfı belirleyen MD değil spec metnidir: GTFS Reference `transfers.txt` *"Primary key (from_stop_id, to_stop_id, from_trip_id, to_trip_id, from_route_id, to_route_id)"* der. Sınıf `Interop/MobilitydataParity` → **`Spec/GtfsSpec`**, önem KRİTİK (yayın engeli).
- **2026-07-09 doğrulandı:** MobilityData karşılığı `duplicate_key` (transfers birleşik anahtarının tekrarı).
  - https://gtfs-validator.mobilitydata.org/rules.html
- GTFS Guru tarafında bu kontrolün canlı doğrulanmış birebir eşleşmesi tespit edilmedi.

### GTFS spec referansı
- GTFS `transfers.txt` — birleşik birincil anahtar tekil olmalıdır.
- 🔗 [GTFS Reference — transfers.txt](https://gtfs.org/documentation/schedule/reference/#transferstxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:1799](../../../crates/pipeline/src/k4_cross_ref.rs#L1799) (`check_transfers`)
- Test: [k4_cross_ref.rs:5415](../../../crates/pipeline/src/k4_cross_ref.rs#L5415)

# TRF_017 — Sefer aktarması yanlış hat

> **Karar:** `transfers.txt`'te `from_route_id`/`to_route_id`, ilgili `from_trip_id`/`to_trip_id` seferinin gerçek hattıyla eşleşmiyor. YÜKSEK, Spec. **Düzeltme:** `route_id`'yi ilgili seferin gerçek hat kodu ile eşleştirin.
> ⚠️ Satır bazlı (Row); from ve to yönleri ayrı ayrı, yalnızca hem trip hem route belirtilmişken ve trip trips.txt'te varken kontrol edilir.
> 🔀 **SINIF DEĞİŞTİ (#96, 2026-08-09): Interop → Spec.** Sert hüküm `P0fa71358 · P585a24d4 · P134a2d40 · P446972ca`'i ölçen TEK kural buydu ve kanıt sınıfı hükmün otoritesiyle uyuşmuyordu: Spec'e filtreleyen kullanıcı ihlali hiç görmüyordu. Spec metni: *"If both from_trip_id and from_route_id are defined, the trip_id must belong to the route_id."* Dört hüküm kimliği aynı cümlenin from/to yazımlarıdır.
> ⚠️ **Yayın davranışı DEĞİŞMEDİ:** R1 kapısı `Spec ∧ Kritik` ve çift yönlüdür; bu kuralın önemi YÜKSEK olduğu için hiçbir feed'i yayına engellemez. **R5 skorunda ağırlık Interop (%30)'den Spec'e (%40) geçer.**

| Alan | Değer |
|---|---|
| Grup | TRF — Transfers |
| Önem | Yüksek |
| Sınıf | Spec |
| Otorite kaynağı | GTFS_SPEC |
| Aşama | K4 |
| Varlık | Row |
| Kimlik alanı | - |
| Skor tabanı | 2 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | `XFL_020` |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_transfers()` içinde:
```
(from_trip_id, from_route_id) ve (to_trip_id, to_route_id) için:
trip map.trips'te VAR VE trip'in gerçek route_id'si != belirtilen route_id
→ TRF_017 (satır bazlı notice)
```
- Kapsam birimi: satır (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** trip veya route belirtilmemişse o yön kontrol edilmez.
- **Yanlış pozitif:** Yok; trip'in gerçek route'u net bilinir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRF_017** | transfer trip↔route uyumsuzluğu | tutarlılık | YÜKSEK · Spec · K4 |
| **XFL_020** | Aynı (trip,route) uyumsuzluğu (from+to kanonik) | tutarlılık (XFL görünümü) | YÜKSEK · Spec · K4 |

> Not: TRF_017 ve XFL_020 aynı trip↔route uyumsuzluğunu kontrol eder (her ikisi de `check_transfers`'de). Çift raporlamayı önlemek için TRF_017 kök kuraldır ve `blocks=[XFL_020]` ile XFL_020'yi R9'da semptom olarak bastırır (ikisi de None scope → bastırma uygulanır).

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** Sefer aktarması yanlış hat
- **Çözüm önerisi:** `from_route_id`/`to_route_id`'yi ilgili seferin gerçek hat kodu ile eşleştirin.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"from_trip_id '{trip_id}' route_id '{gerçek}' ile ilişkili, ancak from_route_id '{belirtilen}' belirtilmiş."*

## Ölçülen alan / değer
- **Dosya/alan:** `transfers.txt` → `from_route_id`/`to_route_id`, `from_trip_id`/`to_trip_id`; `trips.txt` → `route_id`.
- **Gözlenen değer:** Belirtilen `route_id`.
- **Beklenen değer:** Seferin gerçek `route_id`'si.

## Kod değiştirirken minimum test matrisi
- from_route_id ≠ trip'in gerçek route'u → TRF_017 çıkmalı.
- route_id eşleşiyor → notice yok.
- trip veya route boş → o yön atlanır.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 3.0** (YÜKSEK).
- **R1 blocker değil:** Spec sınıfı ama önem YÜKSEK; R1 yalnız `Spec ∧ Kritik` üretir. (#96'dan önce Interop idi.)
- **R8 dış uyum:** MobilityData paritesi görünümüne girer.

### Hangi raporlarda görünür
`report_views = VI = [R2, R5, R8, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
`blocks = [XFL_020]` — TRF_017 ile XFL_020 aynı koşulu yakaladığından, XFL_020 R9'da semptom olarak bastırılır.

### Dış araç eşleşmesi
- **2026-07-09 doğrulandı:** MobilityData birebir karşılığı `transfer_with_invalid_trip_and_route` notice'ıdır.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS `transfers.txt` — `from_route_id`/`to_route_id` belirtilen trip'in route'u ile tutarlı olmalıdır.
- 🔗 [GTFS Reference — transfers.txt](https://gtfs.org/documentation/schedule/reference/#transferstxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:1918](../../../crates/pipeline/src/k4_cross_ref.rs#L1918) (`check_transfers`)

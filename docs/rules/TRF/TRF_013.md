# TRF_013 — Aktarma türü bağlamla uyumsuz

> **Karar:** `transfer_type=4` veya `5` (linked-trip / in-seat) için `from_trip_id` ve `to_trip_id` zorunlu olduğu halde en az biri eksik. KRİTİK, Spec. **Düzeltme:** Linked-trip transfer için her iki `trip_id` alanını doldurun.
> ⚠️ Yalnızca `transfer_type ∈ {4,5}` satırlarında trip_id zorunluluğunu kontrol eder.

| Alan | Değer |
|---|---|
| Grup | TRF — Transfers |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Row |
| Kimlik alanı | - |
| Skor tabanı | 2 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_transfers()` içinde:
```
transfer_type ∈ {4,5}
VE (from_trip_id None VEYA to_trip_id None)
→ TRF_013 (satır bazlı notice)
```
- Kapsam birimi: satır (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `transfer_type` 4/5 değilse değerlendirilmez.
- **Yanlış pozitif:** Yok; tip 4/5 tanım gereği iki trip_id gerektirir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRF_013** | type 4/5 için trip_id'ler eksik | bağlam zorunluluğu | KRİTİK · Spec · K4 |
| **TRF_014** | in-seat seferin stop_times'ı yok | stop_times yokluğu | YÜKSEK · Spec · K4 |
| **TRF_015** | type 4/5 stop bir istasyon | location_type | YÜKSEK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** Aktarma türü bağlamla uyumsuz
- **Çözüm önerisi:** Linked-trip transfer için her iki `trip_id` alanını doldurun.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"transfer_type={tip} için from_trip_id ve to_trip_id zorunludur."*

## Ölçülen alan / değer
- **Dosya/alan:** `transfers.txt` → `transfer_type`, `from_trip_id`, `to_trip_id`.
- **Gözlenen değer:** Eksik trip_id (tip 4/5).
- **Beklenen değer:** Her iki trip_id dolu.

## Kod değiştirirken minimum test matrisi
- `transfer_type=4` + from_trip_id eksik → TRF_013 çıkmalı.
- `transfer_type=5` + her iki trip_id dolu → notice yok.
- `transfer_type=2` + trip_id eksik → TRF_013 çıkmamalı.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** Spec + KRİTİK → yayın engelleyici.
- **R5 kalite skoru:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData ve GTFS Guru tarafında "type 4/5 trip_id zorunluluğu" için doğrulanmış birebir notice karşılığı tespit edilmedi; proje-özel Spec kuralıdır (GTFS linked-trip semantiğine dayalı).
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS `transfers.txt::transfer_type` 4/5 — `from_trip_id` ve `to_trip_id` gerektirir.
- 🔗 [GTFS Reference — transfers.txt](https://gtfs.org/documentation/schedule/reference/#transferstxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:1336](../../../crates/pipeline/src/k4_cross_ref.rs#L1336) (`check_transfers`)
- Test: [k4_cross_ref.rs:3654](../../../crates/pipeline/src/k4_cross_ref.rs#L3654)

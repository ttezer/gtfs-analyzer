# TRF_004 — transfer_type geçersiz

<!-- md-parity: code=unexpected_enum_value -->

> **Karar:** `transfers.txt::transfer_type` 0–5 dışında bir değer (ya da sayı olmayan). YÜKSEK, Spec. **Düzeltme:** `transfer_type`'ı geçerli bir GTFS enum değerine ayarlayın (0–5).
> ⚠️ Satır bazlı (Row); geçerli aralık 0–5, parse edilemeyen değerler de tetikler.

| Alan | Değer |
|---|---|
| Grup | TRF — Transfers |
| Önem | Yüksek |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Row |
| Kimlik alanı | `from_stop_id\|to_stop_id` |
| Skor tabanı | 2 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::transfers::validate_transfers()` içinde:
```
transfer_type dolu VE değer ∉ {0,1,2,3,4,5}  (veya parse hatası)
→ TRF_004 (satır bazlı notice)
```
- Geçerli değerler: 0–5.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `transfer_type` boşsa varsayılan (0) sayılır, atlanır.
- **Yanlış pozitif:** Yok; enum dışı değer kesin Spec ihlalidir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRF_004** | transfer_type 0–5 dışında | enum değeri | YÜKSEK · Spec · K2 |
| **TRF_013** | type 4/5 için trip_id'ler eksik | bağlam | KRİTİK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** transfer_type geçersiz
- **Çözüm önerisi:** `transfer_type`'ı geçerli bir GTFS enum değerine ayarlayın (0–5).

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"transfer_type '{değer}' geçersiz."*

## Ölçülen alan / değer
- **Dosya/alan:** `transfers.txt` → `transfer_type`.
- **Gözlenen değer:** Geçersiz değer.
- **Beklenen değer:** `0–5`.

## Kod değiştirirken minimum test matrisi
- `transfer_type=6` → TRF_004 çıkmalı.
- `transfer_type=abc` → TRF_004 çıkmalı.
- `transfer_type=0..5` → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 3.0** (YÜKSEK).
- **R1 blocker değil:** Spec + YÜKSEK (KRİTİK değil).
- **R5 genel skor:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `unexpected_enum_value` (`transfers.txt::transfer_type`).
  - https://gtfs-validator.mobilitydata.org/rules.html
- GTFS Guru tarafında bu kontrolün canlı doğrulanmış birebir eşleşmesi tespit edilmedi.

### GTFS spec referansı
- GTFS `transfers.txt::transfer_type` — geçerli değerler 0–5.
- 🔗 [GTFS Reference — transfers.txt](https://gtfs.org/documentation/schedule/reference/#transferstxt)

### Kod referansı
- Emit: [k2/transfers.rs:38](../../../crates/pipeline/src/k2/transfers.rs#L38) (`validate_transfers`)

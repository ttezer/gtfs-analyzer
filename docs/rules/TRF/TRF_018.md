# TRF_018 — Sefer aktarması aynı seferi gösteriyor

> **Karar:** `from_trip_id` ve `to_trip_id` aynı sefer; bir seferin kendisine aktarması anlamsız. ORTA, Quality. **Düzeltme:** `from_trip_id` ve `to_trip_id` farklı seferler olmalıdır.
> ⚠️ Satır bazlı (Row); yalnızca her iki trip_id de doluyken ve eşitken tetiklenir.

| Alan | Değer |
|---|---|
| Grup | TRF — Transfers |
| Önem | Orta |
| Sınıf | Quality |
| Otorite kaynağı | PROJECT_QUALITY |
| Aşama | K4 |
| Varlık | Row |
| Kimlik alanı | - |
| Skor tabanı | 2 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_transfers()` içinde:
```
from_trip_id Some VE to_trip_id Some VE from_trip_id == to_trip_id
→ TRF_018 (satır bazlı notice)
```
- Kapsam birimi: satır (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** trip_id'lerden biri boşsa değerlendirilmez.
- **Yanlış pozitif:** Yok; aynı sefere aktarma her zaman anlamsızdır.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRF_018** | from_trip_id == to_trip_id | aynı sefer | ORTA · Quality · K4 |
| **TRF_006/007** | trip_id trips.txt'te yok | FK | KRİTİK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** Sefer aktarması aynı seferi gösteriyor
- **Çözüm önerisi:** `from_trip_id` ve `to_trip_id` farklı seferler olmalıdır.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"from_trip_id ve to_trip_id aynı sefer ('{trip_id}'); aktarmanın bir anlamı yok."*

## Ölçülen alan / değer
- **Dosya/alan:** `transfers.txt` → `from_trip_id`, `to_trip_id`.
- **Gözlenen değer:** Eşit trip_id'ler.
- **Beklenen değer:** Farklı `from_trip_id` ve `to_trip_id`.

## Kod değiştirirken minimum test matrisi
- from_trip_id == to_trip_id → TRF_018 çıkmalı.
- Farklı trip_id'ler → notice yok.
- Biri boş → değerlendirilmemeli.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 2.0** (ORTA).
- **R1 blocker değil:** Quality sınıfı R1 üretmez (R1 yalnız `GtfsSpec` + Spec + Kritik).
- **R5 genel skor:** Quality sınıf penaltısına girer (ağırlık 0.2).

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData ve GTFS Guru tarafında "from_trip_id == to_trip_id" için doğrulanmış birebir notice karşılığı tespit edilmedi; proje-özel kalite kuralıdır.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- **Doğrudan normatif karşılık yok:** GTFS Schedule Reference `transfers.txt`'te `from_trip_id == to_trip_id`'yi açıkça yasaklamaz; kendine-aktarma anlamsız-veri için proje-özel kalite yargısıdır (2026-07 otorite denetimi).
- 🔗 [GTFS Reference — transfers.txt](https://gtfs.org/documentation/schedule/reference/#transferstxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:1573](../../../crates/pipeline/src/k4_cross_ref.rs#L1573) (`check_transfers`)

# TRF_003 — from_stop_id veya to_stop_id bulunamadı

> **Karar:** Aktarma kaydındaki `from_stop_id` veya `to_stop_id` `stops.txt`'te tanımlı değil. KRİTİK, Spec. **Düzeltme:** Geçerli bir `stop_id` kullanın veya eksik durağı `stops.txt`'e ekleyin.
> ⚠️ Satır bazlı (Row); from ve to ayrı ayrı kontrol edilir, yalnızca dolu değerler için.

| Alan | Değer |
|---|---|
| Grup | TRF — Transfers |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Row |
| Kimlik alanı | `from_stop_id\|to_stop_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | `TRF_004`, `TRF_010`, `TRF_011`, `TRF_013`, `TRF_014`, `TRF_015`, `TRF_016` |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_transfers()` içinde:
```
from_stop_id / to_stop_id boş değil
VE map.stops stop_id'yi İÇERMİYOR
→ TRF_003 (satır bazlı, alan başına notice)
```
- Kapsam birimi: satır (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** stop_id boşsa TRF_001/002 kapsamındadır.
- **Yanlış pozitif:** Yok; stops.txt mevcut ama durak eksikse doğru tetiklenir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRF_003** | transfer stop FK yok | FK (stop) | KRİTİK · Spec · K4 |
| **TRF_001/002** | from/to_stop_id boş | zorunluluk | KRİTİK · Spec · K2 |
| **XFL_021** | transfer stop_id seferin duraklarında değil | trip-stop tutarlılığı | YÜKSEK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** from_stop_id veya to_stop_id bulunamadı
- **Çözüm önerisi:** Geçerli bir `stop_id` kullanın veya eksik durağı `stops.txt`'e ekleyin.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"{alan} '{stop_id}' stops.txt'te tanımlı değil."*

## Ölçülen alan / değer
- **Dosya/alan:** `transfers.txt` → `from_stop_id`/`to_stop_id`; karşılaştırma `stops.txt`.
- **Gözlenen değer:** Tanımsız `stop_id`.
- **Beklenen değer:** `stops.txt`'te kayıtlı `stop_id`.

## Kod değiştirirken minimum test matrisi
- stops.txt'te olmayan from_stop_id → TRF_003 çıkmalı.
- to_stop_id tanımsız → TRF_003 çıkmalı.
- İki stop da geçerli → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** Spec + KRİTİK → yayın engelleyici.
- **R5 genel skor:** Spec sınıf penaltısına girer.
- **Bağımlılık:** Sayılan TRF kurallarını maskeleyebilir.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
`blocks = [TRF_004, TRF_010, TRF_011, TRF_013, TRF_014, TRF_015, TRF_016]`.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `foreign_key_violation` (`transfers.txt::from_stop_id`/`to_stop_id` → `stops.txt::stop_id`).
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS `transfers.txt::from_stop_id`/`to_stop_id` → `stops.txt::stop_id` foreign key kuralı.
- 🔗 [GTFS Reference — transfers.txt](https://gtfs.org/documentation/schedule/reference/#transferstxt) · [stops.txt](https://gtfs.org/documentation/schedule/reference/#stopstxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:1701](../../../crates/pipeline/src/k4_cross_ref.rs#L1701) (`check_transfers`)

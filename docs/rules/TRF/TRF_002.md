# TRF_002 — to_stop_id eksik

> **Karar:** `transfers.txt::to_stop_id` boş. KRİTİK, Spec — aktarmanın hedefi tanımsız. **Düzeltme:** Aktarma kaydına `to_stop_id` ekleyin.
> ⚠️ Satır bazlı (Row); yalnızca alanın boşluğunu kontrol eder (FK kontrolü TRF_003'tedir). **Koşullu zorunlu:** yalnızca `transfer_type` 0/1/2/3 (veya boş) için; `transfer_type=4/5` (in-seat / scheduled connection) için `to_stop_id` opsiyoneldir (bunlar `from_trip_id`/`to_trip_id` kullanır) → bu satırlarda TRF_002 üretilmez.

| Alan | Değer |
|---|---|
| Grup | TRF — Transfers |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Row |
| Kimlik alanı | - |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::transfers::validate_transfers()` içinde:
```
transfer_type ∉ {4, 5}  VE  to_stop_id sütun-var-değer-boş
→ TRF_002 (satır bazlı notice)
```
- Kapsam birimi: satır (Row).
- `transfer_type` önce parse edilir; 4/5 ise stop_id koşulsuz atlanır.
- Sütun başlıkta hiç yoksa ARC_025 devralır (TRF_002 değil).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Yok.
- **Yanlış pozitif:** Yok; `transfer_type=4/5` (trip-bazlı in-seat transfer) satırları artık dışlanıyor.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRF_002** | to_stop_id boş | zorunluluk | KRİTİK · Spec · K2 |
| **TRF_001** | from_stop_id boş | zorunluluk | KRİTİK · Spec · K2 |
| **TRF_003** | from/to_stop_id stops.txt'te yok | FK | KRİTİK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** to_stop_id eksik
- **Çözüm önerisi:** Aktarma kaydına `to_stop_id` ekleyin.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"to_stop_id zorunludur."*

## Ölçülen alan / değer
- **Dosya/alan:** `transfers.txt` → `to_stop_id`.
- **Gözlenen değer:** Boş.
- **Beklenen değer:** Dolu `to_stop_id`.

## Kod değiştirirken minimum test matrisi
- Boş `to_stop_id` → TRF_002 çıkmalı.
- Dolu `to_stop_id` → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** Spec + KRİTİK → yayın engelleyici.
- **R5 genel skor:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `missing_required_field` (`transfers.txt::to_stop_id`).
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS `transfers.txt::to_stop_id` — koşullu zorunlu alan.
- 🔗 [GTFS Reference — transfers.txt](https://gtfs.org/documentation/schedule/reference/#transferstxt)

### Kod referansı
- Emit: [k2/transfers.rs:79](../../../crates/pipeline/src/k2/transfers.rs#L79) (`validate_transfers`)

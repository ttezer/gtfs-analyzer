# TRF_007 — to_trip_id bulunamadı

> **Karar:** `transfers.txt::to_trip_id` `trips.txt`'te tanımlı değil. KRİTİK, Spec. **Düzeltme:** Geçerli bir `trip_id` kullanın.
> ⚠️ Satır bazlı (Row); yalnızca dolu `to_trip_id` için FK kontrolü.

| Alan | Değer |
|---|---|
| Grup | TRF — Transfers |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Row |
| Kimlik alanı | `to_trip_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | `TRF_013`, `TRF_014`, `TRF_015`, `TRF_017`, `TRF_018`, `OPR_007` |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_transfers()` içinde:
```
to_trip_id Some VE map.trips İÇERMİYOR
→ TRF_007 (satır bazlı notice)
```
- Kapsam birimi: satır (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `to_trip_id` boşsa atlanır.
- **Yanlış pozitif:** Yok.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRF_007** | to_trip_id trips.txt'te yok | FK (trip) | KRİTİK · Spec · K4 |
| **TRF_006** | from_trip_id trips.txt'te yok | FK (trip) | KRİTİK · Spec · K4 |
| **TRF_018** | from_trip_id == to_trip_id | aynı sefer | YÜKSEK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** to_trip_id bulunamadı
- **Çözüm önerisi:** Geçerli bir `trip_id` kullanın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"to_trip_id '{trip_id}' trips.txt'te tanımlı değil."*

## Ölçülen alan / değer
- **Dosya/alan:** `transfers.txt` → `to_trip_id`; karşılaştırma `trips.txt`.
- **Gözlenen değer:** Tanımsız `trip_id`.
- **Beklenen değer:** `trips.txt`'te kayıtlı `trip_id`.

## Kod değiştirirken minimum test matrisi
- trips.txt'te olmayan to_trip_id → TRF_007 çıkmalı.
- `to_trip_id` boş → notice yok.
- Geçerli `to_trip_id` → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** Spec + KRİTİK → yayın engelleyici.
- **R5 genel skor:** Spec sınıf penaltısına girer.
- **Bağımlılık:** Sayılan TRF/OPR kurallarını maskeleyebilir.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
`blocks = [TRF_013, TRF_014, TRF_015, TRF_017, TRF_018, OPR_007]`.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `foreign_key_violation` (`transfers.txt::to_trip_id` → `trips.txt::trip_id`).
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS `transfers.txt::to_trip_id` → `trips.txt::trip_id` foreign key kuralı.
- 🔗 [GTFS Reference — transfers.txt](https://gtfs.org/documentation/schedule/reference/#transferstxt) · [trips.txt](https://gtfs.org/documentation/schedule/reference/#tripstxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:1777](../../../crates/pipeline/src/k4_cross_ref.rs#L1777) (`check_transfers`)

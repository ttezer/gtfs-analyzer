# BKR_006 — prior_notice_duration_min geçersiz

> **Karar:** `prior_notice_duration_min` dolu olduğu halde pozitif tamsayı olarak okunamıyorsa veya değeri `0` ya da negatifse BKR_006 üretilir. **Düzeltme:** `prior_notice_duration_min` değerini pozitif tam sayı dakika değeri olarak girin.
> ⚠️ Bu kural alan doluysa `booking_type` değerinden bağımsız çalışır; yani yanlış bağlamda verilmiş bir değer aynı zamanda format/değer hatası da alabilir.

| Alan | Değer |
| --- | --- |
| Grup | BKR — Booking Rules |
| Önem | Yüksek |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `booking_rule_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
Kod `prior_notice_duration_min` alanını önce tamsayıya çevirmeye çalışır. Parse başarısızsa veya parse edilen değer `<= 0` ise notice ekler.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Kuralın bağlı olduğu alan boş, parse edilemez veya önceki aşamada elenmişse bu notice çalışmayabilir.
- **Yanlış pozitif:** Kod koşulu sağlanıyorsa veri modeli açısından sorun kabul edilir; özel işletme yorumu varsa veri tarafında ayrıca modellenmelidir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **BKR_007** | `booking_type=1` için `prior_notice_duration_min` zorunludur. | Komşu kontrol | - |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** prior_notice_duration_min geçersiz (≤ 0 veya sayısal değil)

### R2 mesajı/mesajları
- `prior_notice_duration_min sıfır veya negatif olamaz.`
- **Çözüm önerisi:** `prior_notice_duration_min` değerini pozitif tam sayı dakika değeri olarak girin.

## Ölçülen alan / değer
- Ölçülen alan: `prior_notice_duration_min`.

## Kod değiştirirken minimum test matrisi
- Temel pozitif örnek: koşul sağlandığında notice üretilmeli.
- Temel negatif örnek: geçerli satırda notice üretilmemeli.
- Sınır değer: boş, geçersiz veya en küçük/geçerli değer ayrı denenmeli.
- İlgili komşu kural ile karışmama testi yapılmalı.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 3.0** (Yüksek).
- **Sınıf/Aşama:** Spec · K2.

### Hangi raporlarda görünür
`report_views = VS` → R2, R5, R9.

### Bağımlılık (bunu maskeleyen root cause'lar)
- Belirgin `blocks` ilişkisi yok; önce aynı satırdaki zorunlu kimlik ve parse hataları çözülmelidir.

### Dış araç eşleşmesi
2026-06-03 doğrulandı: MobilityData validator tarafındaki en yakın notice `invalid_prior_notice_duration_min` olarak geçer; BKR_006 ile aynı alanın pozitif/sayısal değer doğrulama ailesine denk gelir.

### GTFS spec referansı
[GTFS Schedule Reference - booking_rules.txt](https://gtfs.org/documentation/schedule/reference/#booking_rulestxt): `prior_notice_duration_min` tamsayıdır ve `booking_type=1` için zorunlu, diğer bağlamlarda yasaktır.

### Kod referansı
- Emit: [kaynak](../../../crates/pipeline/src/k2/booking_rules.rs)

# BKR_019 — booking_rule_id eksik veya yineleniyor

> **Karar:** `booking_rule_id`, `booking_rules.txt`'in birincil anahtarıdır (spec: "Primary key (`booking_rule_id`)", tip `Unique ID`, **Required**). Değer boşsa ya da dosyada daha önce görülmüşse BKR_019 üretilir. **Düzeltme:** her satıra benzersiz ve dolu bir id verin. Spec [Kesin], KRİTİK, Spec.

| Alan | Değer |
| --- | --- |
| Grup | BKR — Booking Rules |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `booking_rule_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | `BKR_017`, `BKR_018` |

## Tetikleme koşulu (kesin mantık)
K2 `booking_rules.txt` satırlarını sırayla gezer:
- Değer boşsa (sütun mevcutken) → "eksik" kolu.
- Değer daha önce görülmüşse → "yineleniyor" kolu; notice **ikinci ve sonraki** satırda üretilir, ilk tanım temiz kalır.

Sütun başlıkta hiç yoksa kural susar; ARC_025 devralır (RTS_004 deseni).

## Yanlış pozitif / negatif (asıl tasarım riski)
- Yinelenen id, `stop_times`'tan gelen referansları belirsizleştirir: hangi kural geçerli bilinemez. Bu yüzden `blocks` listesi BKR_017/018'i sayar — anahtar düzeltilmeden FK bulguları güvenilir değildir.
- Büyük/küçük harf farkı ayrı id sayılır (GTFS id'leri case-sensitive'dir); bilinçli.
- Baştaki/sondaki boşluk `get_trimmed_field` ile kırpılır → "BR1" ile "BR1 " aynı sayılır ve yineleme olarak raporlanır (doğru davranış; DQ_016 boşluğu ayrıca bildirir).

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **BKR_019** | `booking_rule_id` boş veya yineleniyor | birincil anahtar | KRİTİK · Spec · K2 |
| **BKR_016** | `booking_type` eksik/geçersiz | zorunlu enum | KRİTİK · Spec · K2 |
| **BKR_017/018** | stop_times'tan gelen referans çözülemiyor | dışa referans | KRİTİK · Spec · K4 |
| **TRP_001** | `trip_id` eksik veya yineleniyor | aynı desen, trips.txt | KRİTİK · Spec · K2 |
| **ARC_025** | `booking_rule_id` SÜTUNU yok | header düzeyi | KRİTİK · Spec · K1 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** booking_rule_id eksik veya yineleniyor
- **Çözüm önerisi:** Her rezervasyon kuralına benzersiz bir booking_rule_id verin.

### R2 mesajı/mesajları
- `booking_rule_id zorunludur.` (boş değer)
- `booking_rule_id '<id>' yineleniyor; bu alan dosyanın birincil anahtarıdır.`

## Ölçülen alan / değer
- `file`: `booking_rules.txt` · `field`: `booking_rule_id`
- `observed`: boş dize veya yinelenen id

## Kod değiştirirken minimum test matrisi
| Senaryo | Beklenen |
|---|---|
| İki satırda aynı `booking_rule_id` | tek `BKR_019` (2. satırda) |
| Boş `booking_rule_id` | `BKR_019` |
| Benzersiz id'ler | `BKR_019` yok |
| `booking_rule_id` sütunu YOK | `BKR_019` yok (ARC_025) |

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (Kritik).
- **Sınıf/Aşama:** Spec · K2.

### Hangi raporlarda görünür
`report_views = VS_K` → R1, R2, R5, R9.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `booking_rules.txt` yoksa kural çalışmaz; sütun yoksa ARC_025 önce gelir.

### Dış araç eşleşmesi
2026-07-25 doğrulandı: MobilityData'da alan-bazlı karşılık yok; generic `duplicate_key` (ERROR) ve `missing_required_field` (ERROR) bu durumu kapsayabilir. Generic catch-all EXACT parite sayılmaz (TRP_001 aynı gerekçeyle Spec) → sınıf `Spec`.

### GTFS spec referansı
[GTFS Schedule Reference - booking_rules.txt](https://gtfs.org/documentation/schedule/reference/#booking_rulestxt): "Primary key (`booking_rule_id`)"; `booking_rule_id` — Unique ID, **Required**, "Identifies a rule."

### Kod referansı
- Emit: [kaynak](../../../crates/pipeline/src/k2/booking_rules.rs)

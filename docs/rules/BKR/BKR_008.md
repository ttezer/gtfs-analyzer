# BKR_008 — booking_type=2 için prior_notice_last_day zorunlu

> **Karar:** `booking_type=2` önceki gün rezervasyonu demektir. Bu satırda `prior_notice_last_day` boşsa BKR_008 üretilir. **Düzeltme:** Önceki gün rezervasyonu için `prior_notice_last_day` değerini girin.
> ⚠️ Bu kural kritik görünürlükte (`VS_K`) çıkar; önceki gün rezervasyonunda son gün sınırı olmadan rezervasyon penceresi eksik kalır.

| Alan | Değer |
| --- | --- |
| Grup | BKR — Booking Rules |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `booking_rule_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
Kod `booking_type == 2` ve `prior_notice_last_day` boş olduğunda notice ekler.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Kuralın bağlı olduğu alan boş, parse edilemez veya önceki aşamada elenmişse bu notice çalışmayabilir.
- **Yanlış pozitif:** Kod koşulu sağlanıyorsa veri modeli açısından sorun kabul edilir; özel işletme yorumu varsa veri tarafında ayrıca modellenmelidir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **BKR_009** | `booking_type=2` için `prior_notice_last_time` zorunludur. | Komşu kontrol | - |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** booking_type=2 için prior_notice_last_day zorunlu

### R2 mesajı/mesajları
- `booking_type=2 iken prior_notice_last_day zorunludur.`
- **Çözüm önerisi:** Önceki gün rezervasyonu için `prior_notice_last_day` değerini girin.

## Ölçülen alan / değer
- Ölçülen alanlar: `booking_type`, `prior_notice_last_day`.

## Kod değiştirirken minimum test matrisi
- Temel pozitif örnek: koşul sağlandığında notice üretilmeli.
- Temel negatif örnek: geçerli satırda notice üretilmemeli.
- Sınır değer: boş, geçersiz veya en küçük/geçerli değer ayrı denenmeli.
- İlgili komşu kural ile karışmama testi yapılmalı.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (Kritik).
- **Sınıf/Aşama:** Spec · K2.

### Hangi raporlarda görünür
`report_views = VS_K` → R1, R2, R5, R9.

### Bağımlılık (bunu maskeleyen root cause'lar)
- Belirgin `blocks` ilişkisi yok; önce aynı satırdaki zorunlu kimlik ve parse hataları çözülmelidir.

### Dış araç eşleşmesi
2026-06-03 doğrulandı: MobilityData validator tarafındaki yakın eşleşme `missing_prior_notice_last_day` notice'ıdır.

### GTFS spec referansı
[GTFS Schedule Reference - booking_rules.txt](https://gtfs.org/documentation/schedule/reference/#booking_rulestxt): `prior_notice_last_day`, `booking_type=2` için zorunludur ve diğer durumlarda yasaktır.

### Kod referansı
- Emit: [kaynak](../../../crates/pipeline/src/k2/booking_rules.rs)

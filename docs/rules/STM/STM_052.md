# STM_052 — Flex penceresi tanımlıyken drop_off_type=0 yasak

> Karar: Bir stop_times satırında `start_pickup_drop_off_window` veya `end_pickup_drop_off_window` tanımlıyken `drop_off_type` 0 (düzenli) kullanılamaz — talep-üzerine (Flex) hizmet düzenli iniş ile çelişir. GTFS spec [Kesin]. MD `forbidden_drop_off_type` karşılığı. YÜKSEK, Spec.

| Alan | Değer |
|---|---|
| Grup | STM — Stop Times |
| Önem | Yüksek |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `trip_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
K2 stop_times satırında `has_any_window` (start veya end pickup/drop-off penceresi dolu) iken `drop_off_type == 0` ise STM_052 üretilir. GTFS reference: "`drop_off_type=0` forbidden if `start_pickup_drop_off_window` or `end_pickup_drop_off_window` are defined." (pickup_type'tan farkı: drop_off için yalnız 0 yasak, 3 değil.)

## Yanlış pozitif / negatif (asıl tasarım riski)
- Koşul pencere alanlarına bağlıdır (location_id değil). Boş `drop_off_type` örtük 0 olsa da susulur (FP riski). STM_051 (pickup) ile aynı tarama ve veriden üretilir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **STM_052** | Flex penceresi + `drop_off_type` 0 (yasak) | drop-off Flex çelişkisi | YÜKSEK · Spec · K2 |
| **STM_051** | Flex penceresi + `pickup_type` 0/3 (yasak) | pickup Flex çelişkisi | YÜKSEK · Spec · K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** Flex penceresi tanımlıyken drop_off_type=0 yasak (talep-üzerine değer gerekli)
- **Çözüm önerisi:** Flex penceresi olan stop_times satırlarında drop_off_type'ı 1 (iniş yok) veya 2 (telefonla) yapın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"trip_id '<id>' Flex penceresi tanımlı iken drop_off_type=0 yasaktır."*

## Ölçülen alan / değer
- `file`: `stop_times.txt`
- `observed`: `0`
- `expected`: 1 veya 2

## Kod değiştirirken minimum test matrisi
| Senaryo | Beklenen |
|---|---|
| Flex penceresi yok | `STM_052` yok |
| Flex penceresi + `drop_off_type=1` | `STM_052` yok |
| Flex penceresi + `drop_off_type=0` | `STM_052` |
| Flex penceresi + `drop_off_type` boş | `STM_052` yok (örtük 0'a susulur) |

---
## Teknik ek
### Skora katkı
`Yüksek`, taban 1: teorik skor katkısı `1 * 3.0 = 3.0`.

### Hangi raporlarda görünür
`VS`: `R2`, `R5`, `R9`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- Flex pencere alanları (`start`/`end_pickup_drop_off_window`) tanımlı değilse kural tetiklenmez.

### Dış araç eşleşmesi
- MobilityData `forbidden_drop_off_type` karşılığıdır (drop-off için yalnız 0 yasak, 3 değil).

### GTFS spec referansı
- GTFS Schedule Reference — [stop_times.txt](https://gtfs.org/documentation/schedule/reference/#stop_timestxt) ilgili alan/dosya tanımını verir.

### Kod referansı
- [`registry.rs`](../../../crates/rules/src/registry.rs)
- [`stop_times.rs`](../../../crates/pipeline/src/k2/stop_times.rs)

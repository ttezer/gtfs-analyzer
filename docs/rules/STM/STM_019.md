# STM_019 — continuous_drop_off geçersiz (stop_times)

> Karar: `continuous_drop_off` verilmişse GTFS enum aralığı olan `0-3` içinde olmalıdır.
> ⚠️ Boş alan hata değildir.

| Alan | Değer |
|---|---|
| Grup | STM — Stop Times |
| Önem | Orta |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Row |
| Kimlik alanı | `trip_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
K2 `continuous_drop_off` alanını `parse_pickup_dropoff_col` ile okur.

Alan boşsa kural çalışmaz. Alan dolu ama unsigned integer olarak parse edilemezse veya parse edilen değer `3`ten büyükse STM_019 üretilir.

## Agregasyon davranışı (#151)

Predikat değişmez; yalnız raporlama granülerliği hacme göre değişir:

- `continuous_drop_off` ihlali **50 satırı aşarsa** (`>50`) satır-başına notice'lar yerine tek `EntityType::Feed` STM_019 özeti üretilir.
- Özetin `observed` değeri ve `details.affected_rows` gerçek ihlal sayısını taşır; `details.example_trips` deterministik olarak sıralanmış ilk 5 `trip_id` örneğidir.
- İhlal sayısı `≤50` ise trip/satır seviyesindeki notice'lar korunur; düşük hacimde pinpoint bilgi kaybolmaz.

Eşik, SHP_010/STP_022 emsallerindeki 50 değeriyle aynıdır: yüksek hacimli üretici tekrarını bastırırken ntd-332 gibi düşük hacimli feed'lerde ayrıntıyı korur.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Boş alan hata değildir.
- **Yanlış pozitif:** `4`, `true`, `false` gibi değerler GTFS enum değildir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **STM_018** | `continuous_pickup` geçersiz | continuous biniş | ORTA · Spec · K2 |
| **STM_019** | `continuous_drop_off` geçersiz | continuous iniş | ORTA · Spec · K2 |
| **RTS_019** | route tarafında continuous drop-off geçersiz | route seviyesi | Spec |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** continuous_drop_off geçersiz (stop_times)
- **Çözüm önerisi:** `continuous_drop_off` alanını `0`, `1`, `2` veya `3` değerlerinden biri yapın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"continuous_drop_off 0, 1, 2 veya 3 olmalıdır."*
- **Ayrıntılı bulgu mesajı:** *"'continuous_drop_off' için u32 bekleniyor, alınan: <değer>"*
- **Ayrıntılı çözüm:** *"Alanı geçerli bir GTFS biniş/iniş enum değerine (0-3) ayarlayın."*

## Ölçülen alan / değer
- `file`: `stop_times.txt`
- `field`: `continuous_drop_off`
- `line`: satır seviyesinde ihlalin satır numarası; feed özetinde yok
- `observed`: satır seviyesinde geçersiz değer; feed özetinde toplam ihlal sayısı
- `expected`: `0-3`

## Kod değiştirirken minimum test matrisi
| Senaryo | Beklenen |
|---|---|
| alan boş | `STM_019` yok |
| değer `4` | `STM_019` |
| değer `0..3` | `STM_019` yok |
| `>50` ihlal | tek feed-özeti; toplam sayı + sıralı ilk 5 örnek |
| `≤50` ihlal | tekil notice'lar korunur |

---

## Teknik ek
### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData tarafında bu grubun yakın karşılıkları `foreign_key_violation`, `invalid_time`, `missing_required_field`, `invalid_row_length`, `forbidden_arrival_or_departure_time`, `invalid_pickup_drop_off_window`, `missing_pickup_or_drop_off_window`, `location_with_unexpected_stop_time`, `forbidden_shape_dist_traveled` ve `decreasing_or_equal_stop_time_distance` notice'larıdır. Bu projedeki hız, segment mesafesi ve büyük hacim kontrollerinin bir bölümü proje-özel eşiklerle çalışır.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS Schedule Reference: `stop_times.txt` ana tablosu; ilgili çapraz referanslarda `trips.txt`, `stops.txt`, `shapes.txt`, `locations.geojson` ve `location_groups.txt` alanları.
- 🔗 https://gtfs.org/documentation/schedule/reference/#stop_timestxt

### Skora katkı
`Orta`, taban 1: teorik skor katkısı `1 * 2.0 = 2.0`.

### Hangi raporlarda görünür
`VS`: `R2`, `R5`, `R9`.

### Kod referansı
- [`registry.rs`](../../../crates/rules/src/registry.rs#L672)
- [`stop_times.rs`](../../../crates/pipeline/src/k2/stop_times.rs#L1684)
- [`stop_times.rs`](../../../crates/pipeline/src/k2/stop_times.rs#L1684)

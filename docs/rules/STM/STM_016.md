# STM_016 — Son duraklama noktası yok

> Karar: Bir seferin son stop_times kaydında `arrival_time` bulunmalıdır.
> ⚠️ `departure_time` eksikliği bu kartın konusu DEĞİL, **STM_034**'ün konusudur
> ("varış veya kalkış zamanından yalnızca biri tanımlı").
> ⚠️ Son satırdaki arrival_time parse edilemediyse STM_003 üretilir, STM_016 bu akışta ana belirti olmayabilir.

| Alan | Değer |
|---|---|
| Grup | STM — Stop Times |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K6 |
| Varlık | Row |
| Kimlik alanı | `trip_id` |
| Skor tabanı | 2 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Spec dayanağı ve kapsam (2026-07-29'da otoriteye göre ayrıldı)
Spec `arrival_time` için *"Required for the **first and last stop** in a trip"* der — bu
**normatif** hükümdür ve bu kuralın dayanağıdır. `departure_time`'da böyle bir madde
**yoktur** (yalnız `timepoint=1` için zorunludur); MD `missing_trip_edge` yine de ikisini
birden arar ve gerekçesini spec'in *"enter the same value"* **önerisine** dayandırır.

Bu kural bu yüzden yalnız `arrival_time`'ı denetler. `departure_time` tarafını **STM_034**
(Interop, K2 — "yalnız biri tanımlı") zaten karşılar; ayrı bir kural gerekmez. Tek kuralda
birleştirilirse `Spec` sınıfı, hiçbir normatif hükmü ihlal etmeyen bir vakayı da R1 yayın
kapısına sokardı.

**Flex guard (ZORUNLU):** spec her iki alan için *"**Forbidden** when
start_pickup_drop_off_window or end_pickup_drop_off_window are defined"* der. Flex pencereli
bir kayıtta eksik zaman alanı ihlal DEĞİL, beklenen durumdur;
`stm015_016_silent_for_flex_pickup_drop_off_window` testi bunu korur.

## Tetikleme koşulu (kesin mantık)
K6 her trip için sıralanmış stop_times listesinin son kaydına bakar.

Son kayıtta `arrival_time` yoksa STM_016 üretilir. Format bozukluğu STM_003 konusudur; bu kural parse sonrası eksiklik kontrolüdür.

## Dolu ama bozuk değer — bu kural o vakada SUSAR (#175)

🔴 `arrival_time()` accessor'ı "alan yok" ile "alan dolu ama ayrıştırılamadı" vakalarını
AYNI `None` değerine indiriyordu. Kural son durakta `is_none()` sorduğu için, `HH:MM`
yazılmış (saniyesiz) bir değeri "eksik" sayıyordu. `mdb-2727`'de **101.621**, `mdb-3401`'de
**10.884** bulgu bu şekilde üretildi ve aynı satırlarda `STM_003` zaten doğrusunu
söylüyordu: "HH:MM:SS bekleniyordu".

Kural **Spec · Kritik** olduğu için bu yanlış iddia R1 yayın kapısına giriyordu — geçerli
olmayan ama DOĞRU KURALLA raporlanmış bir feed, yanlış kural yüzünden ikinci kez ve yanlış
gerekçeyle reddediliyordu.

Düzeltme `TIME_MALFORMED` sentinelidir (`k2/stop_times.rs`): parse hatası `u32::MAX`'e
düşmez, ayrı bir değer alır. Kural artık `arrival_is_absent()` sorar. Ölçüm: `mdb-3401`
STM_016 **10.884 → 0**, `STM_003` 218.087'de sabit.

⚠️ **Bu bir IDIOM hatasıdır, alan hatası değil.** `Err(_) => None` deseninin aynı ailesi
bu depoda beş kez ısırdı (`STM_058`, `PTH_027`, `RTS_029`, `TRP_034`, ve burası). "Değer
yok" iddiasında bulunan her kural, değerin VARLIĞINI ayrı sormalıdır.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Son satırdaki arrival_time parse edilemediyse STM_003 üretilir, STM_016 bu akışta ana belirti olmayabilir.
- **Yanlış pozitif:** Ara duraklarda zaman boş bırakılabilir; son varış zamanı sefer süresini belirleyen temel bilgidir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **STM_015** | ilk durakta `arrival_time` eksik | normatif hüküm |  KRİTİK · Spec · K6 |
| **STM_016** | son durakta `arrival_time` eksik | normatif hüküm |  KRİTİK · Spec · K6 |
| **STM_028** | toplam sefer süresi çok uzun | süre analizi | YÜKSEK · Analytics · K6 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** Son durakta arrival_time eksik
- **Çözüm önerisi:** Her seferin son stop_times satırına geçerli `arrival_time` girin.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"trip_id '<trip_id>' seferinin son durağında arrival_time eksik."*
- **Ayrıntılı çözüm:** *"Son stop_times satırına arrival_time girin."*

## Ölçülen alan / değer
- `file`: `stop_times.txt`
- `field`: `arrival_time`
- `line`: trip'in son stop_times satırı
- `observed`: boş
- `expected`: `HH:MM:SS`

## Kod değiştirirken minimum test matrisi
| Senaryo | Beklenen |
|---|---|
| son durakta arrival_time yok | `STM_016` |
| son durakta arrival_time var | `STM_016` yok |
| son durakta arrival_time bozuk | `STM_003` |

---

## Teknik ek
### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData tarafında bu grubun yakın karşılıkları `foreign_key_violation`, `invalid_time`, `missing_required_field`, `invalid_row_length`, `forbidden_arrival_or_departure_time`, `invalid_pickup_drop_off_window`, `missing_pickup_or_drop_off_window`, `location_with_unexpected_stop_time`, `forbidden_shape_dist_traveled` ve `decreasing_or_equal_stop_time_distance` notice'larıdır. Bu projedeki hız, segment mesafesi ve büyük hacim kontrollerinin bir bölümü proje-özel eşiklerle çalışır.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS Schedule Reference: `stop_times.txt` ana tablosu; ilgili çapraz referanslarda `trips.txt`, `stops.txt`, `shapes.txt`, `locations.geojson` ve `location_groups.txt` alanları.
- 🔗 https://gtfs.org/documentation/schedule/reference/#stop_timestxt

### Skora katkı
`Kritik`, taban 2: teorik skor katkısı `2 * 4.0 = 8.0`.

### Hangi raporlarda görünür
`VS_K`: `R1`, `R2`, `R5`, `R9`.

### Kod referansı
- [`registry.rs`](../../../crates/rules/src/registry.rs#L543)
- [`k6_analytics.rs`](../../../crates/pipeline/src/k6_analytics.rs#L3565)


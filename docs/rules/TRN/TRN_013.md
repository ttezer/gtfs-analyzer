# TRN_013 — feed_info çevirisinde kimlik alanı kullanılamaz

> **Karar:** `table_name=feed_info` çevirisinde `record_id`, `record_sub_id` veya `field_value` kullanılmış; feed_info tek satırlı olduğu için bu adresleme alanları geçersizdir. YÜKSEK, Spec. **Düzeltme:** feed_info satırları için bu alanları boş bırakın.
> ⚠️ Satır bazlı (Row); yalnızca `table_name=feed_info` satırlarında kimlik/değer alanlarının varlığını kontrol eder.

| Alan | Değer |
|---|---|
| Grup | TRN — Translations |
| Önem | Yüksek |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Row |
| Kimlik alanı | - |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::translations::validate_translations()` içinde:
```
table_name == "feed_info"
VE (record_id Some VEYA record_sub_id Some VEYA field_value Some)
→ TRN_013 (satır bazlı notice)
```
- Kapsam birimi: satır (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `table_name` feed_info değilse değerlendirilmez.
- **Yanlış pozitif:** Yok; feed_info tek satırlı olduğu için adresleme alanları gereksizdir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRN_013** | feed_info çevirisinde kimlik alanı | feed_info kısıtı | YÜKSEK · Spec · K2 |
| **TRN_009** | record_id ve field_value birlikte | adresleme çakışması | YÜKSEK · Spec · K2 |
| **XFL_016** | feed_info çevirisi var ama feed_info.txt yok | dosya yokluğu | YÜKSEK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** feed_info çevirisinde kimlik alanı kullanılamaz
- **Çözüm önerisi:** feed_info satırları için `record_id`, `record_sub_id` ve `field_value` alanlarını boş bırakın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"feed_info çevirileri record_id, record_sub_id veya field_value kullanamaz."*

## Ölçülen alan / değer
- **Dosya/alan:** `translations.txt` → `record_id`/`record_sub_id`/`field_value` (+`table_name`).
- **Gözlenen değer:** feed_info satırında dolu kimlik/değer alanı.
- **Beklenen değer:** feed_info satırlarında bu alanların boş olması.

## Kod değiştirirken minimum test matrisi
- feed_info + record_id dolu → TRN_013 çıkmalı.
- feed_info + tüm adresleme alanları boş → notice yok.
- başka tablo + record_id → TRN_013 çıkmamalı.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 3.0** (YÜKSEK).
- **R1 blocker değil:** Spec + YÜKSEK (KRİTİK değil).
- **R5 genel skor:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData ve GTFS Guru tarafında "feed_info çevirisinde kimlik alanı kısıtı" için doğrulanmış birebir notice karşılığı tespit edilmedi; proje-özel Spec kuralıdır.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS `translations.txt` — `feed_info` çevirileri `record_id`/`record_sub_id`/`field_value` kullanamaz.
- 🔗 [GTFS Reference — translations.txt](https://gtfs.org/documentation/schedule/reference/#translationstxt)

### Kod referansı
- Emit: [k2/translations.rs:214](../../../crates/pipeline/src/k2/translations.rs#L214) (`validate_translations`)

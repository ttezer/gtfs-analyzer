# TRN_016 — field_value hiçbir kayıtla eşleşmiyor — çeviri uygulanmıyor

> Karar: `field_value` biçimli bir çeviri, hedef alanın değeriyle **birebir** eşleşmelidir.
> Spec: *"The field must have exactly the value defined in `field_value`."* Eşleşen satır
> yoksa çeviri **sessizce etkisizdir** — üretici çevirdiğini sanır, çevrilmez.
> ⚠️ Feed başına **tek** özet notice üretilir (sayı + ilk 5 örnek).

| Alan | Değer |
|---|---|
| Grup | TRN — Çeviriler |
| Önem | Orta |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Feed |
| Kimlik alanı | - |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)

```
translations satırı: field_value DOLU  VE  record_id BOŞ
  VE table_name ham satır tutan veya typed/indexed değerleri korunan bir tabloya çözülüyor
  VE field_name o tabloda GERÇEKTEN bir sütun
  VE hiçbir satırda o sütunun değeri field_value'ya EŞİT değil
→ sayaç +1  (feed sonunda tek notice)
```

Karşılaştırma **birebir**dir: baştaki/sondaki boşluk ve büyük/küçük harf farkı eşleşmeyi
bozar — spec "exactly" der.

## Yanlış pozitif / negatif (asıl tasarım riski)

Predicate ölçüm sırasında **iki kez daraltıldı**; ikisi de kartta duruyor çünkü ikisi de
başka bir kuralın alanına girme riskiydi.

- **`record_id` biçimi hariç.** O kol `XFL_014`'ün alanı (çözümlenemeyen çeviri referansı).
- **`field_name` sütun değilse hariç.** O ihlal `TRN_002`'nin ("field_name bu tablo için
  geçersiz") alanı. İlk ölçümde bu ayrım yapılmadığında tek feed'de **2546 sahte bulgu**
  çıktı — `routes.long_name` yazan satırlar, doğrusu `route_long_name`.

- **Kapsam:** Ham satır (`row`) taşıyan tabloların yanı sıra `trips` ve `stop_times` da
  denetlenir. `trips` için `trip_headsign` ve `trip_short_name` intern tabloları;
  `stop_times` için `stop_headsign` side-map'i ve mevcut `StopTimesIndex` kullanılır.
  Bu genişleme ham satır kopyası eklemez ve satır sayısıyla büyüyen ikinci bir `RowMap`
  deposu oluşturmaz.
- **Kapsam sınırı:** Dosya yoksa/boşsa veya `field_name` hedef dosyada sütun değilse
  kontrol sessiz kalır; ikinci durum `TRN_002`'nin alanıdır. Desteklenen typed/indexed
  alanlarda değer bulunamadığında ise TRN_016 üretilir.
- **Eski ölçüm notu:** 2026-08-04 tarihli 239-feed ölçümü yalnız ham satır tutan tablo
  kapsamını içeriyordu. `trips` ve `stop_times` typed/indexed eşleşmesi eklendiği için
  eski 841/154 dağılımı güncel kapsam için kullanılmamalıdır; yeni korpus sayımı ayrı
  bir ölçüm olarak yeniden üretilmelidir.
- **Yanlış negatif:** Desteklenen `trips.trip_headsign`, `trips.trip_short_name` ve
  `stop_times.stop_headsign` alanlarında yok; eşleşme typed/indexed değerler üzerinden
  yapılır. Dosya/sütun yokluğu kapsam gereği atlanır.
- **Yanlış pozitif:** Düşük. Eşleşme gerçekten yoksa çeviri gerçekten uygulanmaz.

## Karışan komşular (ayrım)

| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRN_016** | `field_value` hiçbir satırla eşleşmiyor | değere göre eşleşme | ORTA · Spec · K4 |
| **XFL_014** | `record_id` çözülemiyor | kimliğe göre eşleşme | — · Spec · K4 |
| **TRN_002** | `field_name` o tabloda yok | alan adı geçerliliği | — · Spec · K2 |
| **TRN_009** | `record_id` ve `field_value` birlikte | karşılıklı dışlama | — · Spec · K2 |
| **TRN_015** | ikisi de boş | zorunluluk | YÜKSEK · Spec · K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** field_value hiçbir kayıtla eşleşmiyor — çeviri uygulanmıyor
- **Çözüm önerisi:** `field_value` değerlerini hedef alanın gerçek içeriğiyle birebir
  eşleştirin (baştaki/sondaki boşluk ve büyük/küçük harf dahil).

### R2 mesajı/mesajları
- *"<N> çeviri kaydının 'field_value' değeri hedef dosyada hiçbir satırla eşleşmiyor — bu
  çeviriler uygulanmaz. Örnek: stops.stop_name=…"*

## Ölçülen alan / değer
- `field`: `field_value`
- `observed`: eşleşmeyen benzersiz çeviri sayısı

## Kod değiştirirken minimum test matrisi
| Senaryo | Beklenen |
|---|---|
| `field_value` hedefte VAR | sessiz |
| `field_value` hedefte YOK | `TRN_016` ×1, mesajda örnek |
| `record_id` biçimi | sessiz (`XFL_014`) |
| `field_name` sütun değil (`long_name`) | sessiz (`TRN_002`) |
| `table_name=trips` veya `stop_times`, değer VAR | sessiz |
| `table_name=trips` veya `stop_times`, değer YOK | `TRN_016` |

## Teknik ek
### Dış araç eşleşmesi
- **MD paritesi:** Yok.

### GTFS spec referansı
- GTFS Schedule Reference, `translations.txt` → `field_value`
- 🔗 https://gtfs.org/documentation/schedule/reference/#translationstxt

### Korpus ölçümü (2026-08-04, 239 feed)
6 feed · 998 eşleşmeyen kayıt. En yoğunu `mdb-2848`: 2413 `stops.stop_name` çevirisinin
**810'u** hiçbir durak adıyla eşleşmiyor (büyük/küçük harf farkıyla bile değil) — feed'in
çevirilerinin yaklaşık üçte biri ölü. Üreteç: `spec-audit/measure_gaps.py`.

### Skora katkı
`Orta`, taban 1: teorik skor katkısı `2 * 1.0 = 2.0`.

### Hangi raporlarda görünür
`VS`: `R2`, `R5`, `R9`.

### Kod referansı
- [`registry.rs`](../../../crates/rules/src/registry.rs#L1591)
- [`k4_cross_ref.rs`](../../../crates/pipeline/src/k4_cross_ref.rs#L5925)

# TRN_004 — record_id bulunamadı

> **Karar:** `translations.txt::record_id`, belirtilen `table_name` tablosunda bulunamadı. YÜKSEK, Spec — çeviri sahipsiz kalır. **Düzeltme:** Mevcut bir kaydın ID'sini kullanın ya da satırı kaldırın.
> ⚠️ Satır bazlı (Row); arama yalnızca kimliği çözülebilen tablolar için (agency/stops/routes/trips/calendar/levels/pathways/fare_attributes/attributions). feed_info/stop_times/shapes vb. için `exists=true` sayılır (kimlik ya yasaktır ya da tek alanla çözülemez). Eşleme `translation_record_exists()` içinde tekil kaynaktır; XFL_014 aynı fonksiyonu kullanır.

| Alan | Değer |
|---|---|
| Grup | TRN — Translations |
| Önem | Yüksek |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Row |
| Kimlik alanı | `record_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_translations()` içinde:
```
translations.record_id Some
VE table_name'e göre ilgili map'te record_id YOK (lookup yapılabilen tablolar)
→ TRN_004 (satır bazlı notice)
```
- Kapsam birimi: satır (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `feed_info`, `stop_times`, `shapes`, `frequencies`, `transfers`, `fare_rules`, `translations` için kimlik çözülemez; bu tablolara çeviri referansı kontrol edilmez.
- 🔴 **Kapatılan körlük (2026-08-29):** `attributions` de "lookup not feasible" listesindeydi, oysa `attribution_id` K2 kayıtlarında mevcuttur. Sonuç: `attributions` çevirilerinin referans bütünlüğü dil bağımsız eksende hiç denetlenmiyordu; aynı satırı JPN_019 yalnız `ja-Hrkt` için denetliyor, üstelik ters yönde hüküm veriyordu (Katori City feed'i). Eşleme artık TRN_004 ve XFL_014 arasında tekil.
- **Yanlış pozitif:** Yok; lookup yapılabilen tablolarda kayıt eksikse doğru tetiklenir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRN_004** | Çeviri record_id kaynak tabloda yok (satır) | FK (record), per-row | YÜKSEK · Spec · K4 |
| **XFL_014** | Aynı sorun feed-level özet | FK (record), feed | ORTA · Quality · K4 |
| **JPN_019** | Aynı FK, yalnız `ja-Hrkt` satırlarında; GTFS-JP kana ekseni | FK (record), dil filtreli | ORTA · Interop · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** record_id bulunamadı
- **Çözüm önerisi:** Mevcut bir kaydın ID'sini kullanın ya da satırı kaldırın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"record_id '{record_id}' table_name='{table_name}' tablosunda bulunamadı."*

## Ölçülen alan / değer
- **Dosya/alan:** `translations.txt` → `record_id` (+`table_name`); karşılaştırma ilgili kaynak tablo.
- **Gözlenen değer:** Tanımsız `record_id`.
- **Beklenen değer:** Kaynak tabloda var olan `record_id`.

## Kod değiştirirken minimum test matrisi
- stops + olmayan `record_id` → TRN_004 çıkmalı.
- Geçerli `record_id` → notice yok.
- feed_info/stop_times referansı → lookup yapılmaz, notice yok.
- `attributions` + attributions.txt'te mevcut `attribution_id` → notice yok.
- `attributions` + olmayan `attribution_id`, dil `en` → TRN_004 (JPN_019 yalnız `ja-Hrkt`'e baktığı için susar).

---
## Teknik ek

### Skora katkı
- **severity.weight() = 3.0** (YÜKSEK).
- **R1 blocker değil:** Spec + YÜKSEK (KRİTİK değil).
- **R5 genel skor:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da birebir karşılık `translation_foreign_key_violation` (çeviri `record_id`'si kaynak tabloda yok).
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS `translations.txt::record_id` → ilgili `table_name` tablosu foreign key kuralı.
- 🔗 [GTFS Reference — translations.txt](https://gtfs.org/documentation/schedule/reference/#translationstxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:2921](../../../crates/pipeline/src/k4_cross_ref.rs#L2921) (`check_translations`)

# TRN_010 — record_sub_id geçersiz

> **Karar:** `table_name=stop_times` çevirisinde `record_sub_id` (stop_sequence) zorunlu olduğu halde boş. YÜKSEK, Spec. **Düzeltme:** `record_sub_id` değerini ilgili `stop_sequence` değerine ayarlayın.
> ⚠️ Satır bazlı (Row); yalnızca `table_name=stop_times` satırlarında `record_sub_id` eksikliğini kontrol eder.

| Alan | Değer |
|---|---|
| Grup | TRN — Translations |
| Önem | Yüksek |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Row |
| Kimlik alanı | `record_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::translations::validate_translations()` içinde:
```
table_name == "stop_times" VE record_sub_id None
→ TRN_010 (satır bazlı notice)
```
- Kapsam birimi: satır (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `table_name` stop_times değilse değerlendirilmez.
- **Yanlış pozitif:** Yok; stop_times çevirisi `record_sub_id` (stop_sequence) gerektirir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRN_010** | stop_times çevirisinde record_sub_id eksik | zorunluluk | YÜKSEK · Spec · K2 |
| **TRN_014** | record_sub_id stop_times dışında kullanılmış | kapsam | YÜKSEK · Spec · K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** record_sub_id geçersiz
- **Çözüm önerisi:** `record_sub_id` değerini `stop_sequence` değerine ayarlayın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"stop_times çevirileri için record_sub_id zorunludur."*

## Ölçülen alan / değer
- **Dosya/alan:** `translations.txt` → `record_sub_id` (+`table_name`).
- **Gözlenen değer:** Boş `record_sub_id` (stop_times için).
- **Beklenen değer:** `stop_sequence` değeri.

## Kod değiştirirken minimum test matrisi
- stop_times + boş record_sub_id → TRN_010 çıkmalı.
- stop_times + dolu record_sub_id → notice yok.
- başka tablo + boş record_sub_id → TRN_010 çıkmamalı.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 3.0** (YÜKSEK).
- **R1 blocker değil:** Spec + YÜKSEK (KRİTİK değil).
- **R5 kalite skoru:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData ve GTFS Guru tarafında "stop_times çevirisinde record_sub_id zorunluluğu" için doğrulanmış birebir notice karşılığı tespit edilmedi; proje-özel Spec kuralıdır.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS `translations.txt::record_sub_id` — `table_name=stop_times` için zorunlu (`stop_sequence`).
- 🔗 [GTFS Reference — translations.txt](https://gtfs.org/documentation/schedule/reference/#translationstxt)

### Kod referansı
- Emit: [k2/translations.rs:147](../../../crates/pipeline/src/k2/translations.rs#L147) (`validate_translations`)

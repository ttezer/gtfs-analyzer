# TRN_009 — record_id ve field_value aynı anda kullanılamaz

> **Karar:** Bir çeviri satırında hem `record_id` hem `field_value` dolu; GTFS bu ikisini birbirini dışlayan adresleme yöntemi olarak tanımlar. YÜKSEK, Spec. **Düzeltme:** `record_id` veya `field_value` alanlarından yalnızca birini kullanın.
> ⚠️ Satır bazlı (Row); iki alan da doluysa tetiklenir.

| Alan | Değer |
|---|---|
| Grup | TRN — Translations |
| Önem | Yüksek |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Row |
| Kimlik alanı | `record_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::translations::validate_translations()` içinde:
```
record_id Some VE field_value Some
→ TRN_009 (satır bazlı notice)
```
- Kapsam birimi: satır (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Yok; iki alanın birlikteliği kesin yakalanır.
- **Yanlış pozitif:** Yok; GTFS bu kombinasyonu yasaklar.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRN_009** | record_id ve field_value birlikte | adresleme yöntemi çakışması | YÜKSEK · Spec · K2 |
| **TRN_013** | feed_info çevirisinde kimlik alanı | feed_info kısıtı | YÜKSEK · Spec · K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** record_id ve field_value aynı anda kullanılamaz
- **Çözüm önerisi:** `record_id` veya `field_value` alanlarından yalnızca birini kullanın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"record_id ve field_value aynı anda kullanılamaz."*

## Ölçülen alan / değer
- **Dosya/alan:** `translations.txt` → `record_id`, `field_value`.
- **Gözlenen değer:** İkisi de dolu.
- **Beklenen değer:** Yalnızca biri dolu.

## Kod değiştirirken minimum test matrisi
- Hem record_id hem field_value dolu → TRN_009 çıkmalı.
- Yalnızca record_id → notice yok.
- Yalnızca field_value → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 3.0** (YÜKSEK).
- **R1 blocker değil:** Spec + YÜKSEK (KRİTİK değil).
- **R5 genel skor:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData ve GTFS Guru tarafında "record_id + field_value birlikteliği" için doğrulanmış birebir notice karşılığı tespit edilmedi; proje-özel Spec kuralıdır (GTFS karşılıklı dışlama kuralına dayalı).
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS `translations.txt` — `record_id` ile `field_value` birbirini dışlar.
- 🔗 [GTFS Reference — translations.txt](https://gtfs.org/documentation/schedule/reference/#translationstxt)

### Kod referansı
- Emit: [k2/translations.rs:167](../../../crates/pipeline/src/k2/translations.rs#L167) (`validate_translations`)

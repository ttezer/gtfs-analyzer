# TRN_001 — table_name geçersiz değer

> **Karar:** `translations.txt::table_name` desteklenen GTFS tablolarından biri değil. KRİTİK, Spec — çeviri hiçbir tabloya bağlanamaz. **Düzeltme:** `table_name` için geçerli bir GTFS tablo adı kullanın.
> ⚠️ Satır bazlı (Row); izinli tablolar, `spec-audit/spec_fields.json` içindeki resmi GTFS dosya adlarından üretilir. `location_groups` gibi sonradan eklenen resmi tablolar ayrıca elle listeye eklenmez; `translations` tablosu kaynak çeviri tablosu olarak dışarıda bırakılır.

| Alan | Değer |
|---|---|
| Grup | TRN — Translations |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Row |
| Kimlik alanı | - |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::translations::validate_translations()` içinde:
```
table_name generated resmi GTFS tablo kataloğunda DEĞİL
→ TRN_001 (satır bazlı notice)
```
- Kapsam birimi: satır (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Yok; generated katalog dışı her değer yakalanır.
- **Yanlış pozitif:** GTFS'ye eklenmiş resmi bir tablo `spec_fields.json` kataloğuna eklenmeden
  yayımlanırsa, katalog yenilenene kadar geçici olarak raporlanabilir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRN_001** | table_name geçersiz | tablo adı | KRİTİK · Spec · K2 |
| **TRN_002** | field_name bu tablo için geçersiz | alan adı | KRİTİK · Spec · K2 |
| **XFL_016** | feed_info çevirisi var ama feed_info.txt yok | dosya yokluğu | YÜKSEK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** table_name geçersiz değer
- **Çözüm önerisi:** `table_name` için geçerli bir GTFS tablo adı kullanın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"table_name desteklenen bir GTFS tablosu değil."*

## Ölçülen alan / değer
- **Dosya/alan:** `translations.txt` → `table_name`.
- **Gözlenen değer:** İzinli liste dışı `table_name`.
- **Beklenen değer:** Desteklenen GTFS tablo adı.

## Kod değiştirirken minimum test matrisi
- `table_name=foo` (geçersiz) → TRN_001 çıkmalı.
- `table_name=stops` → notice yok.
- `table_name=location_groups` → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** Spec + KRİTİK → yayın engelleyici.
- **R5 genel skor:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da birebir karşılık `translation_unknown_table_name`.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS `translations.txt::table_name` — tanımlı GTFS tablo adı; Analyzer kataloğu
  `spec-audit/spec_fields.json` kaynağından üretilir.
- 🔗 [GTFS Reference — translations.txt](https://gtfs.org/documentation/schedule/reference/#translationstxt)

### Kod referansı
- Emit: [k2/translations.rs:88](../../../crates/pipeline/src/k2/translations.rs#L88) (`validate_translations`)

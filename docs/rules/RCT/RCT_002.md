# RCT_002 — rider_category_name eksik

> **Karar:** `rider_categories.txt::rider_category_name` boş; yolcu kategorisi adsız. KRİTİK, Spec. **Düzeltme:** Her yolcu kategorisi için bir isim girin.
> ⚠️ Satır bazlı (Row); zorunlu alandır.

| Alan | Değer |
|---|---|
| Grup | RCT — Rider Categories (Fares v2) |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Row |
| Kimlik alanı | `rider_category_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::rider_categories::validate_rider_categories()` içinde:
```
rider_category_name boş
→ RCT_002 (satır bazlı notice)
```
- Kapsam birimi: rider category (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Yok.
- **Yanlış pozitif:** Yok.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **RCT_002** | rider_category_name boş | zorunlu alan | KRİTİK · Spec · K2 |
| **RCT_001** | rider_category_id yineleniyor | kimlik | KRİTİK · Spec · K3 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** rider_category_name eksik
- **Çözüm önerisi:** Her yolcu kategorisi için bir isim girin.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"rider_category_name zorunludur."*

## Ölçülen alan / değer
- **Dosya/alan:** `rider_categories.txt` → `rider_category_name`.
- **Gözlenen değer:** Boş.
- **Beklenen değer:** Dolu kategori adı.

## Kod değiştirirken minimum test matrisi
- Boş `rider_category_name` → RCT_002 çıkmalı.
- Dolu → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** Spec + KRİTİK → yayın engelleyici.
- **R5 genel skor:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `missing_required_field` (`rider_categories.txt::rider_category_name`).
  - https://gtfs-validator.mobilitydata.org/rules.html
- GTFS Guru tarafında bu kontrolün canlı doğrulanmış birebir eşleşmesi tespit edilmedi.

### GTFS spec referansı
- GTFS-Fares v2 `rider_categories.txt::rider_category_name` — zorunlu alan.
- 🔗 [GTFS Reference — rider_categories.txt](https://gtfs.org/documentation/schedule/reference/#rider_categoriestxt)

### Kod referansı
- Emit: [k2/rider_categories.rs:38](../../../crates/pipeline/src/k2/rider_categories.rs#L38) (`validate_rider_categories`)

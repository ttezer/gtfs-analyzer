# RCT_004 — min_age veya max_age geçersiz

> **Karar:** `rider_categories.txt::min_age` veya `max_age` negatif olmayan tam sayıya çevrilemiyor. ORTA, Spec. **Düzeltme:** `min_age`/`max_age` için negatif olmayan bir tam sayı girin.
> ⚠️ min ve max ayrı ayrı parse edilir; her biri için ayrı RCT_004 üretebilir.

| Alan | Değer |
|---|---|
| Grup | RCT — Rider Categories (Fares v2) |
| Önem | Orta |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Row |
| Kimlik alanı | `rider_category_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::rider_categories::validate_rider_categories()` içinde:
```
min_age parse hatası → RCT_004
VEYA max_age parse hatası → RCT_004
```
- Kapsam birimi: rider category (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Alan boşsa parse `None`; hata yalnızca dolu-ama-geçersiz değerlerde.
- **Yanlış pozitif:** Yok.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **RCT_004** | min/max_age parse hatası | yaş biçimi | ORTA · Spec · K2 |
| **RCT_005** | max_age < min_age | yaş sırası | ORTA · Spec · K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** min_age veya max_age geçersiz
- **Çözüm önerisi:** `min_age`/`max_age` için negatif olmayan bir tam sayı girin.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"min_age için negatif olmayan bir tam sayı girin."* (max_age için benzer.)

## Ölçülen alan / değer
- **Dosya/alan:** `rider_categories.txt` → `min_age`, `max_age`.
- **Gözlenen değer:** Geçersiz yaş değeri.
- **Beklenen değer:** `≥ 0` tam sayı.

## Kod değiştirirken minimum test matrisi
- `min_age=-1` / `abc` → RCT_004 çıkmalı.
- `max_age=abc` → RCT_004 çıkmalı.
- Geçerli tam sayılar → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 2.0** (ORTA).
- **R1 blocker değil:** Spec + ORTA.
- **R5 kalite skoru:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `number_out_of_range` / sayısal alan doğrulaması (`rider_categories.txt::min_age`/`max_age`); birebir eşleşme canlı doğrulanmadı, proje-özel Spec kuralıdır.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS-Fares v2 `rider_categories.txt::min_age`/`max_age` — negatif olmayan tam sayı.
- 🔗 [GTFS Reference — rider_categories.txt](https://gtfs.org/documentation/schedule/reference/#rider_categoriestxt)

### Kod referansı
- Emit: [k2/rider_categories.rs:74](../../../crates/pipeline/src/k2/rider_categories.rs#L74) (`validate_rider_categories`)

# RCT_007 — eligibility_url geçersiz

> **Karar:** `rider_categories.txt.eligibility_url` dolduğunda geçerli bir `http/https` URL olmalıdır. DÜŞÜK, Spec.
> 📌 Kardeş URL alanlarının hepsinde biçim kuralı vardı (`stop_url`→STP_042, `route_url`→RTS_005, `agency_fare_url`→AGN_008); burada yoktu. **Ayrıca alan adı 2026-08-01'de düzeltildi** — parser eskiden spec'te bulunmayan `rider_category_eligibility_url` sütununu okuyordu, yani alan ancak o düzeltmeden sonra gerçekten görülüyor.

| Alan | Değer |
|---|---|
| Grup | RCT — Rider Categories |
| Önem | Düşük |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `rider_category_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
```text
K2: eligibility_url dolu
VE looks_like_url(eligibility_url) false
→ RCT_007
```

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Boş `eligibility_url` hata değildir (alan Optional).
- **Yanlış pozitif:** URL biçimsel olarak doğrulanır; gerçekten açılıp açılmadığı kontrol edilmez.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **RCT_007** | `eligibility_url` URL biçiminde değil | biçim | DÜŞÜK · Spec · K2 |
| **RCT_002** | `rider_category_name` eksik | zorunlu alan | KRİTİK · Spec · K2 |
| **STP_042** | `stop_url` URL biçiminde değil | biçim (başka dosya) | DÜŞÜK · Spec · K2 |
| **AGN_008** | `agency_fare_url` URL biçiminde değil | biçim (başka dosya) | DÜŞÜK · Spec · K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** eligibility_url geçersiz
- **Çözüm önerisi:** `eligibility_url` için geçerli bir `http/https` URL kullanın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"eligibility_url geçerli bir URL değil."*
- **Ayrıntılı çözüm:** *"eligibility_url için geçerli bir http/https URL'si kullanın."*

## Ölçülen alan / değer
- **Dosya/alan:** `rider_categories.txt` → `eligibility_url`
- **Gözlenen değer:** geçersiz URL metni
- **Beklenen değer:** `http://` veya `https://` ile başlayan geçerli URL

## Kod değiştirirken minimum test matrisi
- `eligibility_url=https://example.com/senior` → RCT_007 çıkmamalı.
- `eligibility_url=notaurl` → RCT_007 çıkmalı.
- `eligibility_url` boş → RCT_007 çıkmamalı.
- Sütun adı `rider_category_eligibility_url` → alan okunmaz, RCT_007 çıkmamalı (spec'te yok).

---
## Teknik ek

### Dış araç eşleşmesi
- **2026-08-01 doğrulandı:** MobilityData tarafında yakın kategori `invalid_url` notice'ıdır.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- `rider_categories.txt` → `eligibility_url`: Field Type **URL**, Presence **Optional**.
- 🔗 https://gtfs.org/documentation/schedule/reference/#rider_categoriestxt
- 🔗 https://gtfs.org/documentation/schedule/reference/#field-types

### Skora katkı
- **severity.weight() = 1.0** (DÜŞÜK).
- **R1 blocker değil:** `VS` görünümü R1 içermez.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Kod referansı
- Emit: [rider_categories.rs](../../../crates/pipeline/src/k2/rider_categories.rs)
- Registry: [registry.rs](../../../crates/rules/src/registry.rs)

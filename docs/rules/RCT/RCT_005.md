# RCT_005 — max_age min_age'den küçük

> **Karar:** `rider_categories.txt`'te `max_age`, `min_age`'den küçük; yaş aralığı geçersiz. ORTA, Quality. **Düzeltme:** `max_age` değerini `min_age`'e eşit veya daha büyük yapın.
> ⚠️ Yalnızca her iki yaş da geçerli tam sayı olarak parse edildiğinde değerlendirilir.

| Alan | Değer |
|---|---|
| Grup | RCT — Rider Categories (Fares v2) |
| Önem | Orta |
| Sınıf | Quality |
| Otorite kaynağı | PROJECT_QUALITY |
| Aşama | K2 |
| Varlık | Row |
| Kimlik alanı | `rider_category_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::rider_categories::validate_rider_categories()` içinde:
```
min_age VE max_age geçerli VE max_age < min_age
→ RCT_005 (satır bazlı notice)
```
- Kapsam birimi: rider category (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Yaşlardan biri parse edilemezse (RCT_004) bu kontrol atlanır.
- **Yanlış pozitif:** Yok; ters yaş aralığı kesin hatadır.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **RCT_005** | max_age < min_age | yaş sırası | ORTA · Quality · K2 |
| **RCT_004** | min/max_age parse hatası | yaş biçimi | ORTA · Spec · K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** max_age min_age'den küçük
- **Çözüm önerisi:** `max_age` değerini `min_age`'e eşit veya daha büyük yapın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"max_age ({mx}) min_age ({mn}) değerinden küçük olamaz."*

## Ölçülen alan / değer
- **Dosya/alan:** `rider_categories.txt` → `min_age`, `max_age`.
- **Gözlenen değer:** `max_age < min_age`.
- **Beklenen değer:** `max_age ≥ min_age`.

## Kod değiştirirken minimum test matrisi
- min_age=65, max_age=18 → RCT_005 çıkmalı.
- max_age ≥ min_age → notice yok.
- Yaşlardan biri geçersiz → RCT_004 (RCT_005 değil).

---
## Teknik ek

### Skora katkı
- **severity.weight() = 2.0** (ORTA).
- **R1 blocker değil:** Quality sınıfı R1 üretmez (R1 yalnız `GtfsSpec` + Spec + Kritik).
- **R5 genel skor:** Interop sınıf penaltısına girer (ağırlık 0.3).

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-07 doğrulandı:** MobilityData'da "max_age < min_age" için parity YOK (yaş aralığı notice'ı yok); resmi GTFS Spec'te de açık ordering normu yok → proje-özel **Quality** (kalite) kuralı.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS-Fares v2 `rider_categories.txt` — `max_age` ≥ `min_age` olmalıdır.
- 🔗 [GTFS Reference — rider_categories.txt](https://gtfs.org/documentation/schedule/reference/#rider_categoriestxt)

### Kod referansı
- Emit: [k2/rider_categories.rs:104](../../../crates/pipeline/src/k2/rider_categories.rs#L104) (`validate_rider_categories`)

# RCT_003 — is_default_fare_category geçersiz

> **Karar:** `rider_categories.txt::is_default_fare_category` 0 veya 1 dışında bir değer (ya da sayı değil). KRİTİK, Spec. **Düzeltme:** 0 (varsayılan değil) veya 1 (varsayılan) kullanın.
> ⚠️ Hem enum dışı değer hem parse hatası RCT_003 üretir.

| Alan | Değer |
|---|---|
| Grup | RCT — Rider Categories (Fares v2) |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Row |
| Kimlik alanı | `rider_category_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::rider_categories::validate_rider_categories()` içinde:
```
is_default_fare_category dolu VE değer ∉ {0,1}  (veya parse hatası)
→ RCT_003 (satır bazlı notice)
```
- Geçerli değerler: 0, 1.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Boşsa atlanır.
- **Yanlış pozitif:** Yok.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **RCT_003** | is_default_fare_category enum dışı | enum geçerliliği | KRİTİK · Spec · K2 |
| **RCT_006** | fare_product başına >1 varsayılan kategori | varsayılan tekilliği | ORTA · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** is_default_fare_category geçersiz
- **Çözüm önerisi:** 0 (varsayılan değil) veya 1 (varsayılan) kullanın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"is_default_fare_category geçerli bir enum değeri değil."*

## Ölçülen alan / değer
- **Dosya/alan:** `rider_categories.txt` → `is_default_fare_category`.
- **Gözlenen değer:** Geçersiz değer.
- **Beklenen değer:** `0` veya `1`.

## Kod değiştirirken minimum test matrisi
- `is_default_fare_category=2` → RCT_003 çıkmalı.
- `is_default_fare_category=abc` → RCT_003 çıkmalı.
- `0`/`1` → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** Spec + KRİTİK → yayın engelleyici.
- **R5 kalite skoru:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `unexpected_enum_value` (`rider_categories.txt::is_default_fare_category`).
  - https://gtfs-validator.mobilitydata.org/rules.html
- GTFS Guru tarafında bu kontrolün canlı doğrulanmış birebir eşleşmesi tespit edilmedi.

### GTFS spec referansı
- GTFS-Fares v2 `rider_categories.txt::is_default_fare_category` — geçerli değerler 0, 1.
- 🔗 [GTFS Reference — rider_categories.txt](https://gtfs.org/documentation/schedule/reference/#rider_categoriestxt)

### Kod referansı
- Emit: [k2/rider_categories.rs:48](../../../crates/pipeline/src/k2/rider_categories.rs#L48) (`validate_rider_categories`)

# RCT_001 — rider_category_id yineleniyor

> **Karar:** `rider_categories.txt`'te aynı `rider_category_id` birden çok satırda kullanılmış. KRİTİK, Spec. **Düzeltme:** Her yolcu kategorisine benzersiz bir `rider_category_id` atayın.
> ⚠️ K3 entity grafiği kurulurken tekrar tespit edilir; GTFS-Fares v2 yolcu kategorileri.

| Alan | Değer |
|---|---|
| Grup | RCT — Rider Categories (Fares v2) |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K3 |
| Varlık | Row |
| Kimlik alanı | `rider_category_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | `FPD_005` |

## Tetikleme koşulu (kesin mantık)
`k3_entity_graph` içinde:
```
rider_category_id daha önce görüldü (tekrar)
→ RCT_001 (satır bazlı notice)
```
- Kapsam birimi: rider category (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `rider_category_id` boşsa başka kural devreye girer.
- **Yanlış pozitif:** Yok.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **RCT_001** | rider_category_id yineleniyor | kimlik tekilliği | KRİTİK · Spec · K3 |
| **RCT_002** | rider_category_name eksik | zorunlu alan | KRİTİK · Spec · K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** rider_category_id yineleniyor
- **Çözüm önerisi:** Her yolcu kategorisine benzersiz bir `rider_category_id` atayın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"'{rider_category_id}' yolcu kategorisi kodu tekrarlanıyor."*

## Ölçülen alan / değer
- **Dosya/alan:** `rider_categories.txt` → `rider_category_id`.
- **Gözlenen değer:** Tekrarlanan `rider_category_id`.
- **Beklenen değer:** Benzersiz `rider_category_id`.

## Kod değiştirirken minimum test matrisi
- Aynı `rider_category_id` iki satırda → RCT_001 çıkmalı.
- Benzersiz id'ler → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** Spec + KRİTİK → yayın engelleyici.
- **R5 kalite skoru:** Spec sınıf penaltısına girer.
- **Bağımlılık:** `FPD_005` maskelenebilir.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
`blocks = [FPD_005]`.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `duplicate_key` (`rider_categories.txt::rider_category_id`).
  - https://gtfs-validator.mobilitydata.org/rules.html
- GTFS Guru tarafında bu kontrolün canlı doğrulanmış birebir eşleşmesi tespit edilmedi.

### GTFS spec referansı
- GTFS-Fares v2 `rider_categories.txt::rider_category_id` — benzersiz birincil anahtar.
- 🔗 [GTFS Reference — rider_categories.txt](https://gtfs.org/documentation/schedule/reference/#rider_categoriestxt)

### Kod referansı
- Emit: [k3_entity_graph.rs:499](../../../crates/pipeline/src/k3_entity_graph.rs#L499)

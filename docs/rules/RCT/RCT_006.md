# RCT_006 — fare_product başına varsayılan yolcu kategorisi sayısı bir değil

> **Karar:** Birden fazla uygun rider category'ye sahip her `fare_product_id` için tam olarak bir rider category `is_default_fare_category=1` olmalı. ORTA, Spec. Kural hem hiç default olmamasını hem de birden fazla default olmasını raporlar.
> ⚠️ K4'te değerlendirilir; boş `fare_products.rider_category_id`, ürünün tanımlı rider category'lerin tümüne uygun olduğu anlamına gelir. Tek uygun kategori varsa “exactly one” kontrolü uygulanmaz.
> 🔀 **İKİ EMİT ŞEKLİ VAR.** Feed'de **hiç** `is_default_fare_category=1` yoksa ve birden fazla ürün etkileniyorsa kök kusur tektir → **tek feed-seviyesi notice** (`rider_categories.txt`). Varsayılan **var** ama ürünün uygunluk kümesinde yoksa kusur ürüne özgüdür → **ürün başına notice** (`fare_products.txt`). Bu yüzden kural hem `field=None` hem `field=is_default_fare_category` çapası üretir.

| Alan | Değer |
|---|---|
| Grup | RCT — Rider Categories (Fares v2) |
| Önem | Orta |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Entity |
| Kimlik alanı | `fare_product_id` |
| Skor tabanı | 2 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_fares_v2()` içinde:
```
tanımlı rider_category_id'ler ve is_default_fare_category=1 olanlar toplanır
fare_products her fare_product_id için uygun rider_category kümesine göre gruplanır
birden fazla uygun kategori varsa default kategori sayısı != 1 → ihlal
ihlaller fare_product_id'ye göre sıralanır (determinizm)

  default kümesi BOŞ ve ihlal sayısı > 1
    → TEK feed notice; çözücü adaylar = ihlalli ürünlerin uygunluk kümelerinin KESİŞİMİ
  aksi halde
    → ürün başına notice (mevcut davranış)
```
- Kapsam birimi: fare_product (Entity) — toplulanmış dalda feed.
- **Çözücü kesişim neden doğru:** seçilen tek varsayılan, her ihlalli ürünün uygunluk kümesinde bulunmak zorundadır (yoksa o üründe sayı 0 kalır); kümelerin kesişiminde olması hem gerek hem yeter koşuldur.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Birden fazla uygun kategori varken default sayısı 0 veya 1’den büyük olan durumlar raporlanır. Tek uygun kategori, bu hükmün “exactly one” kapsamına girmez.
- **Yanlış pozitif:** Bilinmeyen açık `rider_category_id` değerleri uygunluk kümesine alınmaz; bunlar FPD_005 kapsamındadır. Aynı default kategorinin yinelenmesi de FPD_001 kapsamındadır.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **RCT_006** | fare_product başına default kategori sayısı 1 değil | varsayılan tekilliği | ORTA · Spec · K4 |
| **RCT_003** | is_default_fare_category enum dışı | enum geçerliliği | KRİTİK · Spec · K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** fare_product başına varsayılan yolcu kategorisi sayısı bir değil
- **Çözüm önerisi:** Birden fazla uygun rider category'ye sahip her `fare_product_id` için tam olarak bir rider category `is_default_fare_category=1` olmalı.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı (0 default):** *"'{fare_product_id}' ücret ürününde {n} uygun yolcu kategorisi var ancak hiçbiri varsayılan değil; tam olarak 1 olmalı."*
- **Ayrıntılı bulgu mesajı (>1 default):** *"'{fare_product_id}' ücret ürünü {n} varsayılan yolcu kategorisine bağlı; en fazla 1 olmalı."*
- **Feed özeti (hiç default yok, çözücü var):** *"Feed'de hiçbir rider_category varsayılan değil: {n} ücret ürününün her birinde tam olarak 1 varsayılan kategori olmalı. Şu kategorilerden BİRİNİ is_default_fare_category=1 yapmak bulguların tamamını kapatır: {liste}."*
- **Feed özeti (kesişim boş):** *"… Tek bir kategoriyi varsayılan yapmak yetmez; ürünlerin uygunluk kümeleri örtüşmüyor."*
- Feed özeti `details` taşır: `affected_products` · `example_products` (ilk 5) · `resolving_categories` (kesişim boşsa yazılmaz).
- ⚠️ **Locale dala duyarlı DEĞİL:** `en/tr/ja` sözlükleri kural kimliği başına tek metin tutar, o metin ürün dalına göre yazılmıştır. Feed dalında özne (`fare_product`) yanlış kalır. Bu yeni bir kusur değil, toplulayan kuralların ortak sınırı — `STP_022` de aynı (`"Stop '{entity_id}': stop_code is missing."` feed özetinde `entity_id` taşımaz). Çözümü dal-başına locale anahtarı olurdu; ayrı iş.

## Ölçülen alan / değer
- **Dosya/alan:** `rider_categories.txt::is_default_fare_category`; `fare_products.txt::rider_category_id`/`fare_product_id`.
- **Gözlenen değer:** ürün dalında `{n}` varsayılan kategori; feed dalında etkilenen ürün sayısı.
- **Beklenen değer:** `1` (ürün dalı); feed dalında beklenen değer yazılmaz.

## Kod değiştirirken minimum test matrisi
- Bir fare_product 2 varsayılan kategoriye bağlı → RCT_006 çıkmalı.
- Bir fare_product birden fazla uygun kategoriye bağlı, hiçbiri default değil → RCT_006 çıkmalı.
- `rider_category_id` boş ve birden fazla tanımlı kategori default değil → RCT_006 çıkmalı.
- Bir fare_product tek uygun non-default kategoriye bağlı → notice yok.
- Bir fare_product tek default ve bir non-default uygun kategoriye bağlı → notice yok.
- Hiç default yok + birden fazla ürün etkileniyor → **tek** Feed notice, mesaj çözücü kategorileri adıyla sayar ve çözmeyen kategoriyi saymaz.
- Default var ama ürünün uygunluk kümesinde yok → toplulama YOK, ürün başına Row notice.
- Uygunluk kümeleri ayrık → Feed notice “tek kategori yetmez” der, `resolving_categories` yazılmaz.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 2.0** (ORTA).
- **R1 blocker değil:** Spec + ORTA.
- **R5 genel skor:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData tarafında "fare_product başına varsayılan kategori sayısı bir değil" için doğrulanmış birebir notice karşılığı tespit edilmedi; proje-özel Spec kuralıdır (GTFS-Fares v2).
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS-Fares v2 `rider_categories.txt::is_default_fare_category` — fare_product başına tek varsayılan kategori.
- 🔗 [GTFS Reference — rider_categories.txt](https://gtfs.org/documentation/schedule/reference/#rider_categoriestxt) · [fare_products.txt](https://gtfs.org/documentation/schedule/reference/#fare_productstxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:2552](../../../crates/pipeline/src/k4_cross_ref.rs#L2552) (`check_fares_v2`)
- Test: [k4_cross_ref.rs:6954](../../../crates/pipeline/src/k4_cross_ref.rs#L6954)

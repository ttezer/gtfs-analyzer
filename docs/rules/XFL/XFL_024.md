# XFL_024 — location_group_id bulunamadı (stop_times)

> **Karar:** `stop_times.txt::location_group_id` `location_groups.txt`'te tanımlı değil. KRİTİK, Spec — Flex sefer bu gruba bağlanamaz. **Düzeltme:** Geçerli bir `location_group_id` kullanın veya grubu `location_groups.txt`'te tanımlayın.
> ⚠️ Satır bazlı (Row) FK; GTFS-Flex. Yalnızca flex sütunu olan feed'lerde değerlendirilir; aynı tanımsız id tek kez raporlanır (dedup).

| Alan | Değer |
|---|---|
| Grup | XFL — Çapraz Dosya / Semantik |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Row |
| Kimlik alanı | `location_group_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check()` stop_times geçişinde:
```
stop_times.location_group_id boş değil VE map.location_group_ids İÇERMİYOR
→ XFL_024 (satır bazlı notice, location_group_id başına bir kez)
```
- Kapsam birimi: satır (Row), tanımsız `location_group_id` başına dedup.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `location_group_id` boşsa atlanır.
- **Yanlış pozitif:** Yok; location_groups.txt mevcut ama kayıt eksikse doğru tetiklenir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **XFL_024** | stop_times→location_group FK yok | FK (location_group) | KRİTİK · Spec · K4 |
| **XFL_022** | location_group_stops→location_group FK yok | FK (location_group) | KRİTİK · Spec · K4 |
| **STM_041** | stop_id ve location_id/group_id aynı satırda | çakışma | YÜKSEK · Spec · K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** location_group_id bulunamadı (stop_times)
- **Çözüm önerisi:** Geçerli bir `location_group_id` kullanın veya grubu `location_groups.txt`'te tanımlayın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"'{location_group_id}' konum grubu location_groups.txt'te tanımlı değil."*

## Ölçülen alan / değer
- **Dosya/alan:** `stop_times.txt` → `location_group_id`; karşılaştırma `location_groups.txt`.
- **Gözlenen değer:** Tanımsız `location_group_id`.
- **Beklenen değer:** `location_groups.txt`'te kayıtlı `location_group_id`.

## Kod değiştirirken minimum test matrisi
- location_groups.txt'te olmayan `location_group_id` → XFL_024 çıkmalı.
- `location_group_id` boş → notice yok.
- Geçerli `location_group_id` → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** Spec + KRİTİK → yayın engelleyici.
- **R5 genel skor:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- MobilityData'da yakın karşılık `foreign_key_violation` (`stop_times.txt::location_group_id` → `location_groups.txt::location_group_id`).
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS-Flex `stop_times.txt::location_group_id` → `location_groups.txt::location_group_id` foreign key kuralı.
- 🔗 [GTFS Reference — stop_times.txt](https://gtfs.org/documentation/schedule/reference/#stop_timestxt)

### Kod referansı
- Emit: [k4_cross_ref.rs](../../../crates/pipeline/src/k4_cross_ref.rs) (`check`)

# XFL_004 — fare_rules'ta tanımsız route_id

> **Karar:** `fare_rules.txt` bir `route_id`'ye referans veriyor ama bu hat `routes.txt`'te tanımlı değil. KRİTİK, Spec — ücret kuralı bağlanamaz. **Düzeltme:** Eksik hattı `routes.txt`'e ekleyin veya `fare_rules.txt`'teki referansı kaldırın.
> ⚠️ Feed seviyesinde tek özet notice üretir; tüm tanımsız `route_id`'ler tek satırda toplanır.

| Alan | Değer |
|---|---|
| Grup | XFL — Çapraz Dosya / Semantik |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Entity |
| Kimlik alanı | `route_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_xfl()` içinde:
```
fare_rules.route_id var VE boş değil
VE map.routes (routes.txt route_id seti) route_id'yi İÇERMİYOR
→ XFL_004 (tanımsız route_id'ler dedup'lanıp tek feed-level notice)
```
- Kapsam birimi: feed (özet); observed value virgülle ayrılmış benzersiz `route_id` listesi.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `fare_rules.route_id` boş/yoksa atlanır (route_id fare_rules'ta opsiyoneldir).
- **Yanlış pozitif:** Yok; routes.txt mevcut ama ilgili route eksikse doğru tetiklenir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **XFL_004** | fare_rules→route referansı routes.txt'te yok | feed-level özet | KRİTİK · Spec · K4 |
| **FRL_001** | fare_rules→fare_id (fare_attributes) referansı geçersiz | farklı FK ekseni | KRİTİK · Spec · K4 |
| **XFL_007** | routes→agency referansı agency.txt'te yok | farklı FK ekseni | KRİTİK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** fare_rules'ta tanımsız route_id
- **Çözüm önerisi:** Eksik rotaları `routes.txt`'e ekleyin veya `fare_rules.txt`'teki referansları kaldırın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"fare_rules.txt'te routes.txt'te tanımlı olmayan route_id'ler var: {liste}."*

## Ölçülen alan / değer
- **Dosya/alan:** `fare_rules.txt` → `route_id`; karşılaştırma kümesi `routes.txt` → `route_id`.
- **Gözlenen değer:** Tanımsız `route_id` listesi.
- **Beklenen değer:** Her `route_id` için `routes.txt`'te kayıt.

## Kod değiştirirken minimum test matrisi
- fare_rules'ta routes.txt'te olmayan `route_id` → XFL_004 çıkmalı.
- Aynı eksik `route_id` birden çok fare_rule'da → tek notice'ta dedup.
- `fare_rules.route_id` boş → XFL_004 çıkmamalı.
- Geçerli `route_id` → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** Spec + KRİTİK → yayın engelleyici.
- **R5 kalite skoru:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `foreign_key_violation` (child=`fare_rules.txt::route_id` → parent=`routes.txt::route_id`).
  - https://gtfs-validator.mobilitydata.org/rules.html
- GTFS Guru tarafında bu kontrolün canlı doğrulanmış birebir eşleşmesi tespit edilmedi.

### GTFS spec referansı
- GTFS `fare_rules.txt::route_id` → `routes.txt::route_id` foreign key kuralı.
- 🔗 [GTFS Reference — fare_rules.txt](https://gtfs.org/documentation/schedule/reference/#fare_rulestxt) · [routes.txt](https://gtfs.org/documentation/schedule/reference/#routestxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:2855](../../../crates/pipeline/src/k4_cross_ref.rs#L2855) (`check_xfl`)
- Test: [k4_cross_ref.rs:4110](../../../crates/pipeline/src/k4_cross_ref.rs#L4110) (`fare_rule_with_missing_route_produces_xfl_004`)

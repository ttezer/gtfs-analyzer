# XFL_013 — shape_id birden fazla yönde kullanılıyor

> **Karar:** Aynı `shape_id` hem gidiş (`direction_id=0`) hem dönüş (`direction_id=1`) seferlerinde kullanılıyor; tek geometri iki yönü temsil edemez. YÜKSEK, Quality. **Düzeltme:** Gidiş ve dönüş yönleri için ayrı `shape_id` tanımlayın.
> ⚠️ Yalnızca `direction_id` dolu seferler değerlendirilir; aynı shape'i kullanan tüm yön kombinasyonları feed seviyesinde özetlenir.

| Alan | Değer |
|---|---|
| Grup | XFL — Çapraz Dosya / Semantik |
| Önem | Yüksek |
| Sınıf | Quality |
| Otorite kaynağı | PROJECT_QUALITY |
| Aşama | K4 |
| Varlık | Feed |
| Kimlik alanı | - |
| Skor tabanı | 2 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | `SHP_017`, `STM_017` |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_xfl()` içinde:
```
trips.shape_id dolu trip'ler shape_id'ye göre gruplanır
her shape için yön başına (gidiş=0 / dönüş=1) route_id, service_id ve
  ilk durağın kalkış saati (stop_times_index.sorted_stops) toplanır (BTreeSet → benzersiz+sıralı)
her iki yönde de en az bir sefer varsa
→ XFL_013 (shape bazlı notice); mesaj yön başına hat/takvim/kalkış listeler
```
- Kapsam birimi: shape (feed özeti).
- Liste sınırı: route/service en fazla 5, kalkış en fazla 6 gösterilir; fazlası dil-nötr `(+N)`.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `direction_id` boşsa o trip yön sayımına girmez; yön bilgisi eksik feed'lerde çıkmayabilir.
- **Yanlış pozitif:** Bazı operatörler kasıtlı olarak tek geometriyi iki yönde paylaşır; teknik olarak kalite uyarısıdır, her zaman hata olmayabilir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **XFL_013** | shape_id hem gidiş hem dönüşte kullanılıyor | yön çakışması | YÜKSEK · Quality · K4 |
| **SHP_017** | Shape geometri/sıralama tutarsızlığı | geometri | — · · |
| **STM_017** | stop_times shape mesafe tutarsızlığı | shape_dist_traveled | — · · |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** shape_id birden fazla yönde kullanılıyor
- **Çözüm önerisi:** Gidiş ve dönüş yönleri için ayrı `shape_id` tanımlayın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"shape_id '{shape_id}' hem gidiş (yön 0) hem dönüş (yön 1) seferlerinde kullanılıyor. Gidiş → hat {fwd_routes}, takvim {fwd_services}, kalkış {fwd_deps}; Dönüş → hat {bwd_routes}, takvim {bwd_services}, kalkış {bwd_deps}."*
- Dinamik parçalar `details`'te taşınır (`fwd_routes`/`fwd_services`/`fwd_deps`/`bwd_routes`/`bwd_services`/`bwd_deps`); tr Rust mesajını doğrudan gösterir, en/ja bu placeholder'ları çevrilmiş etiketlerle kullanır. Veri yoksa parça `—`.

## Ölçülen alan / değer
- **Dosya/alan:** `trips.txt` → `shape_id`, `direction_id`, `route_id`, `service_id`; ilk kalkış `stop_times.txt` → `departure_time`.
- **Gözlenen değer:** İki yönde de kullanılan `shape_id`; yön başına hat/takvim/kalkış kümeleri.
- **Beklenen değer:** Her shape_id tek bir `direction_id` ile ilişkili.

## Kod değiştirirken minimum test matrisi
- Aynı shape hem direction_id=0 hem =1 trip'lerinde → XFL_013 çıkmalı.
- Bir yönde farklı route_id/service_id → hepsi mesajda listelenmeli (details `fwd_routes`/`bwd_*`).
- Shape yalnızca tek yönde → XFL_013 çıkmamalı.
- `direction_id` boş trip'ler → yön sayımına girmemeli.
- `shape_id` boş → atlanır.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 3.0** (YÜKSEK).
- **R1/R8:** Quality sınıfı R1 veya R8 üretmez.
- **R5 genel skor:** Quality sınıf penaltısına girer.
- **Bağımlılık:** `SHP_017`, `STM_017` kurallarını maskeleyebilir.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
`blocks = [SHP_017, STM_017]`.

### Dış araç eşleşmesi
- **2026-07-10 doğrulandı:** MobilityData'da "shape_id'nin hem gidiş hem dönüş yönünde kullanılması" için birebir notice karşılığı YOKTUR; XFL_013 proje-özel kalite kuralıdır.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS best-practice: yön başına ayrı `shape_id` önerilir.
- 🔗 [GTFS Schedule Best Practices](https://gtfs.org/documentation/schedule/schedule-best-practices/) · [shapes.txt](https://gtfs.org/documentation/schedule/reference/#shapestxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:3990](../../../crates/pipeline/src/k4_cross_ref.rs#L3990) (`check_xfl`)

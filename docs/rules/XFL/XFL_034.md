# XFL_034 — location_group_stops'ta stop_id boş

> Karar: `location_group_stops.txt` içindeki **`stop_id`** alanı boş bırakılamaz. Spec bu alanı `Required`
> işaretler ve "required" yalnız sütunun bulunmasını değil, **değerin dolu olmasını** ister.

| Alan | Değer |
|---|---|
| Grup | XFL — Çapraz dosya |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Row |
| Kimlik alanı | `stop_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)

```
location_group_stops.txt satırında `stop_id` sütunu VAR  VE  değeri boş
→ XFL_034
```

Sütunun **hiç bulunmaması** bu kuralın alanı değildir — o `ARC_025`'e (zorunlu sütun başlıkta
yok) aittir.

⚠️ Bu kayıtlar ham satırı (`row`) TUTMAZ, yalnız alanları taşır. Dolayısıyla "sütun hiç
yok" (→`ARC_025`) ile "sütun var, değer boş" ayrımı yapılamaz: sütun yoksa iki notice
birden çıkar. Küçük bir gürültü; alternatifi bu kayıtlara `RowMap` eklemekti ve bellek
maliyetine değmedi.

## Neden ayrı bir kural gerekti

`XFL_023` bu alanı zaten çapalıyordu ama **yalnız FK çözümü** ölçüyor. Boş değer o kuralın
predicate'inden düşüyordu:

- yinelenme kontrolü boş anahtarları **atlar** — haklı olarak, boşlar birbirinin kopyası değildir;
- FK çözümü boş değeri "referans yok" sayıp **geçer**.

Aradaki hüküm — *"bu alan zorunludur"* — ikisinin arasından düşüyordu. Çapa granülerliği
triyajında (2026-08-05) aynı desen 12 alanda birden bulundu.

## Yanlış pozitif / negatif
- **Yanlış pozitif:** Yok. Koşul spec'in `presence=Required` işaretinin birebir karşılığı.
- **Yanlış negatif:** Sütun hiç yoksa susar (`ARC_025`'in alanı).

## Karışan komşular (ayrım)

| Kural | Ne yakalar | Eksen |
|---|---|---|
| **XFL_034** | `stop_id` boş | değerin varlığı |
| **XFL_023** | yalnız FK çözümü | başka bir hüküm atomu |
| **ARC_025** | sütun başlıkta hiç yok | şema düzeyi |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** location_group_stops'ta stop_id boş

## Ölçülen alan / değer
- `field`: `stop_id` · `observed`: boş dize · `expected`: `(dolu)`

## Kod değiştirirken minimum test matrisi
| Senaryo | Beklenen |
|---|---|
| `stop_id` boş | `XFL_034` |
| `stop_id` dolu | sessiz |
| `stop_id` sütunu hiç yok | sessiz (`ARC_025`) |

## Teknik ek
### Dış araç eşleşmesi
- **MD paritesi:** Yok.

### GTFS spec referansı
- GTFS Schedule Reference, `location_group_stops.txt` → `stop_id` (`presence=Required`)
- 🔗 https://gtfs.org/documentation/schedule/reference/#location_group_stopstxt

### Korpus kanıtı
⚠️ **YOK.** 239 feed'de bu alanın boş olduğu tek satır bile yok
(`spec-audit/presence_corpus.py`). Kural **sentetik fixture** ile kanıtlanır
(`presence_probe.py` + `emit_proof`).

⚠️ İlk korpus ölçümü bu alan için bulgu bildirmişti; o sayı **tamamen boş CSV satırlarını**
sayıyordu ve pipeline onları `ARC_018` ile eleyip kayıt üretmiyor. Ölçüm düzeltildi.

### Skora katkı
`Kritik`, taban 1: teorik skor katkısı `4 * 1.0 = 4.0`.

### Hangi raporlarda görünür
`VS_K`: `R1`, `R2`, `R5`, `R9`.

### Kod referansı
- [`registry.rs`](../../../crates/rules/src/registry.rs#L1570)

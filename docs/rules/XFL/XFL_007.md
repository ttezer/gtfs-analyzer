# XFL_007 — agency_id bulunamadı

> **Karar:** `routes.txt` bir `agency_id`'ye referans veriyor ama bu işletici `agency.txt`'te tanımlı değil. KRİTİK, Spec — hat işleticisine bağlanamaz. **Düzeltme:** Eksik işletici tanımlarını `agency.txt`'e ekleyin.
> ⚠️ Feed seviyesinde tek özet notice üretir; yalnızca dolu ve tanımsız `agency_id`'leri toplar.

| Alan | Değer |
|---|---|
| Grup | XFL — Çapraz Dosya / Semantik |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Entity |
| Kimlik alanı | `agency_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_xfl()` içinde:
```
routes.agency_id Some VE boş değil
VE map.agencies (agency.txt agency_id seti) agency_id'yi İÇERMİYOR
→ XFL_007 (tanımsız agency_id'ler dedup'lanıp tek feed-level notice)
```
- Kapsam birimi: feed (özet); observed value virgülle ayrılmış benzersiz `agency_id` listesi.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `routes.agency_id` boş/yoksa atlanır; tek-işleticili feed'lerde agency_id opsiyoneldir (bu durumda eşleşme tek agency'ye düşer, ayrı kural yakalar).
- **Yanlış pozitif:** Yok; agency.txt mevcut ama ilgili kayıt eksikse doğru tetiklenir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **XFL_007** | routes→agency referansı agency.txt'te yok | feed-level özet | KRİTİK · Spec · K4 |
| **RTS_002** | Tekil route satırında geçersiz `agency_id` referansı | satır bazlı | — · Spec · K4 |
| **XFL_017** | route_cemv_support ile agency_cemv_support çelişiyor | değer çelişkisi | DÜŞÜK · Quality · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** agency_id bulunamadı
- **Çözüm önerisi:** Eksik işletici tanımlarını `agency.txt`'e ekleyin.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"routes.txt'te agency.txt'te tanımlı olmayan agency_id'ler var: {liste}."*

## Ölçülen alan / değer
- **Dosya/alan:** `routes.txt` → `agency_id`; karşılaştırma kümesi `agency.txt` → `agency_id`.
- **Gözlenen değer:** Tanımsız `agency_id` listesi.
- **Beklenen değer:** Her `agency_id` için `agency.txt`'te kayıt.

## Kod değiştirirken minimum test matrisi
- routes'ta agency.txt'te olmayan `agency_id` → XFL_007 çıkmalı.
- Aynı eksik `agency_id` birden çok route'ta → tek notice'ta dedup.
- `routes.agency_id` boş → XFL_007 çıkmamalı.
- Geçerli `agency_id` → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** Spec + KRİTİK → yayın engelleyici.
- **R5 kalite skoru:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `foreign_key_violation` (child=`routes.txt::agency_id` → parent=`agency.txt::agency_id`).
  - https://gtfs-validator.mobilitydata.org/rules.html
- GTFS Guru tarafında bu kontrolün canlı doğrulanmış birebir eşleşmesi tespit edilmedi.

### GTFS spec referansı
- GTFS `routes.txt::agency_id` → `agency.txt::agency_id` foreign key kuralı.
- 🔗 [GTFS Reference — routes.txt](https://gtfs.org/documentation/schedule/reference/#routestxt) · [agency.txt](https://gtfs.org/documentation/schedule/reference/#agencytxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:2407](../../../crates/pipeline/src/k4_cross_ref.rs#L2407) (`check_xfl`)

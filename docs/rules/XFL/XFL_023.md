# XFL_023 — stop_id bulunamadı (location_group_stops)

> **Karar:** `location_group_stops.txt::stop_id` `stops.txt`'te tanımlı değil. KRİTİK, Spec — grup-durak ilişkisi bağlanamaz. **Düzeltme:** Geçerli bir `stop_id` kullanın.
> ⚠️ Satır bazlı (Row) FK; GTFS-Flex konum grubu modeli.

| Alan | Değer |
|---|---|
| Grup | XFL — Çapraz Dosya / Semantik |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Row |
| Kimlik alanı | `stop_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check()` içinde:
```
location_group_stops.stop_id boş değil VE map.stops İÇERMİYOR
→ XFL_023 (satır bazlı notice)
```
- Kapsam birimi: satır (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `stop_id` boşsa atlanır.
- **Yanlış pozitif:** Yok; stops.txt mevcut ama kayıt eksikse doğru tetiklenir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **XFL_023** | location_group_stops→stop FK yok | FK (stop) | KRİTİK · Spec · K4 |
| **XFL_022** | location_group_stops→location_group FK yok | FK (location_group) | KRİTİK · Spec · K4 |
| **SAR_002** | stop_areas→stop FK yok | FK (stop) | KRİTİK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** stop_id bulunamadı (location_group_stops)
- **Çözüm önerisi:** Geçerli bir `stop_id` kullanın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"'{stop_id}' durak stops.txt'te tanımlı değil."*

## Ölçülen alan / değer
- **Dosya/alan:** `location_group_stops.txt` → `stop_id`; karşılaştırma `stops.txt`.
- **Gözlenen değer:** Tanımsız `stop_id`.
- **Beklenen değer:** `stops.txt`'te kayıtlı `stop_id`.

## Kod değiştirirken minimum test matrisi
- stops.txt'te olmayan `stop_id` → XFL_023 çıkmalı.
- `stop_id` boş → notice yok.
- Geçerli `stop_id` → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** Spec + KRİTİK → yayın engelleyici.
- **R5 genel skor:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- MobilityData'da yakın karşılık `foreign_key_violation` (`location_group_stops.txt::stop_id` → `stops.txt::stop_id`).
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS-Flex `location_group_stops.txt::stop_id` → `stops.txt::stop_id` foreign key kuralı.
- 🔗 [GTFS Reference — location_group_stops.txt](https://gtfs.org/documentation/schedule/reference/#location_group_stopstxt)

### Kod referansı
- Emit: [k4_cross_ref.rs](../../../crates/pipeline/src/k4_cross_ref.rs) (`check`)

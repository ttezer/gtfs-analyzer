# XFL_027 — cEMV bayrağı ile Fares v2 bilgisi farklı

> **Karar:** Bir route `route_cemv_support=2` diyor, ancak Fares v2 zincirinde uygulanabilir contactless fare product var. Bu feed ihlali değildir: GTFS precedence gereği Fares v2 bilgisi geçerlidir. BİLGİ, Interop, skor dışı.
> ⚠️ Route bazlı. Uygulanabilirlik: leg rule `network_id` boş (global) / `network_id`=route.network_id / `from/to_area_id` route'un alanlarıyla eşleşir.

| Alan | Değer |
|---|---|
| Grup | XFL — Çapraz Dosya / Semantik |
| Önem | Bilgi |
| Sınıf | Interop |
| Otorite kaynağı | GOOGLE_TRANSIT_INTEROP |
| Aşama | K4 |
| Varlık | Entity |
| Kimlik alanı | `route_id` |
| Skor tabanı | 2 |
| Skor ağırlığı | 0 (no-score) |
| Görünürlük | VI (`R2`, `R5`, `R8`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check()`: `fare_media(type=3) → fare_products → fare_leg_rules` zincirinden type3 leg rule kapsamı çıkarılır. Bir route için:
```
route.cemv_support=2 VE route KAPSANIYOR (global VEYA network eşleşir VEYA area eşleşir)
→ XFL_027
```

## Yanlış pozitif / negatif (asıl tasarım riski)
- **FP düşük:** yalnız KESİN kapsama (global/network/area eşleşmesi) tetikler; belirsizlikte tetiklenmez.
- **Yanlış negatif:** area zinciri eksikse area-eşleşmeli kapsama görülmeyebilir (konservatif).

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Önem · Sınıf |
|---|---|---|
| **XFL_027** | route cemv=2 ama uygulanabilir contactless product var; Fares v2 geçerlidir | BİLGİ · Interop · skor dışı |
| **AGN_012 / RTS_024** | cemv_support enum geçersiz | DÜŞÜK · Spec |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** cEMV bayrağı ile Fares v2 bilgisi farklı; Fares v2 bilgisi geçerlidir
- **Çözüm önerisi:** Bu yalnızca precedence bilgisidir; Fares v2 bilgisi geçerlidir.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"'{route_id}' route'unda cEMV bayrağı ile Fares v2 bilgisi farklı; GTFS precedence gereği Fares v2 bilgisi geçerlidir."*

## Ölçülen alan / değer
- **Dosya/alan:** `routes.txt` → `cemv_support`; zincir `fare_media`/`fare_products`/`fare_leg_rules`.

## Kod değiştirirken minimum test matrisi
- cemv=2 route + global contactless leg rule → XFL_027 var.
- cemv=2 route + network eşleşen contactless leg rule → var.
- cemv=2 route + contactless leg rule başka network → yok.
- cemv=2 route + hiç contactless product → yok.

---
## Teknik ek

### Skora katkı
- Bu bildirim skor dışıdır (`severity.weight() = 0.0`); yalnızca tüketici precedence davranışını görünür kılar.

### Hangi raporlarda görünür
`report_views = VI = [R2, R5, R8, R9]`.

### Dış araç eşleşmesi
- GTFS cEMV/Fares v2 precedence davranışını görünür kılan proje içi bir Interop bildirimidir; feed ihlali iddiasında bulunmaz.

### GTFS spec referansı
- GTFS-Fares v2 + `routes.txt::cemv_support` precedence ilişkisi.
- 🔗 [GTFS Reference — routes.txt](https://gtfs.org/documentation/schedule/reference/#routestxt)

### Kod referansı
- Emit: [k4_cross_ref.rs](../../../crates/pipeline/src/k4_cross_ref.rs) (`check`)

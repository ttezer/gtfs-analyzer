# XFL_010 — frequencies'te tanımsız trip_id

> **Karar:** `frequencies.txt` bir `trip_id`'ye referans veriyor ama bu sefer `trips.txt`'te tanımlı değil. KRİTİK, Spec — sıklık tanımı sefere bağlanamaz. **Düzeltme:** Eksik seferleri `trips.txt`'e ekleyin.
> ⚠️ Feed seviyesinde tek özet notice üretir; tüm tanımsız `trip_id`'ler tek satırda toplanır.

| Alan | Değer |
|---|---|
| Grup | XFL — Çapraz Dosya / Semantik |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Entity |
| Kimlik alanı | `trip_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_xfl()` içinde:
```
frequencies.trip_id boş değil
VE map.trips (trips.txt trip_id seti) trip_id'yi İÇERMİYOR
→ XFL_010 (tanımsız trip_id'ler dedup'lanıp tek feed-level notice)
```
- Kapsam birimi: feed (özet); observed value virgülle ayrılmış benzersiz `trip_id` listesi.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `frequencies.trip_id` boşsa atlanır.
- **Yanlış pozitif:** Yok; trips.txt mevcut ama ilgili sefer eksikse doğru tetiklenir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **XFL_010** | frequencies→trip referansı trips.txt'te yok | feed-level özet | KRİTİK · Spec · K4 |
| **FRQ_001** | Tekil frequencies satırında geçersiz `trip_id` | satır bazlı | — · Spec · K4 |
| **XFL_002** | trips→stop_times referansı eksik | farklı FK ekseni | YÜKSEK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** frequencies'te tanımsız trip_id
- **Çözüm önerisi:** Eksik seferleri `trips.txt`'e ekleyin.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"frequencies.txt'te trips.txt'te tanımlı olmayan trip_id'ler var: {liste}."*

## Ölçülen alan / değer
- **Dosya/alan:** `frequencies.txt` → `trip_id`; karşılaştırma kümesi `trips.txt` → `trip_id`.
- **Gözlenen değer:** Tanımsız `trip_id` listesi.
- **Beklenen değer:** Her `trip_id` için `trips.txt`'te kayıt.

## Kod değiştirirken minimum test matrisi
- frequencies'te trips.txt'te olmayan `trip_id` → XFL_010 çıkmalı.
- Aynı eksik `trip_id` birden çok frequency satırında → tek notice'ta dedup.
- `frequencies.trip_id` boş → XFL_010 çıkmamalı.
- Geçerli `trip_id` → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** Spec + KRİTİK → yayın engelleyici.
- **R5 kalite skoru:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `foreign_key_violation` (child=`frequencies.txt::trip_id` → parent=`trips.txt::trip_id`).
  - https://gtfs-validator.mobilitydata.org/rules.html
- GTFS Guru tarafında bu kontrolün canlı doğrulanmış birebir eşleşmesi tespit edilmedi.

### GTFS spec referansı
- GTFS `frequencies.txt::trip_id` → `trips.txt::trip_id` foreign key kuralı.
- 🔗 [GTFS Reference — frequencies.txt](https://gtfs.org/documentation/schedule/reference/#frequenciestxt) · [trips.txt](https://gtfs.org/documentation/schedule/reference/#tripstxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:2732](../../../crates/pipeline/src/k4_cross_ref.rs#L2732) (`check_xfl`)

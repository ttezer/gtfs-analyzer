# XFL_018 — feed_info.txt eksik

> **Karar:** `feed_info.txt` dosyası feed'de yok; besleme sürümü, yayıncı ve geçerlilik tarihleri tanımsız. ORTA, Quality — GTFS best-practice ihlali. **Düzeltme:** `feed_info.txt` ekleyin (`feed_publisher_name`, `feed_publisher_url`, `feed_lang`, `feed_start_date`, `feed_end_date`).
> ⚠️ Yalnızca dosyanın varlığını kontrol eder; içerik doğruluğu ayrı kuralların kapsamındadır.

| Alan | Değer |
|---|---|
| Grup | XFL — Çapraz Dosya / Semantik |
| Önem | Orta |
| Sınıf | Quality |
| Aşama | K4 |
| Varlık | Feed |
| Kimlik alanı | - |
| Skor tabanı | 2 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_xfl()` içinde:
```
records.feed_info BOŞ (feed_info.txt yok veya satırsız)
→ XFL_018 (tek feed-level notice)
```
- Kapsam birimi: feed.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** feed_info.txt mevcut ama tüm alanları boşsa XFL_018 çıkmaz (içerik kuralları devreye girer).
- **Yanlış pozitif:** Yok; dosya gerçekten yoksa best-practice uyarısı doğrudur.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **XFL_018** | feed_info.txt eksik (genel best-practice) | dosya yokluğu | ORTA · Quality · K4 |
| **XFL_016** | feed_info çevirisi var ama feed_info.txt yok | dosya yokluğu (çeviri bağlamı) | YÜKSEK · Spec · K4 |
| **XFL_011** | calendar↔feed_info tarih tutarsızlığı | tarih penceresi | ORTA · Interop · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** feed_info.txt eksik
- **Çözüm önerisi:** `feed_info.txt` ekleyin: `feed_publisher_name`, `feed_publisher_url`, `feed_lang`, `feed_start_date`, `feed_end_date`.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"feed_info.txt dosyası yok; besleme sürümü, yayıncı ve geçerlilik tarihleri tanımsız."*

## Ölçülen alan / değer
- **Dosya/alan:** `feed_info.txt` (varlık kontrolü).
- **Gözlenen değer:** Dosya yok / satırsız.
- **Beklenen değer:** `feed_info.txt` mevcut ve zorunlu alanları dolu.

## Kod değiştirirken minimum test matrisi
- feed_info.txt yok → XFL_018 çıkmalı.
- feed_info.txt en az bir satırla mevcut → XFL_018 çıkmamalı.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 2.0** (ORTA).
- **R1 blocker değil:** Quality sınıfı R1'e girmez.
- **R5 kalite skoru:** Quality sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `missing_recommended_file` (`feed_info.txt` önerilen dosya). MobilityData'da tarih alanlarına özgü `missing_feed_info_date` ayrı bir notice'tır.
  - https://gtfs-validator.mobilitydata.org/rules.html
- GTFS Guru tarafında bu kontrolün canlı doğrulanmış birebir eşleşmesi tespit edilmedi.

### GTFS spec referansı
- GTFS `feed_info.txt` — koşullu önerilen dosya; yayıncı/sürüm/geçerlilik bilgisi.
- 🔗 [GTFS Reference — feed_info.txt](https://gtfs.org/documentation/schedule/reference/#feed_infotxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:2883](../../../crates/pipeline/src/k4_cross_ref.rs#L2883) (`check_xfl`)

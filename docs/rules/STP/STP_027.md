# STP_027 — Pathway istasyonunda stop_access belirtilmemiş

> Karar: Pathway ağına bağlı platformlarda erişilebilirlik bilgisi bilinmiyor (`0`) veya boş bırakılmamalıdır.
> ⚠️ Bu kural yalnızca pathway uç noktası olan, parent istasyona bağlı fiziksel platformlarda çalışır; parent eksikse STP_032 önce çözülmelidir.

| Alan | Değer |
|---|---|
| Grup | STP — Stops |
| Önem | Orta |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Entity |
| Kimlik alanı | `stop_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_ACC (`R2`, `R5`, `R7`, `R9`) |
| Bloke ettiği kurallar | `PTH_012`, `PTH_013` |

## Tetikleme koşulu (kesin mantık)
K4 pathway uç noktalarından oluşan bir durak kümesi çıkarır. Bu kümedeki fiziksel platform (`location_type` boş veya `0`) bir istasyona bağlıysa ve `stop_access` boş/`0` ise STP_027 üretilir.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Platformun `parent_station` ilişkisi yoksa STP_032 devreye girer; STP_027 için istasyon bağı aranır.
- **Yanlış pozitif:** Erişim bilgisi bilinmiyor diye `0` yazmak veri olarak kabul edilir ama pathway senaryosunda kalite/spec riski taşır.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **STP_027** | pathway platformunda `stop_access` boş/0 | erişim bilgisi | ORTA · Spec · K4 |
| **STP_026** | `stop_access` geçersiz enum | enum doğrulama | DÜŞÜK · Spec · K4 |
| **STP_032** | pathway platformunda parent yok | istasyon bağı | ORTA · Quality · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** Pathway istasyonunda stop_access belirtilmemiş
- **Çözüm önerisi:** Pathway bağlı platformlarda `stop_access` değerini `1` veya `2` olarak belirtin.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"'<id>' platformunun stop_access'i belirtilmemiş veya 0 (bilinmiyor); pathway tanımlı istasyonda."*
- **Ayrıntılı çözüm:** *"Pathway tanımlı istasyonlarda platformların stop_access alanını doldurun."*

## Ölçülen alan / değer
- `file`: `stops.txt`
- `field`: `stop_access`
- `line`: ilgili platform satırı
- `observed`: boş veya `0`
- `expected`: erişim durumu açık değer

## Kod değiştirirken minimum test matrisi
| Senaryo | Beklenen |
|---|---|
| pathway platformu `stop_access=1` | `STP_027` yok |
| pathway platformu `stop_access=0` | `STP_027` |
| pathway platformunda parent yok | `STP_032` |

---

## Teknik ek
### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData tarafında yakın karşılıklar `duplicate_key`, `duplicate_geography_id`, `missing_stop_name`, `invalid_float`, `number_out_of_range`, `foreign_key_violation`, `location_without_parent_station`, `location_with_unexpected_stop_time`, `station_with_parent_station`, `invalid_timezone` ve `invalid_url` notice'larıdır. Bu projedeki yakın durak, kullanılmayan durak ve parent mesafe kontrollerinin bir kısmı proje-özel kalite/analytics kuralıdır.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS Schedule Reference: `stops.txt` ana tablosu; ilgili durumlarda `stop_times.txt`, `pathways.txt` ve `levels.txt` çapraz referansları.
- 🔗 https://gtfs.org/documentation/schedule/reference/#stopstxt

### Skora katkı
`Orta`, taban 1: teorik skor katkısı `1 * 2.0 = 2.0`.

### Hangi raporlarda görünür
`VS_ACC`: `R2`, `R5`, `R7`, `R9`.

### Kod referansı
- [`registry.rs`](../../../crates/rules/src/registry.rs)
- [`k4_cross_ref.rs`](../../../crates/pipeline/src/k4_cross_ref.rs)

# STP_004 — stop_lat sayısal değil

> Karar: `stop_lat` doluysa ondalıklı sayı olarak okunabilmelidir.
> ⚠️ Bu kural yalnızca parse hatasıdır; `stop_lat` boşsa STP_006, sayı olup `[-90, 90]` dışında kalıyorsa STP_003 devreye girer.

| Alan | Değer |
|---|---|
| Grup | STP — Stops |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `stop_id` |
| Skor tabanı | 2 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | `STP_021`, `GEO_002`, `GEO_009`, `GEO_012`, `STP_016`, `STP_017` |

## Tetikleme koşulu (kesin mantık)
K2 `stop_lat` alanı boş değilse `f64` olarak parse etmeye çalışır. Parse başarısızsa STP_004 üretilir.
### ✅ Gerçek feed mutasyonuyla ölçüldü (2026-08-15, issue #129)
Kural korpusun 239 feed'inin hiçbirinde ateşlemiyordu. **Kardeş kural kontrolü soruyu
önceden daralttı:** `SHP_009` (162 feed) · `SHP_020` (152) · `STM_045` (79) korpusta ateşliyor, yani `parse_f64` yolu gerçek veride
BAŞARIYLA çalışıyor — belirsiz olan yalnızca HATA dalıydı. `mdb-1903`'te `stop_lat = "abc"`
→ `STP_004` emit etti (`real_feed_mutation.rs`). **Hiçbir yan etki yok** — tek durağın
koordinatı bozulunca hiçbir toplu/kaskad kural sınıf değiştirmiyor.


## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Alan boşsa STP_006 üretir; parse hatası değildir.
- **Yanlış pozitif:** Virgüllü ondalık biçimi (`41,01`) geçerli sayı kabul edilmez.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **STP_004** | `stop_lat` sayısal değil | parse | KRİTİK · Spec · K2 |
| **STP_006** | `stop_lat` eksik | zorunlu alan | KRİTİK · Spec · K2 |
| **STP_003** | `stop_lat` sayısal ama aralık dışı | değer aralığı | KRİTİK · Spec · K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** stop_lat sayısal değil
- **Çözüm önerisi:** `stop_lat` değerini nokta ayracı kullanan ondalıklı enlem olarak yazın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"'<field>' için f64 bekleniyor, alınan: <raw>"*
- **Ayrıntılı çözüm:** *"stop_lat alanını ondalıklı enlem değeriyle doldurun."*

## Ölçülen alan / değer
- `file`: `stops.txt`
- `field`: `stop_lat`
- `line`: ilgili satır
- `observed`: parse edilemeyen değer
- `expected`: `f64`

## Kod değiştirirken minimum test matrisi
| Senaryo | Beklenen |
|---|---|
| `stop_lat=41.01` | `STP_004` yok |
| `stop_lat=41,01` | `STP_004` |
| `stop_lat` boş | `STP_006` |

---

## Teknik ek
### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData tarafında yakın karşılıklar `duplicate_key`, `duplicate_geography_id`, `missing_stop_name`, `invalid_float`, `number_out_of_range`, `foreign_key_violation`, `location_without_parent_station`, `location_with_unexpected_stop_time`, `station_with_parent_station`, `invalid_timezone` ve `invalid_url` notice'larıdır. Bu projedeki yakın durak, kullanılmayan durak ve parent mesafe kontrollerinin bir kısmı proje-özel kalite/analytics kuralıdır.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS Schedule Reference: `stops.txt` ana tablosu; ilgili durumlarda `stop_times.txt`, `pathways.txt` ve `levels.txt` çapraz referansları.
- 🔗 https://gtfs.org/documentation/schedule/reference/#stopstxt

### Skora katkı
`Kritik`, taban 2: teorik skor katkısı `2 * 4.0 = 8.0`.

### Hangi raporlarda görünür
`VS_K`: `R1`, `R2`, `R5`, `R9`.

### Kod referansı
- [`registry.rs`](../../../crates/rules/src/registry.rs)
- [`stops.rs`](../../../crates/pipeline/src/k2/stops.rs)

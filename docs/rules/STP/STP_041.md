# STP_041 — Alt durak adı üst istasyon adını içermiyor

| Alan | Değer |
|---|---|
| Önem | Düşük |
| Sınıf | Quality |
| Görünürlük | VS (`R2`, `R5`, `R9`) |

Yalnız `stop_name_best_practices=true` profilinde çalışır; yerel adlandırma modelleri nedeniyle varsayılan kapalıdır.

### R9 Kural Mesajı
- **Kural mesajı:** Alt durak adı üst istasyon adını içermiyor

### Kod referansı
- [`k6_analytics.rs`](../../../crates/pipeline/src/k6_analytics.rs)

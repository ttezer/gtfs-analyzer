# STP_040 — Durak adı gereksiz genel stop/station sözcüğü içeriyor

| Alan | Değer |
|---|---|
| Önem | Düşük |
| Sınıf | Quality |
| Görünürlük | VS (`R2`, `R5`, `R9`) |

Yalnız `stop_name_best_practices=true` profilinde çalışır. Dil bağımlı olduğu için varsayılan kapalıdır.

### R9 Kural Mesajı
- **Kural mesajı:** Durak adı gereksiz genel stop/station sözcüğü içeriyor

### Kod referansı
- [`k6_analytics.rs`](../../../crates/pipeline/src/k6_analytics.rs)

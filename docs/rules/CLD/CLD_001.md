# CLD_001 — service_id eksik

> **Karar:** `calendar_dates.txt` satırında `service_id` boş. KRİTİK, yayın engelleyici. **Düzeltme:** İstisna gününü hangi servise aitse o `service_id` ile bağlayın.
> ⚠️ Bu kural yalnızca alanın boş olmasını kontrol eder; verilen `service_id`nin gerçekten takvimlerde tanımlı olup olmadığı çapraz referans kurallarının konusudur.

| Alan | Değer |
|---|---|
| Grup | CLD — Calendar Dates |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Row |
| Kimlik alanı | `service_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::calendar_dates::validate_calendar_dates()` içinde:
```
calendar_dates.service_id trim sonrası boş
→ CLD_001
```
- Satır seviyesi kuraldır.
- Entity id gönderilmez; çünkü servis kimliği yoktur.
- Aynı satırda `date` veya `exception_type` hataları varsa onlar ayrı notice üretebilir.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `service_id` boş değil ama hatalı/olmayan bir servise referans veriyorsa CLD_001 çıkmaz.
- **Yanlış pozitif:** Yok; `calendar_dates.service_id` GTFS için zorunlu referans alanıdır.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **CLD_001** | `calendar_dates.service_id` boş | alan zorunluluğu | KRİTİK · Spec · k2 |
| **TRP_003** | `trips.service_id` eksik | trip alanı | KRİTİK · Spec · k2/k3 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** service_id eksik
- **Çözüm önerisi:** `service_id` alanını doldurun.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"service_id zorunludur."*

## Ölçülen alan / değer
- **Dosya/alan:** `calendar_dates.txt` → `service_id`
- **Gözlenen değer:** boş string.
- **Beklenen değer:** notice'ta gönderilmiyor; kavramsal beklenti geçerli bir `service_id`.

## Kod değiştirirken minimum test matrisi
- `service_id=""` → CLD_001 çıkmalı.
- `service_id="SVC1"` → CLD_001 çıkmamalı.
- `service_id` dolu ama calendar/calendar_dates servis setinde yok → CLD_001 değil çapraz referans kuralı beklenmeli.

---
## Teknik ek

### Dış araç eşleşmesi
- Henüz tarih damgalı, doğrulanmış dış araç eşleşmesi eklenmedi.

### GTFS spec referansı
- GTFS Schedule Reference — [calendar_dates.txt](https://gtfs.org/documentation/schedule/reference/#calendar_datestxt) ilgili alan/dosya tanımını verir.

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **Yayın engelleyici:** KRİTİK + Spec → R1 blocker.
- **R5 genel skor:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]` → R1, R2, R5, R9.

### Kod referansı
- Emit: [calendar_dates.rs:134](../../../crates/pipeline/src/k2/calendar_dates.rs#L134) (`validate_calendar_dates`) — notice [calendar_dates.rs:134](../../../crates/pipeline/src/k2/calendar_dates.rs#L134)
- Test: [calendar_dates.rs:428](../../../crates/pipeline/src/k2/calendar_dates.rs#L428) (`missing_service_id_produces_cld_001`)

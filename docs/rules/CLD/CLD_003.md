# CLD_003 — exception_type eksik veya geçersiz

> **Karar:** `calendar_dates.txt` satırında `exception_type` yok, sayı olarak parse edilemiyor veya `1/2` dışında. KRİTİK, yayın engelleyici. **Düzeltme:** `exception_type` değerini `1` (hizmet eklendi) veya `2` (hizmet kaldırıldı) yapın.
> ⚠️ Bu kural enum değerini doğrular; `exception_type=2` kullanımının anlamlı olup olmadığı ayrı çapraz/analitik kontrollerin konusudur.

| Alan | Değer |
|---|---|
| Grup | CLD — Calendar Dates |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Row |
| Kimlik alanı | `service_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | `CLD_005`, `CLD_006`, `CLD_007` |

## Tetikleme koşulu (kesin mantık)
`k2::calendar_dates::validate_calendar_dates()` içinde:
```
exception_type boşsa
VEYA sayı değilse
VEYA sayı ama 1/2 dışında ise
→ CLD_003
```
- `1` hizmet eklendi, `2` hizmet kaldırıldı anlamındadır.
- Bir satırda tarih hatası olsa bile exception_type ayrıca kontrol edilir.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Yok; boş, parse edilemeyen ve enum dışı değerler kapsanır.
- **Yanlış pozitif:** `0`, `3`, `added`, `removed` gibi değerler üreticiye anlamlı gelebilir ama GTFS enum değildir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **CLD_003** | `exception_type` eksik/geçersiz | enum | KRİTİK · Spec · k2 |
| **CLD_004** | calendar_dates-only serviste aktif gün yok | kapsam | YÜKSEK · Interop · k4 |
| **XFL_006** | yalnız `exception_type=2` olan servis aktif olamaz | semantik | YÜKSEK · Spec · k4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** exception_type eksik veya geçersiz
- **Çözüm önerisi:** `exception_type` değerini `1` (hizmet eklendi) veya `2` (hizmet kaldırıldı) olarak ayarlayın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"exception_type zorunludur."*, *"exception_type 1 veya 2 olmalıdır."* veya parse hatası.

## Ölçülen alan / değer
- **Dosya/alan:** `calendar_dates.txt` → `exception_type`
- **Gözlenen değer:** boş string, hatalı ham değer veya enum dışı sayı.
- **Beklenen değer:** `1 veya 2`.

## Kod değiştirirken minimum test matrisi
- `exception_type=""` → CLD_003 çıkmalı.
- `exception_type=3` → CLD_003 çıkmalı.
- `exception_type=abc` → CLD_003 çıkmalı.
- `exception_type=1` veya `2` → CLD_003 çıkmamalı.

---
## Teknik ek

### Dış araç eşleşmesi
- Henüz tarih damgalı, doğrulanmış dış araç eşleşmesi eklenmedi.

### GTFS spec referansı
- GTFS Schedule Reference — [calendar_dates.txt](https://gtfs.org/documentation/schedule/reference/#calendar_datestxt) ilgili alan/dosya tanımını verir.

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **Yayın engelleyici:** KRİTİK + Spec → R1 blocker.
- **R5 genel skor:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]` → R1, R2, R5, R9.

### Bağımlılık (bunu maskeleyen root cause'lar)
CLD_003, `CLD_005`, `CLD_006`, `CLD_007` gibi exception semantiğine dayalı aşağı akış uyarılarını maskeleyebilir.

### Kod referansı
- Emit: [calendar_dates.rs:171](../../../crates/pipeline/src/k2/calendar_dates.rs#L171) (`validate_calendar_dates`) — eksik notice [calendar_dates.rs:171](../../../crates/pipeline/src/k2/calendar_dates.rs#L171), enum notice [calendar_dates.rs:171](../../../crates/pipeline/src/k2/calendar_dates.rs#L171)
- Test: [calendar_dates.rs:424](../../../crates/pipeline/src/k2/calendar_dates.rs#L424) (`invalid_exception_type_produces_cld_003`)

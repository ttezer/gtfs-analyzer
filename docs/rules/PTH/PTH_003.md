# PTH_003 — to_stop_id bulunamadı

> **Karar:** `pathways.txt.to_stop_id` değeri `stops.txt` içinde tanımlı değilse geçidin hedef düğümü çözülemez. KRİTİK, spec hatası. **Düzeltme:** `to_stop_id` değerini geçerli bir `stops.stop_id` ile eşleştirin.
> ⚠️ Bu root cause pathway graph, erişilebilir yol ve traversal analizlerini bozar; registry’de `PTH_011`, `PTH_012`, `PTH_013`, `PTH_015` kurallarını bloke eder.

| Alan | Değer |
|---|---|
| Grup | PTH — Pathways |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Entity |
| Kimlik alanı | `pathway_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | `PTH_011`, `PTH_012`, `PTH_013`, `PTH_015` |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_references()` içinde:
```
pathways.txt satırı okunur
VE to_stop_id boş değil
VE map.stops içinde to_stop_id yok
→ PTH_003
```
- Boş `to_stop_id` bu çapraz referans kontrolünde atlanır.
- Notice `pathways.txt` satırındaki `to_stop_id` alanını işaret eder.
- `from_stop_id` için aynı mantık PTH_002'dir.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Boş `to_stop_id` burada yakalanmaz; zorunlu alan/header kontrolü ayrı olmalıdır.
- **Yanlış pozitif:** Stop üretim pipeline'ında var sanılıp `stops.txt` çıktısına yazılmadıysa validator açısından referans kırılır.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **PTH_003** | `to_stop_id` hedef stop yok | bitiş referansı | KRİTİK · Spec · K4 |
| **PTH_002** | `from_stop_id` hedef stop yok | başlangıç referansı | KRİTİK · Spec · K4 |
| **PTH_014** | from/to farklı istasyon bağlamında | istasyon sınırı | KRİTİK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** to_stop_id bulunamadı
- **Çözüm önerisi:** `to_stop_id` değerini `stops.txt` içinde tanımlı bir `stop_id` yapın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"to_stop_id '{stop_id}' stops.txt'te tanımlı değil."*
- **Çözüm önerisi:** *"Geçerli bir stop_id kullanın."*

## Ölçülen alan / değer
- **Dosya/alan:** `pathways.txt` → `to_stop_id`; `stops.txt` → `stop_id`
- **Gözlenen değer:** `to_stop_id` stops map içinde yok.
- **Beklenen değer:** `to_stop_id` geçerli bir stop_id olmalı.

## Kod değiştirirken minimum test matrisi
- `to_stop_id` stops.txt'te yok → PTH_003 çıkmalı.
- `to_stop_id` stops.txt'te var → PTH_003 çıkmamalı.
- `from_stop_id` yok → PTH_002 çıkmalı, PTH_003 ile karışmamalı.
- Boş `to_stop_id` → PTH_003 çıkmamalı.
- Bloke ilişkileri registry’de korunmalı.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- Spec sınıfıdır; R1 kritik listesine girer.
- `blocks = [PTH_011, PTH_012, PTH_013, PTH_015]`.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]` → R1, R2, R5, R9.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `stops.txt` parse/primary-key hataları stop map'i eksik kurarsa PTH_003 zincirleme oluşabilir.

### Dış araç eşleşmesi
- MobilityData validator tarafında foreign-key referans kontrolleriyle eşdeğer bir kontrol ailesi bulunur; PTH_003 repo özel ID'sidir. Kaynak, 2026-06-03: [MobilityData GTFS validator rules](https://gtfs-validator.mobilitydata.org/rules.html)

### GTFS spec referansı
- GTFS `pathways.txt.to_stop_id` alanını `stops.stop_id` foreign key'i olarak tanımlar. Kaynak: [GTFS Schedule Reference — pathways.txt](https://gtfs.org/documentation/schedule/reference/#pathwaystxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:3257](../../../crates/pipeline/src/k4_cross_ref.rs#L3257) — notice [k4_cross_ref.rs:3257](../../../crates/pipeline/src/k4_cross_ref.rs#L3257)
- Registry: [registry.rs:251](../../../crates/rules/src/registry.rs#L251)

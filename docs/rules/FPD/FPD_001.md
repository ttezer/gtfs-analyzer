# FPD_001 — fare_product_id yineleniyor

> **Karar:** `fare_products.txt` içinde aynı `fare_product_id` birden fazla satırda kullanılmış. KRİTİK, spec hatası. **Düzeltme:** Her ücret ürününe benzersiz bir `fare_product_id` verin.
> ⚠️ Bu kural K3 entity map kurulurken çalışır; yinelenen ürün kimliği FLG_001 ve FTR_004 gibi ürün referansı kontrollerini belirsizleştirir.

| Alan | Değer |
|---|---|
| Grup | FPD — Fare Products |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K3 |
| Varlık | Row |
| Kimlik alanı | `fare_product_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | `FLG_001`, `FTR_004` |

## Tetikleme koşulu (kesin mantık)
`k3_entity_graph::build_fares_v2()` içinde:
```
fare_products.fare_product_id boş değil
VE aynı fare_product_id daha önce map.fare_product_ids içine eklenmiş
→ FPD_001
```
- Notice yinelenen sonraki satıra bağlanır.
- Mesaj ilk görünüm satırını da belirtir.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Boş `fare_product_id` bu tekillik kontrolünde atlanır; boş kimlik ayrı zorunlu alan/root-cause kontrollerine bırakılır.
- **Yanlış pozitif:** Aynı ücret ürünü iki satırda bilinçli tekrar edilmiş olsa bile GTFS primary key açısından geçerli değildir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **FPD_001** | `fare_products.txt` içinde yinelenen ürün kimliği | Primary key | KRİTİK · Spec · K3 |
| **FLG_001** | `fare_leg_rules.txt.fare_product_id` hedef ürününü bulamaz | Referans | KRİTİK · Spec · K4 |
| **FTR_004** | `fare_transfer_rules.txt.fare_product_id` hedef ürününü bulamaz | Referans | KRİTİK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** fare_product_id yineleniyor
- **Çözüm önerisi:** Her ücret ürününe benzersiz bir `fare_product_id` atayın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"`'<fare_product_id>' ücret ürünü tekrarlanıyor (ilk görünüm: satır <satır>).`"*
- **Çözüm önerisi:** `Her ücret ürününe benzersiz bir fare_product_id atayın.`

## Ölçülen alan / değer
- **Dosya/alan:** `fare_products.txt` → `fare_product_id`
- **Gözlenen değer:** Tekrarlanan `fare_product_id`
- **Beklenen değer:** Her ürün için tekil `fare_product_id`

## Kod değiştirirken minimum test matrisi
- Aynı `fare_product_id` iki satırda verildiğinde FPD_001 çıkmalı.
- Farklı `fare_product_id` değerlerinde FPD_001 çıkmamalı.
- Boş `fare_product_id` FPD_001 üretmemeli.
- FPD_001 varken FLG/FTR referans yorumunun belirsizleştiği korunmalı.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- Spec sınıfı ve `VS_K` görünürlüğü nedeniyle kritik rapor görünürlüklerine girer.

### Hangi raporlarda görünür
`report_views = VS_K` → R1, R2, R5, R9.

### Bağımlılık (bunu maskeleyen root cause'lar)
`FPD_001`, `FLG_001` ve `FTR_004` referans kontrollerini bloke eder.

### Dış araç eşleşmesi
2026-06-03 doğrulandı: MobilityData kuralları sayfasında birebir FPD_001 adı görünmedi; GTFS Fares v2 dokümantasyonu `fare_product_id` alanını fare product primary key'i olarak tanımlar.

Kaynaklar: [MobilityData GTFS Validator Rules](https://gtfs-validator.mobilitydata.org/rules.html), [GTFS Fares Feature Guide](https://gtfs.org/getting-started/features/fares/)

### GTFS spec referansı
[GTFS Schedule Reference - fare_products.txt](https://gtfs.org/documentation/schedule/reference/#fare_productstxt): `fare_product_id`, ücret ürününü tanımlayan unique ID alanıdır.

### Kod referansı
- Emit: [k3_entity_graph.rs](../../../crates/pipeline/src/k3_entity_graph.rs)


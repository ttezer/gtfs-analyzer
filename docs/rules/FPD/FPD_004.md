# FPD_004 — fare_media_id bulunamadı

> **Karar:** `fare_products.txt.fare_media_id` dolu ama `fare_media.txt` içinde tanımlı değil. KRİTİK, spec hatası. **Düzeltme:** Geçerli bir `fare_media_id` kullanın veya ilgili medya kaydını ekleyin.
> ⚠️ Bu kontrol yalnızca dolu `fare_media_id` için çalışır; boş medya alanı bazı ürünlerde geçerli olabilir.

| Alan | Değer |
|---|---|
| Grup | FPD — Fare Products |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Row |
| Kimlik alanı | `fare_product_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_fares_v2()` içinde:
```
fare_products.fare_media_id dolu
VE map.fare_media_ids içinde yok
→ FPD_004
```
- Entity id `fare_product_id` olarak verilir.
- Hedef tablo `fare_media.txt` kayıtlarıdır.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `fare_media_id` boşsa bu kural çalışmaz.
- **Yanlış pozitif:** `fare_media_id` başka sistemde tanımlı olsa bile GTFS feed içinde `fare_media.txt` kaydı yoksa geçersizdir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **FMD_001** | `fare_media.txt` içinde yinelenen medya kimliği | Primary key | KRİTİK · Spec · K3 |
| **FPD_004** | Ürün kaydındaki medya referansı bulunamadı | Foreign key | KRİTİK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** fare_media_id bulunamadı
- **Çözüm önerisi:** `fare_media.txt` içinde tanımlı geçerli bir `fare_media_id` kullanın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"`'<fare_media_id>' odeme araci fare_media.txt te tanimli degil.`"*
- **Çözüm önerisi:** `Gecerli bir fare_media_id kullanin.`

## Ölçülen alan / değer
- **Dosya/alan:** `fare_products.txt` → `fare_media_id`
- **Gözlenen değer:** `fare_media.txt` içinde bulunmayan medya kimliği
- **Beklenen değer:** `fare_media.txt.fare_media_id` içinde tanımlı değer

## Kod değiştirirken minimum test matrisi
- Var olmayan `fare_media_id` → FPD_004 çıkmalı.
- Geçerli `fare_media_id` → FPD_004 çıkmamalı.
- Boş `fare_media_id` → FPD_004 çıkmamalı.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- Spec sınıfı ve `VS_K` görünürlüğü nedeniyle kritik rapor görünürlüklerine girer.

### Hangi raporlarda görünür
`report_views = VS_K` → R1, R2, R5, R9.

### Bağımlılık (bunu maskeleyen root cause'lar)
`FMD_001` medya kimliği tekilliği bozuksa referans yorumu güvenilmezleşir.

### Dış araç eşleşmesi
2026-06-03 doğrulandı: MobilityData kuralları sayfasında birebir FPD_004 adı görünmedi; GTFS Fares v2 dokümantasyonu `fare_media_id` alanını `fare_media.fare_media_id` referansı olarak tanımlar.

Kaynaklar: [MobilityData GTFS Validator Rules](https://gtfs-validator.mobilitydata.org/rules.html), [GTFS Fares Feature Guide](https://gtfs.org/getting-started/features/fares/)

### GTFS spec referansı
[GTFS Schedule Reference - fare_products.txt](https://gtfs.org/documentation/schedule/reference/#fare_productstxt): `fare_media_id`, `fare_media.fare_media_id` değerine foreign ID olarak referans verir.

### Kod referansı
- Emit: [k4_cross_ref.rs](../../../crates/pipeline/src/k4_cross_ref.rs)


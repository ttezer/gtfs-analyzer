# AGN_003 — agency_url eksik veya geçersiz

> Karar: Her işletici satırında `agency_url` dolu ve URL biçiminde olmalıdır.
> ⚠️ `looks_like_url` sadece biçimsel kontrol yapar; URL çalışıyor mu, alan adı gerçekten var mı kontrol edilmez.

| Alan | Değer |
|---|---|
| Grup | AGN — Agency |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `agency_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
K2 `agency_url` değerini trim ederek okur.

İki ayrı durumda aynı kural üretilir:
- değer boşsa,
- değer dolu ama `looks_like_url` kontrolünden geçmezse.

> ℹ️ **`looks_like_url` 2026-08-03'te daraltıldı.** Spec `URL` tipini *"a fully qualified URL
> that includes http:// or https://"* diye tanımlar; kontrol daha önce yalnız
> `Url::parse(v).is_ok()` idi ve `mailto:`, `ftp://`, `file:///`, `javascript:` gibi her
> şemayı geçiriyordu. Artık **`http://` veya `https://` şeması zorunlu** (şema adı
> büyük/küçük harfe duyarsız). Korpus etkisi ölçüldü: 1 feed, 116 satır; hiçbir feed'in
> yayın kararı değişmedi.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `looks_like_url` sadece biçimsel kontrol yapar; URL çalışıyor mu, alan adı gerçekten var mı kontrol edilmez.
- **Yanlış pozitif:** Protokolsüz alan adları (`example.com`) üreticiye anlaşılır gelebilir ama geçerli URL kabul edilmez.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **AGN_003** | `agency_url` boş veya URL değil | zorunlu URL | KRİTİK · Spec · K2 |
| **AGN_008** | opsiyonel `agency_fare_url` dolu ama URL değil | ücret URL'si | DÜŞÜK · Spec · K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** agency_url eksik veya geçersiz
- **Çözüm önerisi:** `agency_url` alanına geçerli bir `http` veya `https` URL'si yazın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"agency_url zorunludur."*
- **Ayrıntılı çözüm:** *"Geçerli bir http/https URL'si girin."*
- **Ayrıntılı bulgu mesajı:** *"agency_url geçerli bir URL değil."*
- **Ayrıntılı çözüm:** *"Geçerli bir http/https URL'si kullanın."*

## Ölçülen alan / değer
- `file`: `agency.txt`
- `field`: `agency_url`
- `line`: satır numarası
- `observed`: boş string veya geçersiz URL değeri

## Kod değiştirirken minimum test matrisi
| Senaryo | Beklenen |
|---|---|
| `agency_url` boş | `AGN_003` |
| `agency_url=example.com` | `AGN_003` |
| `agency_url=https://example.com` | `AGN_003` yok |

---

## Teknik ek
### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData GTFS Schedule Validator'da bu grubun yakın karşılıkları `missing_required_agency_id`, `inconsistent_agency_timezone`, `invalid_timezone`, `invalid_url`, `invalid_language_code`, `invalid_phone_number` ve `invalid_email` notice'larıdır. Bu projedeki bazı AGN kuralları aynı alanı daha erken veya daha ürün odaklı sınıflandırır; birebir severity eşleşmesi garanti edilmez.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS Schedule Reference: `agency.txt` alanları (`agency_name`, `agency_url`, `agency_timezone`, `agency_lang`, `agency_phone`, `agency_email`, `agency_fare_url`).
- 🔗 https://gtfs.org/documentation/schedule/reference/#agencytxt

### Skora katkı
`Kritik`, taban 1: teorik skor katkısı `1 * 4.0 = 4.0`.

### Hangi raporlarda görünür
`VS`: `R2`, `R5`, `R9`.

### Kod referansı
- [`registry.rs`](../../../crates/rules/src/registry.rs#L92)
- [`agency.rs`](../../../crates/pipeline/src/k2/agency.rs#L71)


# DQ_005b — Hiçbir seferin durak zamanı yok

> Karar: `trips.txt` içinde seferler var ama hiçbir trip için stop_times kaydı yoksa feed işlevsel değildir.
> ⚠️ Bazı triplerde stop_times varsa bu kural çalışmaz; kısmi eksiklik başka kurallara kalır.

| Alan | Değer |
|---|---|
| Grup | DQ — Veri Kalitesi |
| Önem | Yüksek |
| Sınıf | Interop |
| Aşama | K6 |
| Varlık | Feed |
| Kimlik alanı | - |
| Skor tabanı | 2 |
| Görünürlük | VI_K (`R1`, `R2`, `R5`, `R8`, `R9`) |
| Bloke ettiği kurallar | `DQ_009` |

## Tetikleme koşulu (kesin mantık)
K6 `records.trips` boş değilse her trip'in `idx.by_trip` içinde stop_times kaydı olup olmadığını sayar.

Stop_times kaydı olmayan trip sayısı toplam trip sayısına eşitse DQ_005b üretilir. Bu kural “hiçbir trip'in stop_times yok” durumunu feed seviyesinde özetler.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Bazı triplerde stop_times varsa bu kural çalışmaz; kısmi eksiklik başka kurallara kalır.
- **Yanlış pozitif:** Placeholder feed'lerde beklenebilir ama yayınlanabilir GTFS için ciddi sorundur.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **DQ_005b** | tüm tripler stop_times'sız | trip-stop_times kapsamı | YÜKSEK · Interop · K6 |
| **DQ_009** | stop_times toplam satırı 0 | satır varlığı | BİLGİ · Quality · K6 |
| **XFL_002** | belirli trip için stop_times eksik | trip çapraz referansı | Spec |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** Hiçbir seferin durak zamanı yok
- **Çözüm önerisi:** `stop_times.txt` dosyasını oluşturun ve her trip için durak zamanlarını ekleyin.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"Feed'deki hiçbir sefer için stop_times kaydı bulunamadı."*
- **Ayrıntılı çözüm:** *"stop_times.txt dosyasını oluşturun."*

## Ölçülen alan / değer
- `file`: `stop_times.txt`
- `observed`: `"tüm seferler stop_times'sız"`

## Kod değiştirirken minimum test matrisi
| Senaryo | Beklenen |
|---|---|
| trip yok | `DQ_005b` yok |
| trip var, hiçbirinde stop_times yok | `DQ_005b` |
| en az bir tripte stop_times var | `DQ_005b` yok |

---

## Teknik ek
### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData dokümanı ERROR/WARNING/INFO ayrımını GTFS Reference, Best Practices ve kalite etkisine göre yapar. Bu projedeki DQ kurallarının önemli kısmı ürün-özel kalite/analytics kontrolüdür; yakın dış karşılıklar alanına göre `missing_required_field`, `invalid_url`, `invalid_phone_number`, `invalid_language_code`, `duplicate_key`, `empty_file` ve feed geçerlilik/önerilen dosya uyarılarıdır, ancak çoğu DQ kuralı birebir MobilityData notice'ı değildir.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS Schedule Reference: kuralın alanına göre `feed_info.txt`, `routes.txt`, `trips.txt`, `stops.txt`, `stop_times.txt` ve genel dosya/alan tanımları.
- 🔗 https://gtfs.org/documentation/schedule/reference/

### Skora katkı
`Yüksek`, taban 2: teorik skor katkısı `2 * 3.0 = 6.0`.

### Hangi raporlarda görünür
`VI_K`: `R1`, `R2`, `R5`, `R8`, `R9`.

### Kod referansı
- [`registry.rs`](../../../crates/rules/src/registry.rs#L95)
- [`k6_analytics.rs`](../../../crates/pipeline/src/k6_analytics.rs#L4379)


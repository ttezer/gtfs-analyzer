# DQ_016 — Değerde fazladan boşluk karakteri

> Karar: CSV **her alan değerinin** başında veya sonunda gereksiz boşluk olmamalıdır (isim, id, url, desc, kod, sayı, enum, tarih ve saat dahil).
> ℹ️ Bulgular **DOSYA başına tek özet**tir: etkilenen satır sayısı + sütun listesi. Satır-başına DEĞİL.
> ⚠️ Alan içindeki anlamlı boşluklar değil, sadece baş/son boşluk kontrol edilir. Parser'ın tipli alanlarda boşluğu tolere etmesi ham girdiyi değiştirmez: tip/FK kontrolleri strict ham değer üzerinde çalışır. Yalnızca bu kök whitespace ihlalinden doğduğu kanıtlanan türev bulgular rapordan bastırılır; bağımsız içerik/aralık/FK hataları korunur.

## Neden dosya özeti (satır-başına değil)
Baş/son boşluk tek bir **üretici alışkanlığıdır** — en sık ayraç olarak `,` yerine `, ` yazmak — ve o zaman dosyanın **her satırında** çıkar. Satır-başına emit `mdb-992` feed'inde **2.361.873** notice üretiyordu (feed toplamının %98,6'sı; MobilityData aynı feed'de **12**): `fare_attributes` 1.118.170 · `fare_rules` 1.117.657 · `stops` 10.142 (=her satır). Bu, tarayıcıyı OOM eden **STM_050 vakasının** aynısı; aynı karar verildi.

**Tespit doğruydu, sunum yanlıştı:** RFC 4180'e göre ayraç sonrası boşluk alanın parçasıdır, yani boşluk gerçekten var (MD'nin parser'ı ayraç etrafını kırptığı için görmüyor). Özet + cascade kapısından sonra mdb-992'de 10 dosya kökü kaldı; kök notice'ları ham örnekleri ve bastırılan türev kural/sayaçlarını taşır.

| Alan | Değer |
|---|---|
| Grup | DQ — Veri Kalitesi |
| Önem | Orta |
| Sınıf | Quality |
| Aşama | K1/K2 |
| Varlık | File |
| Kimlik alanı | - |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
K1 genel CSV satırlarında, K2 ise streaming işlenen `stop_times.txt` satırlarında aynı mantığı uygular.

Satırdaki **herhangi** alan değeri `value != value.trim()` koşulunu sağlıyorsa DQ_016 üretilir. Notice field değeri, boşluk taşıyan alan adlarının virgülle birleştirilmiş listesidir; details içindeki `raw_samples` ilk ham değerleri, `affected_rows` gerçek satır sayısını taşır.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Alan içindeki anlamlı boşluklar değil, sadece baş/son boşluk kontrol edilir.
- **Yanlış pozitif:** Üretici baş/son boşluğu bilinçli koymuş olabilir; GTFS tüketicileri bunu çoğunlukla kirli veri sayar. Bu yine de ham lexical girdidir; sessiz trim uygulanmaz.
- **Kök/türev ayrımı:** `" 3"` gibi trim sonrası geçerli tipli değerlerde typed parse/FK semptomu DQ_016'ya bağlanır ve bastırılır. `" 99"`, `" 91.0"` gibi trim sonrası bağımsız enum/aralık ihlali taşıyan değerler bastırılmaz.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **DQ_016** | alan değerinde baş/son boşluk | değer temizliği | ORTA · Quality · K1/K2 |
| **ARC_010** | başlık/CSV yapısal sorunları | parse/format | archive |
| **ARC_021** | yazdırılamaz/ASCII dışı karakter | karakter seti | archive |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** Değerde fazladan boşluk karakteri
- **Çözüm önerisi:** CSV değerlerinin başındaki ve sonundaki gereksiz boşlukları kaldırın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"'<entity>' kaydında (<file>, satır <line>): '<fields>' alanlarında baştaki/sondaki boşluk var."*
- **Ayrıntılı çözüm:** *"Değerlerdeki gereksiz baştaki/sondaki boşlukları kaldırın."*

## Ölçülen alan / değer
- `file`: ilgili GTFS dosyası
- `field`: boşluk taşıyan alanların listesi
- `line`: satır numarası
- `observed`: `"satır <line>"`

## Kod değiştirirken minimum test matrisi
| Senaryo | Beklenen |
|---|---|
| `stop_id=" S1"` | `DQ_016`; yalnız trim edilmiş hedef mevcutsa türemiş FK notice'ı bastırılır |
| `stop_name="Merkez Durak"` | `DQ_016` yok |
| `stop_name="Merkez Durak "` | `DQ_016` |
| `route_type=" 3"` | `DQ_016`; typed parse türevi bastırılır, ham değer kanıtta kalır |
| `route_type=" 99"` | `DQ_016` + bağımsız enum bulgusu |
| `stop_lat=" 91.0"` | `DQ_016` + bağımsız aralık bulgusu |
| `pathway_mode=" 9"` | `DQ_016` + `PTH_004` |
| `is_bidirectional=" 9"` | `DQ_016` + `PTH_005` |
| `length=" -5"` | `DQ_016` + `PTH_006` |
| `traversal_time=" 0"` | `DQ_016` + `PTH_007` |
| `min_width=" 0"` | `DQ_016` + `PTH_010` |
| `pathway_mode=" 3"` | yalnız `DQ_016`; geçerli trim değeri türevdir |
| `stop_times.txt` streaming satırı | K2 tarafında `DQ_016` |

---

## Teknik ek
### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData dokümanı ERROR/WARNING/INFO ayrımını GTFS Reference, Best Practices ve kalite etkisine göre yapar. Bu projedeki DQ kurallarının önemli kısmı ürün-özel kalite/analytics kontrolüdür; yakın dış karşılıklar alanına göre `missing_required_field`, `invalid_url`, `invalid_phone_number`, `invalid_language_code`, `duplicate_key`, `empty_file` ve feed geçerlilik/önerilen dosya uyarılarıdır, ancak çoğu DQ kuralı birebir MobilityData notice'ı değildir.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS Schedule Reference: kuralın alanına göre `feed_info.txt`, `routes.txt`, `trips.txt`, `stops.txt`, `stop_times.txt` ve genel dosya/alan tanımları.
- 🔗 https://gtfs.org/documentation/schedule/reference/

### Skora katkı
`Orta`, taban 1: teorik skor katkısı `1 * 2.0 = 2.0`.

### Hangi raporlarda görünür
`VS`: `R2`, `R5`, `R9`.

### Kod referansı
- [`registry.rs`](../../../crates/rules/src/registry.rs#L90)
- [`k1_parse.rs`](../../../crates/pipeline/src/k1_parse.rs#L1585)
- [`stop_times.rs`](../../../crates/pipeline/src/k2/stop_times.rs#L2121)

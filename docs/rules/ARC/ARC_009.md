# ARC_009 — Dosyada veri satırı yok

> Karar: Tanınan GTFS `.txt` dosyası yalnızca başlıktan oluşamaz; en az bir veri satırı içermelidir.
> ⚠️ Tek boş veri satırı varsa dosya veri var gibi okunabilir; ARC_018 ayrıca boş satırı yakalar.

| Alan | Değer |
|---|---|
| Grup | ARC — Arşiv / Dosya Seviyesi |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K1/K2 |
| Varlık | File |
| Kimlik alanı | - |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
K1 CSV tokenize sonrası kayıt yoksa veya başlık çıkarıldıktan sonra veri satırı kalmıyorsa ARC_009 üretir. `stop_times.txt` streaming işlendiği için aynı kontrol K2 tarafında `total_rows == 0` ile yapılır.

**Önem dosyaya göre değişir (per-instance):** künye nominal `Kritik`'tir AMA emit'te `arc009_critical(file)` ile ayarlanır — yalnız ZORUNLU dosyalar (agency/stops/routes/trips/stop_times) + servis tanımlayan TAKVİM dosyaları (calendar/calendar_dates) boşken `Kritik` kalır (veri beklenen yerde yok). Diğer OPSİYONEL dosyalar (transfers, fare_*, shapes, frequencies, translations, Fares v2/Flex/JP …) boş eklendiğinde zararsızdır → `Bilgi`'ye düşürülür (eski davranış hepsini Kritik sayıyordu = yanlış-pozitif).

**İstisna — `calendar_dates.txt`:** Bu dosya şartlı zorunludur (calendar.txt yoksa servisi tanımlar). Boş (veya yalnızca başlık) bir `calendar_dates.txt`, feed'de `calendar.txt` MEVCUTSA servis zaten tanımlı olduğu için ARC_009 **bastırılır** (yanlış-pozitif). `calendar.txt` YOKSA boş `calendar_dates.txt` "hiç servis tanımı yok" demektir → ARC_009 KALIR (Kritik; ARC_008 yalnız dosya varlığına baktığından bu durumda tek sinyaldir).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Tek boş veri satırı varsa dosya veri var gibi okunabilir; ARC_018 ayrıca boş satırı yakalar.
- **Yanlış pozitif:** Opsiyonel dosya bilerek boş bırakılmış olabilir ama GTFS dosyası olarak eklendiyse veri satırı beklenir. (Özel durum: boş `calendar_dates.txt` + mevcut `calendar.txt` → yukarıdaki istisna ile bastırılır.)

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **ARC_009** | dosyada veri satırı yok | dosya içeriği | KRİTİK · Spec · K1/K2 |
| **ARC_018** | veri satırı tamamen boş | satır kalitesi | ORTA · Quality · K1/K2 |
| **ARC_004** | zorunlu dosya hiç yok | dosya varlığı | KRİTİK · Spec · K1 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** Dosyada veri satırı yok
- **Çözüm önerisi:** Dosyaya en az bir geçerli veri satırı ekleyin veya gereksiz opsiyonel dosyayı kaldırın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"'<file>' dosyası boş veya yalnızca başlık satırı içeriyor."*
- **Ayrıntılı bulgu mesajı:** *"'<file>' dosyasında başlık satırı var ama veri satırı yok."*
- **Ayrıntılı çözüm:** *"Dosyaya en az bir veri satırı ekleyin."*

## Ölçülen alan / değer
- `file`: ilgili `.txt`
- `field`: -
- `line`: dosya seviyesi
- `observed`: veri satırı yok

## Kod değiştirirken minimum test matrisi
| Senaryo | Beklenen |
|---|---|
| başlık + veri | `ARC_009` yok |
| sadece başlık | `ARC_009` |
| `stop_times.txt` sadece başlık | K2 yolunda `ARC_009` |

---

## Teknik ek
### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData tarafında yakın karşılıklar `csv_parsing_failed`, `duplicated_column`, `empty_column_name`, `empty_file`, `invalid_input_files_in_subfolder`, `invalid_row_length`, `missing_required_file`, `missing_required_field`, `invalid_character` ve UTF-8/IO kaynaklı loader hatalarıdır. Bu projede ARC bazı durumları fatal pipeline davranışıyla birlikte raporlar.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS Schedule Reference: feed ZIP yapısı, kök dizindeki GTFS `.txt` dosyaları, CSV/UTF-8 dosya biçimi ve required/optional file listesi.
- 🔗 https://gtfs.org/documentation/schedule/reference/

### Skora katkı
`Kritik`, taban 1: teorik skor katkısı `1 * 4.0 = 4.0`.

### Hangi raporlarda görünür
`VS_K`: `R1`, `R2`, `R5`, `R9`.

### Kod referansı
- [`registry.rs`](../../../crates/rules/src/registry.rs)
- [`k1_parse.rs`](../../../crates/pipeline/src/k1_parse.rs)
- [`stop_times.rs`](../../../crates/pipeline/src/k2/stop_times.rs)

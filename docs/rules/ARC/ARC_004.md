# ARC_004 — Zorunlu dosya eksik

<!-- md-parity: code=missing_required_file -->

> Karar: GTFS feed, validator'ın `REQUIRED_FILES` listesinde beklediği zorunlu dosyaları ZIP kök dizininde içermelidir.
> ⚠️ Dosya kökte var ama içeriği boşsa ARC_009 devreye girer.
> 📍 **`stops.txt` KOŞULLU zorunludur.** Spec: *"Optional if demand-responsive zones are defined in locations.geojson. Required otherwise."* Yalnızca-Flex bir feed hizmetini `locations.geojson` bölgeleriyle tanımlar ve hiç `stops.txt` taşımayabilir; bu durumda ARC_004 ÜRETİLMEZ. **İkisi de yoksa ARC_004 korunur ve sonuç `PARTIAL` olur** — hizmetin nerede verildiği raporda açıkça görünür, ancak bağımlı aşamalar çalıştırılmaz.
> 📌 **MobilityData paritesi:** MD'nin `missing_required_file` notice'ının karşılığı BU kuraldır.
> 2026-08-02'ye kadar bu pariteyi belgelemek için `AGN_001` adında, hiçbir yolla emit edilmeyen
> ayrı bir kayıt tutuluyordu; kaldırıldı — belgeleme yeri burasıdır, sahte bir kural değil.


| Alan | Değer |
|---|---|
| Grup | ARC — Arşiv / Dosya Seviyesi |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K1 |
| Varlık | Feed |
| Kimlik alanı | - |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | `RTS_002`, `STM_002`, `XFL_002`, `DQ_005`, `DQ_005b` |

## Tetikleme koşulu (kesin mantık)
K1 ZIP kök dizinindeki `.txt` dosyalarını `present_files` kümesine ekler. `REQUIRED_FILES` içindeki bir dosya bu kümede yoksa her eksik dosya için ARC_004 üretilir ve dosya `partial.unavailable_files` içine eklenir. Pipeline bağımsız K1/K2 kontrollerini tamamlar; eksik önkoşullara dayanan K4–K6 aşamalarını atlayarak `PARTIAL` rapor döndürür.

Alt dizindeki dosya var sayılmaz; bu durumda ARC_024 ayrı uyarı verir.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Dosya kökte var ama içeriği boşsa ARC_009 devreye girer.
- **Yanlış pozitif:** Dosya ZIP içinde alt klasördeyse kullanıcı onu var sanabilir; GTFS kök dizin bekler.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **ARC_004** | zorunlu dosya yok | dosya varlığı | KRİTİK · Spec · K1 |
| **ARC_024** | `.txt` alt dizinde | ZIP yerleşimi | ORTA · Spec · K1 |
| **ARC_009** | dosya var ama veri satırı yok | dosya içeriği | KRİTİK · Spec · K1/K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** Zorunlu dosya eksik
- **Çözüm önerisi:** Eksik zorunlu GTFS dosyasını ZIP kök dizinine ekleyin.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"Zorunlu GTFS dosyası eksik: '<file>'."*
- **Ayrıntılı çözüm:** *"Eksik dosyayı feed ZIP arşivine ekleyin."*

## Ölçülen alan / değer
- `file`: eksik dosya adı
- `field`: -
- `line`: feed seviyesi
- `observed`: eksik zorunlu dosya

## Kod değiştirirken minimum test matrisi
- stops.txt YOK + locations.geojson VAR → `PARTIAL`, ARC_004 YOK.
- stops.txt YOK + locations.geojson YOK → `PARTIAL` + ARC_004.
| Senaryo | Beklenen |
|---|---|
| bütün zorunlu dosyalar kökte | `ARC_004` yok |
| `stops.txt` eksik | `ARC_004` ve `PARTIAL` |
| zorunlu dosya alt dizinde | `ARC_024`, `ARC_004` ve `PARTIAL` |

---

## Teknik ek
### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData tarafında yakın karşılıklar `csv_parsing_failed`, `duplicated_column`, `empty_column_name`, `empty_file`, `invalid_input_files_in_subfolder`, `invalid_row_length`, `missing_required_file`, `missing_required_field`, `invalid_character` ve UTF-8/IO kaynaklı loader hatalarıdır. Bu projede ARC eksik dosyayı `PARTIAL` rapora taşır; bağımsız bulgular korunur.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS Schedule Reference: feed ZIP yapısı, kök dizindeki GTFS `.txt` dosyaları, CSV/UTF-8 dosya biçimi ve required/optional file listesi.
- 🔗 https://gtfs.org/documentation/schedule/reference/#dataset-files

### Skora katkı
`Kritik`, taban 1: teorik skor katkısı `1 * 4.0 = 4.0`.

### Hangi raporlarda görünür
`VS_K`: `R1`, `R2`, `R5`, `R9`.

### Kod referansı
- [`registry.rs`](../../../crates/rules/src/registry.rs)
- [`k1_parse.rs`](../../../crates/pipeline/src/k1_parse.rs)

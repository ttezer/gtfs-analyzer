# ARC_012 — Satır sütun sayısı başlıkla uyuşmuyor

> Karar: CSV veri satırındaki sütun sayısı başlık sütun sayısıyla uyumlu olmalıdır.
> ⚠️ Sütun sayısı doğru ama değer yanlış alana kaymışsa bu kural yakalamayabilir.

| Alan | Değer |
|---|---|
| Grup | ARC — Arşiv / Dosya Seviyesi |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K1/K2 |
| Varlık | Row |
| Kimlik alanı | - |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | `ARC_015` |

## Tetikleme koşulu (kesin mantık)
K1 her veri satırının sütun sayısını header sayısıyla karşılaştırır. `stop_times.txt` streaming işlendiği için aynı kontrol K2 `stop_times` akışında yapılır.

Satır header'dan uzunsa kritik format hatasıdır. Satır header'dan kısaysa mevcut kod notice severity değerini `Bilgi`ye düşürür; çünkü sondaki boş opsiyonel alanlar atlanmış olabilir.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Sütun sayısı doğru ama değer yanlış alana kaymışsa bu kural yakalamayabilir.
- **Yanlış pozitif:** Kısa satır opsiyonel sondaki alanları atlıyorsa bilgi seviyesine düşürülür.

## ⚠️ BOZUK SATIR YİNE AYRIŞTIRILIR — türev alan bulguları BİLİNÇLİ olarak bastırılmaz

17. korpus koşumunda ölçüldü (`ntd-60097`, 2026-09-11). Aynı 23 bozuk satır iki validatörde
farklı davranışa yol açıyor:

| | davranış |
|---|---|
| **MD** | `invalid_row_length` **23** verir ve o satırların ALANLARINI hiç doğrulamaz |
| **biz** | `ARC_012` **23** veririz (MD ile birebir) **ve** satırı ayrıştırmaya devam ederiz |

Ayrıştırmaya devam etmek türev bulgular üretir. O feed'de aynı 23 satırdan `STM_009` 23,
`STM_013` 48, `STP_017` 15, `STP_016` 5 ve dokuz kural daha doğuyor; en çarpıcı örnek
`pickup_type = 'Bath & Beyond)'` — kaçırılmamış bir virgülün kolonları kaydırması.
Feed toplamı bizde 410, MD'de 76.

**KARAR: bastırma kapısı KURULMADI.** Üç gerekçe:
1. **Hacim küçük ve tek feed.** Yüz notice mertebesinde; `TRP_004`'ün 70.135'i ya da
   `DQ_016` türevleri gibi bir patlama YOK. Kapı kurmanın maliyeti kazancı aşıyor.
2. **Yüzey geniş.** `ARC_012` kökünden türeyen her kuralı işaretlemek gerekir; boşluk
   bastırmasında (`DQ_016` ekseni) bu yüzeyin ne kadar büyüdüğü ölçüldü.
3. **MD'nin davranışı tartışmalı.** Bozuk satırı tamamen düşürmek, o satırdaki GERÇEK veri
   hatalarını da görünmez kılar. Satırı hem bozuk diye bildirmek hem içeriğini denetlemek
   üretici için daha bilgilendirici.

🔑 Bu yüzden `md_mapped_over`/`analyzer_spec_md_absent` kovalarında bu feed'in türev bulguları
görünmeye devam eder ve bu bir KUSUR DEĞİL, beyan edilmiş bir tasarım tercihidir.

⚠️ **Kolon kaydığını VARSAYMA, ÖLÇ.** Aynı turda `STM_022` (`timepoint='2'`, `mdb-2805`) ve
`STP_008` (`location_type='0.0'`, `mdb-2838`) da kolon kayması sanılmıştı; ikisinde de MD
`invalid_row_length` **0** ve bizde `ARC_012` YOK. Satırlar sağlam, değerler gerçekten geçersiz.
Türev şüphesini `ARC_012`/`invalid_row_length` sayısına bakarak doğrula.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **ARC_012** | row/header sütun sayısı farklı | CSV satır yapısı | KRİTİK · Spec · K1/K2 |
| **ARC_013** | CSV tokenize edilemiyor | CSV sözdizimi | KRİTİK · Spec · K1 |
| **ARC_016** | zorunlu alan boş/eksik | alan zorunluluğu | YÜKSEK · Spec · K1/K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** Satır sütun sayısı başlıkla uyuşmuyor
- **Çözüm önerisi:** Her satırın başlıkla aynı sayıda CSV alanı içerdiğinden emin olun.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"'<file>' <line>. satırda fazla alan: <n> (beklenen <header_count>) — kaçmamış virgül veya format hatası."*
- **Ayrıntılı bulgu mesajı:** *"'<file>' <line>. satırda sondaki <missing> isteğe bağlı alan atlanmış (<n> sütun, başlık: <header_count>)."*
- **Ayrıntılı çözüm:** *"Her satırın başlık sayısı kadar virgülle ayrılmış değer içerdiğinden emin olun."*

## Ölçülen alan / değer
- `file`: ilgili `.txt`
- `field`: -
- `line`: ilgili veri satırı
- `observed`: `<row_len> sütun`
- `expected`: `<header_count> sütun`

## Kod değiştirirken minimum test matrisi
| Senaryo | Beklenen |
|---|---|
| sütun sayısı eşit | `ARC_012` yok |
| fazla sütun | `ARC_012` kritik |
| eksik sondaki opsiyonel sütun | `ARC_012` bilgi severity |

---

## Teknik ek
### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData tarafında yakın karşılıklar `csv_parsing_failed`, `duplicated_column`, `empty_column_name`, `empty_file`, `invalid_input_files_in_subfolder`, `invalid_row_length`, `missing_required_file`, `missing_required_field`, `invalid_character` ve UTF-8/IO kaynaklı loader hatalarıdır. Bu projede ARC bazı durumları fatal pipeline davranışıyla birlikte raporlar.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS Schedule Reference: feed ZIP yapısı, kök dizindeki GTFS `.txt` dosyaları, CSV/UTF-8 dosya biçimi ve required/optional file listesi.
- 🔗 https://gtfs.org/documentation/schedule/reference/#file-requirements

### Skora katkı
`Kritik`, taban 1: teorik skor katkısı `1 * 4.0 = 4.0`.

### Hangi raporlarda görünür
`VS_K`: `R1`, `R2`, `R5`, `R9`.

### Kod referansı
- [`registry.rs`](../../../crates/rules/src/registry.rs)
- [`k1_parse.rs`](../../../crates/pipeline/src/k1_parse.rs)
- [`stop_times.rs`](../../../crates/pipeline/src/k2/stop_times.rs)

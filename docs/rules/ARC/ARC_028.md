# ARC_028 — GTFS yayın URL'si .zip dosya adıyla bitmiyor

| Alan | Değer |
|---|---|
| Önem | Düşük |
| Sınıf | Quality |
| Görünürlük | VS (`R2`, `R5`, `R9`) |

Yalnız analiz config'inde `source_url` sağlandığında çalışır. Query string ve fragment karşılaştırma öncesi çıkarılır. Dosya yükleme modunda sessizdir.

### R9 Kural Mesajı
- **Kural mesajı:** GTFS yayın URL'si .zip dosya adıyla bitmiyor

### Kod referansı
- [`k6_analytics.rs`](../../../crates/pipeline/src/k6_analytics.rs)

# ARC_008 — Takvim dosyası eksik

> Karar: Feed'de `calendar.txt` veya `calendar_dates.txt` dosyalarından en az biri bulunmalıdır.
> ⚠️ Dosya var ama boşsa ARC_009 veya sonraki takvim kuralları devreye girer.

| Alan | Değer |
|---|---|
| Grup | ARC — Arşiv / Dosya Seviyesi |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K1 |
| Varlık | Feed |
| Kimlik alanı | - |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
K1 `present_files` içinde `calendar.txt` veya `calendar_dates.txt` var mı diye bakar. İkisi de yoksa ARC_008 üretilir.

Bu kural kritik notice üretir ama mevcut kodda fatal değildir; pipeline devam eder.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Dosya var ama boşsa ARC_009 veya sonraki takvim kuralları devreye girer.
- **Yanlış pozitif:** Servis takvimi dış sistemden geliyor olsa bile GTFS feed içinde en az bir takvim dosyası beklenir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **ARC_008** | iki takvim dosyası da yok | dosya varlığı | KRİTİK · Spec · K1 |
| **CAL_*** | calendar.txt içerik sorunları | takvim satırları | Spec/Quality |
| **CLD_*** | calendar_dates.txt içerik sorunları | istisna günleri | Spec/Quality |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** Takvim dosyası eksik (calendar.txt ve calendar_dates.txt)
- **Çözüm önerisi:** ZIP'e `calendar.txt` veya `calendar_dates.txt` dosyalarından en az birini ekleyin.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"calendar.txt ve calendar_dates.txt dosyalarının ikisi de eksik."*
- **Ayrıntılı çözüm:** *"En az birini (calendar.txt veya calendar_dates.txt) ZIP'e ekleyin."*

## Ölçülen alan / değer
- `file`: `calendar.txt|calendar_dates.txt`
- `field`: -
- `line`: feed seviyesi
- `observed`: iki takvim dosyası da yok

## Kod değiştirirken minimum test matrisi
| Senaryo | Beklenen |
|---|---|
| sadece `calendar.txt` var | `ARC_008` yok |
| sadece `calendar_dates.txt` var | `ARC_008` yok |
| ikisi de yok | `ARC_008` |

---

## Teknik ek
### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData tarafında yakın karşılıklar `csv_parsing_failed`, `duplicated_column`, `empty_column_name`, `empty_file`, `invalid_input_files_in_subfolder`, `invalid_row_length`, `missing_required_file`, `missing_required_field`, `invalid_character` ve UTF-8/IO kaynaklı loader hatalarıdır. Bu projede ARC bazı durumları fatal pipeline davranışıyla birlikte raporlar.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS Schedule Reference: feed ZIP yapısı, kök dizindeki GTFS `.txt` dosyaları, CSV/UTF-8 dosya biçimi ve required/optional file listesi.
- 🔗 https://gtfs.org/documentation/schedule/reference/

### Skora katkı
`Kritik`, taban 1: teorik skor katkısı `1 * 4.0 = 4.0`.

### Hangi raporlarda görünür
`VS_K`: `R1`, `R2`, `R5`, `R9`.

### Kod referansı
- [`registry.rs`](../../../crates/rules/src/registry.rs)
- [`k1_parse.rs`](../../../crates/pipeline/src/k1_parse.rs)

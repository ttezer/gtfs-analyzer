# ARC_034 — Başlık satırı veri satırı olarak tekrarlanmış

> **Karar:** Dosyanın ortasında başlık satırı yeniden yazılmış; ayrıştırıcı onu veri sanıyor ve her alan kendi sütun adını "değer" olarak alıyor. YÜKSEK, Spec. **Düzeltme:** Tekrar eden başlık satırını kaldırın.
> ⚠️ Satır veri olarak **KAYDEDİLMEZ** — hayalet varlık oluşmaz.
> 🔑 **Neden ayrı bir kural:** tek yapısal kusur, aksi hâlde onlarca alan hatasına dağılır. `mdb-1003`/`mdb-1004`'te hayalet bir agency'nin `agency_timezone`'u literal `"agency_timezone"` oluyor ve `AGN_004` + `AGN_005` üretiyordu; hiçbiri kullanıcıya asıl sorunu söylemiyordu. `ARC_018`'in emsali aynıdır: tamamen boş bir satır TEK hatadır, on üç eksik alan değil.

| Alan | Değer |
|---|---|
| Grup | ARC — Arşiv / CSV yapısı |
| Önem | Yüksek |
| Sınıf | Spec |
| Otorite kaynağı | GTFS_SPEC |
| Aşama | K1 |
| Varlık | File |
| Kimlik alanı | - |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k1_parse` satır döngüsünde, her veri satırı için:

```
row.len() == headers.len()
  && !headers.is_empty()
  && her (değer, başlık) çifti için  değer.trim() == başlık.trim()
→ ARC_034, satır KAYDEDİLMEZ (continue)
```

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış pozitif kapısı BÜTÜN alanların eşleşmesidir.** Tek bir sütunda `route_id` değerinin `"route_id"` olması meşrudur ve tetiklemez. Bütün alanların birden kendi sütun adına eşit olması başka türlü oluşamaz.
- **Yanlış negatif:** başlık tekrarı SÜTUN SIRASI değişmiş hâlde yazılmışsa (`agency_name,agency_id,...`) eşleşme kurulmaz ve kural susar. Bu bilinçlidir: sıra değişmişse satır gerçekten farklı bir şeydir ve `ARC_012` alanına girer.
- Sütun sayısı başlıkla uyuşmuyorsa kural çalışmaz; o vaka `ARC_012`'nindir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **ARC_034** | veri satırı başlığın birebir tekrarı | dosya yapısı | YÜKSEK · Spec · K1 |
| **ARC_018** | satırın tamamı boş | dosya yapısı | ORTA · Spec · K1 |
| **ARC_015** | BAŞLIK içinde aynı sütun iki kez | başlık yapısı | KRİTİK · Spec · K1 |
| **ARC_012** | satır sütun sayısı başlıkla uyuşmuyor | satır yapısı | KRİTİK · Spec · K1 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** Başlık satırı veri satırı olarak tekrarlanmış
- **Çözüm önerisi:** Dosyanın ortasındaki tekrar eden başlık satırını kaldırın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"'<dosya>' <satır>. satırı başlık satırının tekrarı — veri olarak okunuyor."*
- **Ayrıntılı çözüm:** *"Dosyanın ortasındaki tekrar eden başlık satırını kaldırın; genellikle iki dosyanın uç uca eklenmesinden doğar."*

## Ölçülen alan / değer
- `file`: ilgili dosya
- `line`: tekrar eden başlık satırının numarası

## Kod değiştirirken minimum test matrisi
| Senaryo | Beklenen |
|---|---|
| Dosya ortasında birebir başlık tekrarı | `ARC_034`, satır kaydedilmez |
| Tek alan kendi sütun adına eşit | `ARC_034` YOK |
| Satırın tamamı boş | `ARC_018`, `ARC_034` YOK |

---

## Teknik ek
### Dış araç eşleşmesi
- MobilityData'da karşılığı **YOK** — 200 kodluk katalogda tekrar eden başlık satırını denetleyen kod bulunmuyor. Onların tarafında bu kusur, `unexpected_enum_value` ve `invalid_timezone` gibi türev bulgulara dağılır.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS Schedule Reference, Dataset Files: *"The first line of each file must contain field names."* Tek bir başlık satırı öngörülür.
- 🔗 https://gtfs.org/documentation/schedule/reference/#file-requirements

### Skora katkı
`Yüksek`, taban 1: teorik skor katkısı `1 * 3.0 = 3.0`.

### Hangi raporlarda görünür
`VS`: `R2`, `R5`, `R9`.

### Kod referansı
- Emit: [k1_parse.rs](../../../crates/pipeline/src/k1_parse.rs)
- [registry.rs](../../../crates/rules/src/registry.rs)

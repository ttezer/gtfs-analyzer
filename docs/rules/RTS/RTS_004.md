# RTS_004 — route_type eksik veya geçersiz

> **Karar:** `routes.txt::route_type` boş, sayı değil ya da geçerli GTFS hat tipi (0-7, 11, 12 veya genişletilmiş 100-1799) değil. KRİTİK, Spec. **Düzeltme:** Geçerli bir `route_type` girin.
> ⚠️ Üç durum da RTS_004 üretir: boş, parse hatası, geçersiz enum.

| Alan | Değer |
|---|---|
| Grup | RTS — Routes |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `route_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::routes::validate_routes()` içinde:
```
route_type boş VEYA parse hatası VEYA !is_valid_route_type(val)
→ RTS_004 (route bazlı notice)
```
- Geçerli değerler: 0-7, 11, 12 ve genişletilmiş 100-1799.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Yok; üç hata yolu kapsanır.
- **Yanlış pozitif:** Genişletilmiş tip aralığı dışındaki yeni/özel tipler hatalı raporlanabilir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **RTS_004** | route_type eksik/geçersiz | tip enum | KRİTİK · Spec · K2 |
| **TRP_024** | Block içinde tutarsız route_type | block tutarlılığı | DÜŞÜK · Interop · K6 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** route_type eksik veya geçersiz
- **Çözüm önerisi:** Geçerli bir `route_type` girin (0-7, 11, 12 veya genişletilmiş 100-1799).

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"route_type zorunludur."* / *"route_type {değer} geçerli bir GTFS hat tipi değil."*

## Ölçülen alan / değer
- **Dosya/alan:** `routes.txt` → `route_type`.
- **Gözlenen değer:** Boş / geçersiz değer.
- **Beklenen değer:** `0-7, 11, 12, 100-1799`.

## Kod değiştirirken minimum test matrisi
- Boş `route_type` → RTS_004 çıkmalı.
- `route_type=99` (geçersiz) → RTS_004 çıkmalı.
- `route_type=3` → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** Spec + KRİTİK → yayın engelleyici.
- **R5 genel skor:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılıklar: eksik için `missing_required_field`, geçersiz değer için `unexpected_enum_value` (`routes.txt::route_type`).
  - https://gtfs-validator.mobilitydata.org/rules.html
- GTFS Guru tarafında bu kontrolün canlı doğrulanmış birebir eşleşmesi tespit edilmedi.

### GTFS spec referansı
- GTFS `routes.txt::route_type` — zorunlu, tanımlı hat tipi enum'u.
- 🔗 [GTFS Reference — routes.txt](https://gtfs.org/documentation/schedule/reference/#routestxt)

### Kod referansı
- Emit: [k2/routes.rs:134](../../../crates/pipeline/src/k2/routes.rs#L134) (`validate_routes`)

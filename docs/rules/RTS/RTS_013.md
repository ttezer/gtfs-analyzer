# RTS_013 — continuous_pickup geçersiz

> **Karar:** `routes.txt::continuous_pickup` geçerli enum (0–3) değil. DÜŞÜK, Spec. **Düzeltme:** `continuous_pickup`'ı geçerli bir GTFS enum değerine ayarlayın (0–3).
> ⚠️ Ortak `parse_continuous_field` yardımcısıyla doğrulanır; yalnızca dolu-ama-geçersiz değerler tetikler.

| Alan | Değer |
|---|---|
| Grup | RTS — Routes |
| Önem | Düşük |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `route_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::routes::validate_routes()` → `parse_continuous_field(... "continuous_pickup")`:
```
continuous_pickup dolu VE geçerli enum (0–3) değil
→ RTS_013 (route bazlı notice)
```
- Geçerli değerler: 0 (sürekli), 1 (yok), 2 (telefonla), 3 (sürücüye).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Boşsa atlanır (varsayılan 1).
- **Yanlış pozitif:** Yok.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **RTS_013** | continuous_pickup enum dışı | sürekli biniş | DÜŞÜK · Spec · K2 |
| **RTS_018** | continuous_drop_off enum dışı | sürekli iniş | DÜŞÜK · Spec · K2 |
| **TRP_019** | Continuous aktifken shape_id yok | geometri zorunluluğu | YÜKSEK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** continuous_pickup geçersiz
- **Çözüm önerisi:** `continuous_pickup`'ı geçerli bir GTFS enum değerine ayarlayın (0–3).

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"continuous_pickup geçerli bir değer değil (0–3)."*

## Ölçülen alan / değer
- **Dosya/alan:** `routes.txt` → `continuous_pickup`.
- **Gözlenen değer:** Geçersiz değer.
- **Beklenen değer:** `0–3`.

## Kod değiştirirken minimum test matrisi
- `continuous_pickup=5` → RTS_013 çıkmalı.
- `continuous_pickup=0..3` → notice yok.
- Boş → değerlendirilmemeli.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 1.0** (DÜŞÜK).
- **R1 blocker değil:** Spec + DÜŞÜK.
- **R5 kalite skoru:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `unexpected_enum_value` (`routes.txt::continuous_pickup`).
  - https://gtfs-validator.mobilitydata.org/rules.html
- GTFS Guru tarafında bu kontrolün canlı doğrulanmış birebir eşleşmesi tespit edilmedi.

### GTFS spec referansı
- GTFS `routes.txt::continuous_pickup` — geçerli değerler 0–3.
- 🔗 [GTFS Reference — routes.txt](https://gtfs.org/documentation/schedule/reference/#routestxt)

### Kod referansı
- Emit: [k2/routes.rs:226](../../../crates/pipeline/src/k2/routes.rs#L226) (`validate_routes`)

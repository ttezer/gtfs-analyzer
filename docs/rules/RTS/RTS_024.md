# RTS_024 — route_cemv_support geçersiz

> Karar: `route_cemv_support` verilmişse sayısal olarak okunabilmeli ve yalnız `0`, `1` veya `2` olabilir.
> ⚠️ **Sınıf Quality→Spec (2026-08-01, WP-3).** `Enum` tipi normatiftir (bkz. [[AGN_012]]).
> Bu kural yalnız ENUM KÜMESİNİ ölçer; `cemv_support`'un fare product'larla semantik
> tutarlılığı [[XFL_017]]/[[XFL_026]]/[[XFL_027]]'nin işidir ve onlar meşru biçimde Quality kalır.

| Alan | Değer |
|---|---|
| Grup | RTS — Routes |
| Önem | Düşük |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `route_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
K2 `route_cemv_support` değerini `parse_u32` ile okur.

Değer sayıya çevrilemez veya `2`den büyükse RTS_024 üretilir. `0`, `1` ve `2` geçerlidir (GTFS: 0/boş=bilgi yok, 1=contactless EMV destekleniyor, 2=desteklenmiyor). Alan yoksa veya boşsa kural çalışmaz.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Yok; alan dolu ve parse edilemiyorsa RTS_024 üretilir.
- **Yanlış pozitif:** Yok; `0/1/2` dışındaki sayısal değerler geçersizdir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **RTS_024** | `route_cemv_support` parse edilemez veya `> 2` | enum benzeri sayı | DÜŞÜK · Spec · K2 |
| **AGN_012** | `agency_cemv_support` parse edilemez veya `> 2` | enum benzeri sayı (agency tarafı) | DÜŞÜK · Spec · K2 |
| **XFL_017** | route vs agency cemv_support çelişiyor | tutarlılık | DÜŞÜK · Quality · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** route_cemv_support geçersiz
- **Çözüm önerisi:** `route_cemv_support` alanını `0` (bilgi yok), `1` (destekleniyor) veya `2` (desteklenmiyor) olarak ayarlayın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"route_cemv_support 0, 1 veya 2 olmalıdır."*

## Ölçülen alan / değer
- `file`: `routes.txt`
- `field`: `route_cemv_support`
- `observed`: `2`den büyük sayısal değer
- `expected`: `"0, 1 veya 2"`

## Kod değiştirirken minimum test matrisi
| Senaryo | Beklenen |
|---|---|
| Alan boş/yok | `RTS_024` yok |
| `route_cemv_support=0/1/2` | `RTS_024` yok |
| `route_cemv_support=3` | `RTS_024` |
| `route_cemv_support=abc` | `RTS_024` |

---

## Teknik ek

### Dış araç eşleşmesi
- MobilityData GTFS Schedule Validator'da yakın karşılık `invalid_cemv_support_value` (routes.txt::cemv_support enum).
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS Schedule Reference: `routes.txt::cemv_support` — geçerli değerler 0 (bilgi yok), 1 (destekleniyor), 2 (desteklenmiyor).
- 🔗 [GTFS Reference — routes.txt](https://gtfs.org/documentation/schedule/reference/#routestxt)

### Skora katkı
`Düşük`, taban 1: teorik skor katkısı `1 * 1.0 = 1.0`.

### Hangi raporlarda görünür
`VS`: `R2`, `R5`, `R9`.

### Kod referansı
- [`registry.rs`](../../../crates/rules/src/registry.rs)
- [`routes.rs`](../../../crates/pipeline/src/k2/routes.rs)

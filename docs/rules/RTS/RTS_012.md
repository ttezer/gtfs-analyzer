# RTS_012 — Seferi olmayan hat

> **Karar:** `routes.txt`'te tanımlı bir hat hiçbir `trips.txt` satırı tarafından kullanılmıyor; hat boş. ORTA, Quality. **Düzeltme:** Bu hatta seferler ekleyin veya hattı kaldırın.
> ⚠️ XFL_012'den farkı: RTS_012 hiç trip'i olmayan hattı, XFL_012 trip'i olan ama tüm trip'leri stop_times'sız hattı yakalar.

| Alan | Değer |
|---|---|
| Grup | RTS — Routes |
| Önem | Orta |
| Sınıf | Quality |
| Aşama | K4 |
| Varlık | Entity |
| Kimlik alanı | `route_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_routes()` içinde:
```
route_id, trips'in kullandığı route kümesinde DEĞİL
→ RTS_012 (route bazlı notice)
```
- Kapsam birimi: route (Entity).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Hattın en az bir trip'i varsa çıkmaz (boş trip'ler XFL_012).
- **Yanlış pozitif:** Sezonluk/gelecekte aktive edilecek hatlar bilinçli boş olabilir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **RTS_012** | Hattın hiç trip'i yok | trip yokluğu | ORTA · Quality · K4 |
| **XFL_012** | Trip var ama hepsi stop_times'sız | stop_times yokluğu | YÜKSEK · Quality · K4 |
| **RTS_016** | Hattın aktif servis günü yok | takvim aktifliği | DÜŞÜK · Quality · K6 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** Seferi olmayan hat
- **Çözüm önerisi:** Bu hatta seferler ekleyin veya hattı kaldırın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"'{ad}' kodlu hattın hiçbir seferi bulunmamaktadır."*

## Ölçülen alan / değer
- **Dosya/alan:** `routes.txt` → `route_id`; `trips.txt` → `route_id` kullanımı.
- **Gözlenen değer:** Kullanılmayan `route_id`.
- **Beklenen değer:** Her hattın en az bir seferi.

## Kod değiştirirken minimum test matrisi
- Hiç trip'i olmayan hat → RTS_012 çıkmalı.
- En az bir trip'i olan hat → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 2.0** (ORTA).
- **R1 blocker değil:** Quality sınıfı.
- **R5 genel skor:** Quality sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData ve GTFS Guru tarafında "seferi olmayan hat" için doğrulanmış birebir notice karşılığı tespit edilmedi; proje-özel Quality kuralıdır.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- Doğrudan spec maddesi yoktur; veri tutarlılığı sinyalidir.
- 🔗 [GTFS Reference — routes.txt](https://gtfs.org/documentation/schedule/reference/#routestxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:800](../../../crates/pipeline/src/k4_cross_ref.rs#L800) (`check_routes`)
- Test: [k4_cross_ref.rs:4582](../../../crates/pipeline/src/k4_cross_ref.rs#L4582)

# RTS_029 — route_sort_order geçersiz

> **Karar:** `routes.txt.route_sort_order` spec'te `Non-negative integer` tipindedir; negatif ya da sayı olmayan değer bulgu üretir. DÜŞÜK, Spec.
> 📌 2026-08-01'e kadar hata **sessizce yutuluyordu** (`Err(_) => None`): değer düşüyor, hattın sıralaması kayboluyor ve kullanıcı hiçbir şey görmüyordu. Aynı yutma deseni STM_058 ve PTH_027'de de bulundu — üçü aynı gün düzeltildi.

| Alan | Değer |
|---|---|
| Grup | RTS — Routes |
| Önem | Düşük |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `route_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
```text
K2: route_sort_order dolu
VE parse_u32(...) Err döndürüyor   (negatif değer veya sayı olmayan metin)
→ RTS_029
```

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Alan boşsa kural çalışmaz (Optional).
- **Yanlış pozitif:** Bilinmiyor. `0` geçerlidir — tip **non-negative**'dir, `Non-null` değil;
  PTH_027'nin `stair_count`'u ile karıştırılmamalı, orada sıfır yasaktır.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **RTS_029** | `route_sort_order` negatif/sayı değil | değer tipi | DÜŞÜK · Spec · K2 |
| **PTH_027** | `stair_count` sıfır veya tam sayı değil | değer tipi (sıfır YASAK) | ORTA · Spec · K2 |
| **RTS_004** | `route_type` eksik veya geçersiz | enum | KRİTİK · Spec · K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** route_sort_order geçersiz
- **Çözüm önerisi:** `route_sort_order` değerini negatif olmayan bir tam sayı olarak girin.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *`parse_u32` hata metni*
- **Ayrıntılı çözüm:** *"route_sort_order değerini negatif olmayan bir tam sayı olarak girin."*

## Ölçülen alan / değer
- **Dosya/alan:** `routes.txt` → `route_sort_order`
- **Gözlenen değer:** parse edilemeyen ham metin (ör. `-5`, `abc`)
- **Beklenen değer:** negatif olmayan tam sayı

## Kod değiştirirken minimum test matrisi
- `route_sort_order=-5` → RTS_029 çıkmalı.
- `route_sort_order=abc` → RTS_029 çıkmalı.
- `route_sort_order=0` → RTS_029 çıkmamalı (sıfır geçerli).
- `route_sort_order` boş → RTS_029 çıkmamalı.

---
## Teknik ek

### Dış araç eşleşmesi
- **2026-08-01:** MobilityData tarafında yakın kategori alan tipi doğrulamasıdır
  (`invalid_integer` ailesi).
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- `routes.txt` → `route_sort_order`: Field Type **Non-negative integer**, Presence **Optional**.
- 🔗 https://gtfs.org/documentation/schedule/reference/#routestxt
- 🔗 https://gtfs.org/documentation/schedule/reference/#field-types

### Skora katkı
- **severity.weight() = 1.0** (DÜŞÜK).
- **R1 blocker değil:** `VS` görünümü R1 içermez.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Kod referansı
- Emit: [routes.rs](../../../crates/pipeline/src/k2/routes.rs)
- Registry: [registry.rs](../../../crates/rules/src/registry.rs)

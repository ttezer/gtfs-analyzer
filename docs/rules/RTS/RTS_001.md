# RTS_001 — route_id yineleniyor

> **Karar:** `routes.txt`'te aynı `route_id` birden çok satırda kullanılmış; hat kimliği benzersiz olmalı. KRİTİK, Spec. **Düzeltme:** Her hatta benzersiz bir `route_id` atayın.
> ⚠️ K3 entity grafiği kurulurken tekrar tespit edilir; ilk görünüm korunur.

| Alan | Değer |
|---|---|
| Grup | RTS — Routes |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K3 |
| Varlık | Entity |
| Kimlik alanı | `route_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | `TRP_002`, `XFL_007`, `FRL_002`, `ATR_006` |

## Tetikleme koşulu (kesin mantık)
`k3_entity_graph` içinde:
```
route_id daha önce map.routes'a eklendi (tekrar)
→ RTS_001 (ilk görünüm satırı referanslı)
```
- Kapsam birimi: route (Entity).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `route_id` boşsa bu kural değil RTS_003/004 vb. devreye girer.
- **Yanlış pozitif:** Yok; tekrar eden anahtar kesin ihlaldir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **RTS_001** | route_id yineleniyor | kimlik tekilliği | KRİTİK · Spec · K3 |
| **RTS_019** | route adı (short/long) yineleniyor | ad tekilliği | ORTA · Quality · K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** route_id yineleniyor
- **Çözüm önerisi:** Her hatta benzersiz bir `route_id` atayın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"'{route_id}' hat kodu tekrarlanıyor."*

## Ölçülen alan / değer
- **Dosya/alan:** `routes.txt` → `route_id`.
- **Gözlenen değer:** Tekrarlanan `route_id`.
- **Beklenen değer:** Feed genelinde benzersiz `route_id`.

## Kod değiştirirken minimum test matrisi
- Aynı `route_id` iki satırda → RTS_001 çıkmalı.
- Benzersiz `route_id`'ler → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** Spec + KRİTİK → yayın engelleyici.
- **R5 kalite skoru:** Spec sınıf penaltısına girer.
- **Bağımlılık:** Sayılan FK/referans kurallarını maskeleyebilir.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
`blocks = [TRP_002, XFL_007, FRL_002, ATR_006]`.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `duplicate_key` (`routes.txt::route_id`).
  - https://gtfs-validator.mobilitydata.org/rules.html
- GTFS Guru tarafında bu kontrolün canlı doğrulanmış birebir eşleşmesi tespit edilmedi.

### GTFS spec referansı
- GTFS `routes.txt::route_id` — zorunlu, benzersiz birincil anahtar.
- 🔗 [GTFS Reference — routes.txt](https://gtfs.org/documentation/schedule/reference/#routestxt)

### Kod referansı
- Emit: [k3_entity_graph.rs:215](../../../crates/pipeline/src/k3_entity_graph.rs#L215)
- Test: [k3_entity_graph.rs:686](../../../crates/pipeline/src/k3_entity_graph.rs#L686)

# RTS_003 — route_short_name ve route_long_name ikisi de eksik

> **Karar:** Bir hatta hem `route_short_name` hem `route_long_name` boş; en az biri zorunludur. KRİTİK, Spec. **Düzeltme:** `route_short_name` veya `route_long_name` alanını doldurun.
> ⚠️ Satır bazlı; iki ad da boşsa tetiklenir.

| Alan | Değer |
|---|---|
| Grup | RTS — Routes |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `route_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | `TRP_015`, `DQ_003` |

## Tetikleme koşulu (kesin mantık)
`k2::routes::validate_routes()` içinde:
```
route_short_name None VE route_long_name None
→ RTS_003 (route bazlı notice)
```
- Kapsam birimi: route (Entity).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Herhangi biri doluysa çıkmaz.
- **Yanlış pozitif:** Yok; GTFS en az birini zorunlu kılar.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **RTS_003** | İki ad da yok | ad yokluğu | KRİTİK · Spec · K2 |
| **RTS_009** | İki ad birbirinin aynısı | ad çakışması | DÜŞÜK · Spec · K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** route_short_name ve route_long_name ikisi de eksik
- **Çözüm önerisi:** `route_short_name` veya `route_long_name` alanını doldurun.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"route_short_name ve route_long_name alanlarının her ikisi de boş; en az biri zorunludur."*

## Ölçülen alan / değer
- **Dosya/alan:** `routes.txt` → `route_short_name`, `route_long_name`.
- **Gözlenen değer:** İkisi de boş.
- **Beklenen değer:** En az biri dolu.

## Kod değiştirirken minimum test matrisi
- İki ad da boş → RTS_003 çıkmalı.
- short veya long dolu → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** Spec + KRİTİK → yayın engelleyici.
- **R5 kalite skoru:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
`blocks = [TRP_015, DQ_003]`.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da birebir karşılık `route_both_short_and_long_name_missing`.
  - https://gtfs-validator.mobilitydata.org/rules.html
- GTFS Guru tarafında bu kontrolün canlı doğrulanmış birebir eşleşmesi tespit edilmedi.

### GTFS spec referansı
- GTFS `routes.txt` — `route_short_name` veya `route_long_name`'den en az biri zorunludur.
- 🔗 [GTFS Reference — routes.txt](https://gtfs.org/documentation/schedule/reference/#routestxt)

### Kod referansı
- Emit: [k2/routes.rs:65](../../../crates/pipeline/src/k2/routes.rs#L65) (`validate_routes`)

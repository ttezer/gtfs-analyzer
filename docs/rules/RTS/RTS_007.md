# RTS_007 — route_text_color geçersiz hex renk

> **Karar:** `routes.txt::route_text_color` 6 haneli geçerli hex renk kodu değil. DÜŞÜK, Quality. **Düzeltme:** Geçerli bir 6 haneli hex renk kullanın (örn. `FFFFFF`).
> ⚠️ Yalnızca dolu `route_text_color` kontrol edilir.

| Alan | Değer |
|---|---|
| Grup | RTS — Routes |
| Önem | Düşük |
| Sınıf | Quality |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `route_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::routes::validate_routes()` içinde:
```
route_text_color dolu VE is_hex_color_6(color) == false
→ RTS_007 (route bazlı notice)
```
- Kapsam birimi: route (Entity).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Boşsa atlanır (varsayılan siyah).
- **Yanlış pozitif:** Yok.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **RTS_007** | route_text_color geçersiz hex | metin rengi | DÜŞÜK · Quality · K2 |
| **RTS_006** | route_color geçersiz hex | arka plan rengi | ORTA · Spec · K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** route_text_color geçersiz hex renk
- **Çözüm önerisi:** Geçerli bir 6 haneli hex renk kullanın (örn. FFFFFF).

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"route_text_color 6 karakterli hex renk kodu olmalıdır."*

## Ölçülen alan / değer
- **Dosya/alan:** `routes.txt` → `route_text_color`.
- **Gözlenen değer:** Geçersiz renk kodu.
- **Beklenen değer:** 6 haneli hex.

## Kod değiştirirken minimum test matrisi
- `route_text_color=XYZ` → RTS_007 çıkmalı.
- `route_text_color=FFFFFF` → notice yok.
- Boş → değerlendirilmemeli.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 1.0** (DÜŞÜK).
- **R1 blocker değil:** Quality sınıfı.
- **R5 genel skor:** Quality sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `invalid_color` (`routes.txt::route_text_color`).
  - https://gtfs-validator.mobilitydata.org/rules.html
- GTFS Guru tarafında bu kontrolün canlı doğrulanmış birebir eşleşmesi tespit edilmedi.

### GTFS spec referansı
- GTFS `routes.txt::route_text_color` — `#` önekisiz 6 haneli hex renk.
- 🔗 [GTFS Reference — routes.txt](https://gtfs.org/documentation/schedule/reference/#routestxt)

### Kod referansı
- Emit: [k2/routes.rs:201](../../../crates/pipeline/src/k2/routes.rs#L201) (`validate_routes`)

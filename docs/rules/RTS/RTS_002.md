# RTS_002 — agency_id bulunamadı

> **Karar:** `routes.txt::agency_id` `agency.txt`'te tanımlı değil. KRİTİK, Spec — hat işleticisine bağlanamaz. **Düzeltme:** Geçerli bir `agency_id` kullanın.
> ⚠️ Satır bazlı (per-route) FK; yalnızca dolu `agency_id` kontrol edilir.

| Alan | Değer |
|---|---|
| Grup | RTS — Routes |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Entity |
| Kimlik alanı | `route_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_routes()` içinde:
```
routes.agency_id Some VE map.agencies İÇERMİYOR
→ RTS_002 (route bazlı notice)
```
- Kapsam birimi: route (Entity).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `agency_id` boşsa atlanır (tek-işleticili feed'de opsiyonel).
- **Yanlış pozitif:** Yok.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **RTS_002** | routes→agency FK yok (satır) | FK, per-route | KRİTİK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** agency_id bulunamadı
- **Çözüm önerisi:** Geçerli bir `agency_id` kullanın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"'{agency_id}' işletici kodu agency.txt'te tanımlı değil."*

## Ölçülen alan / değer
- **Dosya/alan:** `routes.txt` → `agency_id`; karşılaştırma `agency.txt`.
- **Gözlenen değer:** Tanımsız `agency_id`.
- **Beklenen değer:** `agency.txt`'te kayıtlı `agency_id`.

## Kod değiştirirken minimum test matrisi
- agency.txt'te olmayan `agency_id` → RTS_002 çıkmalı.
- `agency_id` boş → notice yok.
- Geçerli `agency_id` → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** Spec + KRİTİK → yayın engelleyici.
- **R5 genel skor:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
`blocks = [RTS_002]`.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `foreign_key_violation` (`routes.txt::agency_id` → `agency.txt::agency_id`).
  - https://gtfs-validator.mobilitydata.org/rules.html
- GTFS Guru tarafında bu kontrolün canlı doğrulanmış birebir eşleşmesi tespit edilmedi.

### GTFS spec referansı
- GTFS `routes.txt::agency_id` → `agency.txt::agency_id` foreign key kuralı.
- 🔗 [GTFS Reference — routes.txt](https://gtfs.org/documentation/schedule/reference/#routestxt) · [agency.txt](https://gtfs.org/documentation/schedule/reference/#agencytxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:779](../../../crates/pipeline/src/k4_cross_ref.rs#L779) (`check_routes`)
- Test: [k4_cross_ref.rs:4525](../../../crates/pipeline/src/k4_cross_ref.rs#L4525)

# RTS_011 — route_long_name çok uzun

> **Karar:** `routes.txt::route_long_name` 100 karakterden uzun; uzun ad makul sınırda olmalı. DÜŞÜK, Quality. **Düzeltme:** `route_long_name`'i 100 karakterin altında tutun.
> ⚠️ Eşik **>100 karakter**; yalnızca dolu `route_long_name` kontrol edilir.

| Alan | Değer |
|---|---|
| Grup | RTS — Routes |
| Önem | Düşük |
| Sınıf | Quality |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `route_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::routes::validate_routes()` içinde:
```
route_long_name uzunluğu > 100
→ RTS_011 (route bazlı notice)
```
- Eşik: **>100 karakter**.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Boşsa atlanır.
- **Yanlış pozitif:** Bazı uzun güzergah açıklamaları meşru olabilir; kalite sinyalidir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **RTS_011** | route_long_name >100 karakter | uzunluk | DÜŞÜK · Quality · K2 |
| **RTS_010** | route_short_name >12 karakter | uzunluk | DÜŞÜK · Quality · K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** route_long_name çok uzun
- **Çözüm önerisi:** `route_long_name`'i 100 karakterin altında tutun.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"'{route_id}' hattının route_long_name değeri {n} karakter; 100 karakteri aşıyor."*

## Ölçülen alan / değer
- **Dosya/alan:** `routes.txt` → `route_long_name`.
- **Gözlenen değer:** Karakter sayısı (>100).
- **Beklenen değer:** `≤100`.

## Kod değiştirirken minimum test matrisi
- 101 karakterlik long_name → RTS_011 çıkmalı.
- ≤100 → notice yok.
- Boş → değerlendirilmemeli.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 1.0** (DÜŞÜK).
- **R1 blocker değil:** Quality sınıfı.
- **R5 kalite skoru:** Quality sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData ve GTFS Guru tarafında "route_long_name uzunluk sınırı" için doğrulanmış birebir notice karşılığı tespit edilmedi; proje-özel Quality kuralıdır (>100 karakter eşiği).
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS best-practice: `route_long_name` makul uzunlukta tutulmalıdır.
- 🔗 [GTFS Reference — routes.txt](https://gtfs.org/documentation/schedule/reference/#routestxt)

### Kod referansı
- Emit: [k2/routes.rs:109](../../../crates/pipeline/src/k2/routes.rs#L109) (`validate_routes`)

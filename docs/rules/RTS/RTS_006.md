# RTS_006 — route_color geçersiz hex renk

> **Karar:** `routes.txt::route_color` 6 haneli geçerli hex renk kodu değil. ORTA, Spec. **Düzeltme:** Geçerli bir 6 haneli hex renk kullanın (örn. `FF0000`).
> ⚠️ Yalnızca dolu `route_color` kontrol edilir; `#` öneki olmadan 6 hex hane beklenir.

| Alan | Değer |
|---|---|
| Grup | RTS — Routes |
| Önem | Orta |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `route_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::routes::validate_routes()` içinde:
```
route_color dolu VE is_hex_color_6(color) == false
→ RTS_006 (route bazlı notice)
```
- Kapsam birimi: route (Entity).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Boşsa atlanır (varsayılan beyaz).
- **Yanlış pozitif:** Yok; 6 hane dışı kesin geçersizdir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **RTS_006** | route_color geçersiz hex | arka plan rengi | ORTA · Spec · K2 |
| **RTS_007** | route_text_color geçersiz hex | metin rengi | DÜŞÜK · Quality · K2 |
| **RTS_008** | renk kontrastı düşük | kontrast | ORTA · Quality · K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** route_color geçersiz hex renk
- **Çözüm önerisi:** Geçerli bir 6 haneli hex renk kullanın (örn. FF0000).

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"route_color 6 karakterli hex renk kodu olmalıdır."*

## Ölçülen alan / değer
- **Dosya/alan:** `routes.txt` → `route_color`.
- **Gözlenen değer:** Geçersiz renk kodu.
- **Beklenen değer:** 6 haneli hex.

## Kod değiştirirken minimum test matrisi
- `route_color=XYZ` → RTS_006 çıkmalı.
- `route_color=FF0000` → notice yok.
- Boş → değerlendirilmemeli.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 2.0** (ORTA).
- **R1 blocker değil:** Spec + ORTA.
- **R5 genel skor:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `invalid_color` (`routes.txt::route_color`).
  - https://gtfs-validator.mobilitydata.org/rules.html
- GTFS Guru tarafında bu kontrolün canlı doğrulanmış birebir eşleşmesi tespit edilmedi.

### GTFS spec referansı
- GTFS `routes.txt::route_color` — `#` önekisiz 6 haneli hex renk.
- 🔗 [GTFS Reference — routes.txt](https://gtfs.org/documentation/schedule/reference/#routestxt)

### Kod referansı
- Emit: [k2/routes.rs:178](../../../crates/pipeline/src/k2/routes.rs#L178) (`validate_routes`)

# FLG_001 — fare_product_id bulunamadı

> **Karar:** `fare_leg_rules.txt` içinde dolu bir `fare_product_id` var ama bu değer `fare_products.txt` içinde tanımlı değilse FLG_001 üretilir. **Düzeltme:** `fare_products.txt` içinde tanımlı geçerli bir `fare_product_id` kullanın.
> ⚠️ Bu kritik bir Fares v2 bağ hatasıdır; bacak kuralı hangi ücret ürününü uygulayacağını gösterir ama hedef ürün yoktur.

| Alan | Değer |
| --- | --- |
| Grup | FLG — Fare Leg Rules |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Row |
| Kimlik alanı | `fare_product_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
K4 `fare_leg_rules.txt.fare_product_id` değerini `map.fare_product_ids` içinde arar. Değer boş değilse ve bulunamazsa FLG_001 notice'ı üretir.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Kuralın bağlı olduğu alan boş, parse edilemez veya önceki aşamada elenmişse bu notice çalışmayabilir.
- **Yanlış pozitif:** Kod koşulu sağlanıyorsa veri modeli açısından sorun kabul edilir; özel işletme yorumu varsa veri tarafında ayrıca modellenmelidir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **FPD_001** | `fare_products.txt` içinde `fare_product_id` tekilliğini kontrol eder. | Komşu kontrol | - |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** fare_product_id bulunamadı

### R2 mesajı/mesajları
- `'<fare_product_id>' ucret urunu fare_products.txt te tanimli degil.`
- **Çözüm önerisi:** `fare_products.txt` içinde tanımlı geçerli bir `fare_product_id` kullanın.

## Ölçülen alan / değer
- Ölçülen alan: `fare_product_id`.

## Kod değiştirirken minimum test matrisi
- Temel pozitif örnek: koşul sağlandığında notice üretilmeli.
- Temel negatif örnek: geçerli satırda notice üretilmemeli.
- Sınır değer: boş, geçersiz veya en küçük/geçerli değer ayrı denenmeli.
- İlgili komşu kural ile karışmama testi yapılmalı.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (Kritik).
- **Sınıf/Aşama:** Spec · K4.

### Hangi raporlarda görünür
`report_views = VS_K` → R1, R2, R5, R9.

### Bağımlılık (bunu maskeleyen root cause'lar)
- Belirgin `blocks` ilişkisi yok; önce aynı satırdaki zorunlu kimlik ve parse hataları çözülmelidir.

### Dış araç eşleşmesi
2026-06-03 doğrulandı: MobilityData kuralları sayfasında birebir FLG_001 adı görünmedi; Fares v2 dokümantasyonu `fare_leg_rules.txt` içinde `fare_product_id` alanının `fare_products.fare_product_id` referansı olduğunu belirtir.

### GTFS spec referansı
[GTFS Schedule Reference - fare_leg_rules.txt](https://gtfs.org/documentation/schedule/reference/#fare_leg_rulestxt): `fare_product_id`, `fare_products.fare_product_id` değerine foreign ID olarak referans verir ve zorunludur.

### Kod referansı
- Emit: [kaynak](../../../crates/pipeline/src/k4_cross_ref.rs)

# FLG_002 — network_id bulunamadı

> **Karar:** `fare_leg_rules.txt.network_id` dolu olduğu halde hiçbir geçerli kaynakta (`networks.txt`, `routes.txt.network_id` sütunu veya `route_networks.txt`) tanımlı değilse FLG_002 üretilir. **Düzeltme:** Bu kaynaklardan birinde tanımlı geçerli bir `network_id` kullanın.
> ⚠️ `network_id` kümesi GTFS Fares v2'nin üç tanım kaynağından da kurulur: `networks.txt`, `routes.txt`'teki `network_id` sütunu ve `route_networks.txt` satırları.

| Alan | Değer |
| --- | --- |
| Grup | FLG — Fare Leg Rules |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Row |
| Kimlik alanı | `network_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
K4 `fare_leg_rules.txt.network_id` değerini `map.network_ids` içinde arar. Bu küme K3'te `networks.txt.network_id`, `routes.txt.network_id` ve `route_networks.txt.network_id` değerlerinden kurulur. Değer dolu ve kümede yoksa FLG_002 notice'ı üretir.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Kuralın bağlı olduğu alan boş, parse edilemez veya önceki aşamada elenmişse bu notice çalışmayabilir.
- **Yanlış pozitif:** Kod koşulu sağlanıyorsa veri modeli açısından sorun kabul edilir; özel işletme yorumu varsa veri tarafında ayrıca modellenmelidir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **NET_001** | `networks.txt.network_id` tekilliğini kontrol eder. | Komşu kontrol | - |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** network_id bulunamadı

### R2 mesajı/mesajları
- `'<network_id>' ag kodu networks.txt/routes.txt/route_networks.txt te tanimli degil.`
- **Çözüm önerisi:** `networks.txt`, `routes.txt` veya `route_networks.txt` içinde tanımlı geçerli bir `network_id` kullanın.

## Ölçülen alan / değer
- Ölçülen alan: `network_id`.

## Kod değiştirirken minimum test matrisi
- Temel pozitif örnek: koşul sağlandığında notice üretilmeli.
- Temel negatif örnek: geçerli satırda notice üretilmemeli.
- Sınır değer: boş, geçersiz veya en küçük/geçerli değer ayrı denenmeli.
- İlgili komşu kural ile karışmama testi yapılmalı.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (Kritik).
- **Sınıf/Aşama:** Spec · K4.

### Hangi raporlarda görünür
`report_views = VS_K` → R1, R2, R5, R9.

### Bağımlılık (bunu maskeleyen root cause'lar)
- Belirgin `blocks` ilişkisi yok; önce aynı satırdaki zorunlu kimlik ve parse hataları çözülmelidir.

### Dış araç eşleşmesi
2026-06-03 doğrulandı: MobilityData kuralları sayfasında birebir FLG_002 adı görünmedi; Fares v2 dokümantasyonu `network_id` alanının route/network bağlamında filtreleme yaptığını belirtir.

### GTFS spec referansı
[GTFS Schedule Reference - fare_leg_rules.txt](https://gtfs.org/documentation/schedule/reference/#fare_leg_rulestxt): `network_id`, `routes.network_id` veya `networks.network_id` referansı olarak fare leg matching alanıdır.

### Kod referansı
- Emit: [kaynak](../../../crates/pipeline/src/k4_cross_ref.rs)

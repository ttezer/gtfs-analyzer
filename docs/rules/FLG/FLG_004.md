# FLG_004 — to_area_id bulunamadı

> **Karar:** `fare_leg_rules.txt.to_area_id` dolu olduğu halde `areas.txt` içinde tanımlı değilse FLG_004 üretilir. **Düzeltme:** `areas.txt` içinde tanımlı geçerli bir `to_area_id` kullanın.
> ⚠️ Bu kural varış alanı referansını kontrol eder; alanın hangi durakları kapsadığı `stop_areas.txt` ilişkileriyle ayrıca belirlenir.

| Alan | Değer |
| --- | --- |
| Grup | FLG — Fare Leg Rules |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Row |
| Kimlik alanı | `to_area_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
K4 `to_area_id` değerini `map.areas` içinde arar. Dolu değer `areas.txt` içinde yoksa FLG_004 üretir.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Kuralın bağlı olduğu alan boş, parse edilemez veya önceki aşamada elenmişse bu notice çalışmayabilir.
- **Yanlış pozitif:** Kod koşulu sağlanıyorsa veri modeli açısından sorun kabul edilir; özel işletme yorumu varsa veri tarafında ayrıca modellenmelidir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **ARS_001** | `areas.txt.area_id` tekilliğini kontrol eder. | Komşu kontrol | - |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** to_area_id bulunamadı

### R2 mesajı/mesajları
- `'<to_area_id>' alan kodu areas.txt te tanimli degil.`
- **Çözüm önerisi:** `areas.txt` içinde tanımlı geçerli bir `to_area_id` kullanın.

## Ölçülen alan / değer
- Ölçülen alan: `to_area_id`.

## Kod değiştirirken minimum test matrisi
- Temel pozitif örnek: koşul sağlandığında notice üretilmeli.
- Temel negatif örnek: geçerli satırda notice üretilmemeli.
- Sınır değer: boş, geçersiz veya en küçük/geçerli değer ayrı denenmeli.
- İlgili komşu kural ile karışmama testi yapılmalı.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (Kritik).
- **Sınıf/Aşama:** Spec · K4.

### Hangi raporlarda görünür
`report_views = VS_K` → R1, R2, R5, R9.

### Bağımlılık (bunu maskeleyen root cause'lar)
- Belirgin `blocks` ilişkisi yok; önce aynı satırdaki zorunlu kimlik ve parse hataları çözülmelidir.

### Dış araç eşleşmesi
2026-06-03 doğrulandı: MobilityData kuralları sayfasında birebir FLG_004 adı görünmedi; Fares v2 dokümantasyonu `to_area_id` alanını varış alanı filtreleme referansı olarak tanımlar.

### GTFS spec referansı
[GTFS Schedule Reference - fare_leg_rules.txt](https://gtfs.org/documentation/schedule/reference/#fare_leg_rulestxt): `to_area_id`, `areas.area_id` referansı olarak varış alanını tanımlar.

### Kod referansı
- Emit: [kaynak](../../../crates/pipeline/src/k4_cross_ref.rs)

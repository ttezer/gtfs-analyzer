# TFR_002 — service_id bulunamadı

> **Karar:** `timeframes.txt::service_id` `calendar`/`calendar_dates`'te tanımlı değil. KRİTİK, Spec — zaman dilimi takvime bağlanamaz. **Düzeltme:** Geçerli bir `service_id` kullanın.
> ⚠️ Satır bazlı (Row); yalnızca dolu `service_id` için FK kontrolü.

| Alan | Değer |
|---|---|
| Grup | TFR — Timeframes (Fares v2) |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Row |
| Kimlik alanı | `timeframe_group_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_fares_v2()` içinde:
```
timeframes.service_id boş değil
VE map.services (calendar ∪ calendar_dates) İÇERMİYOR
→ TFR_002 (satır bazlı notice)
```
- Kapsam birimi: satır (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `service_id` boşsa atlanır.
- **Yanlış pozitif:** Yok; servis yalnızca calendar_dates'te olsa bile `map.services` içerir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TFR_002** | timeframes→service FK yok | FK (service) | KRİTİK · Spec · K4 |
| **TFR_001** | timeframe_group_id boş | zorunluluk | KRİTİK · Spec · K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** service_id bulunamadı
- **Çözüm önerisi:** Geçerli bir `service_id` kullanın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"'{service_id}' servis takvimi tanımlı değil."*

## Ölçülen alan / değer
- **Dosya/alan:** `timeframes.txt` → `service_id`; karşılaştırma `calendar.txt` + `calendar_dates.txt`.
- **Gözlenen değer:** Tanımsız `service_id`.
- **Beklenen değer:** Takvim dosyalarında kayıtlı `service_id`.

## Kod değiştirirken minimum test matrisi
- Takvimde olmayan `service_id` → TFR_002 çıkmalı.
- Geçerli `service_id` → notice yok.
- `service_id` boş → atlanır.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** Spec + KRİTİK → yayın engelleyici.
- **R5 genel skor:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `foreign_key_violation` (`timeframes.txt::service_id` → `calendar.txt`/`calendar_dates.txt`).
  - https://gtfs-validator.mobilitydata.org/rules.html
- GTFS Guru tarafında bu kontrolün canlı doğrulanmış birebir eşleşmesi tespit edilmedi.

### GTFS spec referansı
- GTFS `timeframes.txt::service_id` → `calendar.txt`/`calendar_dates.txt` foreign key kuralı.
- 🔗 [GTFS Reference — timeframes.txt](https://gtfs.org/documentation/schedule/reference/#timeframestxt) · [calendar.txt](https://gtfs.org/documentation/schedule/reference/#calendartxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:2373](../../../crates/pipeline/src/k4_cross_ref.rs#L2373) (`check_fares_v2`)

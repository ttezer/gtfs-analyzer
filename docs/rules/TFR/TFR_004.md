# TFR_004 — end_time start_time'dan küçük

> **Karar:** `timeframes.txt`'te `end_time`, `start_time`'dan küçük veya ona eşit; zaman aralığı geçersiz. ORTA, Quality. **Düzeltme:** `end_time` değerini `start_time`'dan sonraya ayarlayın.
> ⚠️ Satır bazlı (Row); yalnızca her iki zaman da geçerli biçimde parse edildiğinde değerlendirilir.

| Alan | Değer |
|---|---|
| Grup | TFR — Timeframes (Fares v2) |
| Önem | Orta |
| Sınıf | Quality |
| Otorite kaynağı | PROJECT_QUALITY |
| Aşama | K2 |
| Varlık | Row |
| Kimlik alanı | `timeframe_group_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::timeframes::validate_timeframes()` içinde:
```
start_time ve end_time geçerli
VE end_secs <= start_secs
→ TFR_004 (satır bazlı notice)
```
- Kapsam birimi: satır (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Zamanlardan biri parse edilemezse (TFR_003) bu kontrol atlanır.
- **Yanlış pozitif:** Yok; eşit/ters aralık geçersizdir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TFR_004** | end_time ≤ start_time | sıralama | ORTA · Quality · K2 |
| **TFR_003** | start/end_time biçim hatası | biçim | YÜKSEK · Spec · K2 |
| **TFR_005** | Aynı grupta örtüşen aralıklar | örtüşme | ORTA · Spec · K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** end_time start_time'dan küçük
- **Çözüm önerisi:** `end_time` değerini `start_time`'dan sonraya ayarlayın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"end_time, start_time'dan büyük olmalıdır."*

## Ölçülen alan / değer
- **Dosya/alan:** `timeframes.txt` → `start_time`, `end_time`.
- **Gözlenen değer:** `end_time ≤ start_time`.
- **Beklenen değer:** `end_time > start_time`.

## Kod değiştirirken minimum test matrisi
- `start=10:00:00`, `end=09:00:00` → TFR_004 çıkmalı.
- `start == end` → TFR_004 çıkmalı.
- `end > start` → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 2.0** (ORTA).
- **R1 blocker değil:** Quality sınıfı R1 üretmez (R1 yalnız `GtfsSpec` + Spec + Kritik).
- **R5 genel skor:** Interop sınıf penaltısına girer (ağırlık 0.3).

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-07 doğrulandı:** MobilityData'da "timeframe end ≤ start" için birebir parity YOK (`timeframe_overlap` örtüşmeyi kapsar, end<start'ı değil); resmi GTFS Spec'te de açık ordering normu yok → proje-özel **Quality** (kalite) kuralı.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS `timeframes.txt` — `end_time` `start_time`'dan büyük olmalıdır.
- 🔗 [GTFS Reference — timeframes.txt](https://gtfs.org/documentation/schedule/reference/#timeframestxt)

### Kod referansı
- Emit: [k2/timeframes.rs:75](../../../crates/pipeline/src/k2/timeframes.rs#L75) (`validate_timeframes`)

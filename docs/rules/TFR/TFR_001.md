# TFR_001 — timeframe_group_id eksik

> **Karar:** `timeframes.txt::timeframe_group_id` boş; zaman dilimi grubu kimliksiz. KRİTİK, Spec. **Düzeltme:** Her zaman dilimi kaydı için bir `timeframe_group_id` girin.
> ⚠️ Satır bazlı (Row); GTFS-Fares v2 timeframes dosyasıdır. Yalnızca alanın boşluğunu kontrol eder.

| Alan | Değer |
|---|---|
| Grup | TFR — Timeframes (Fares v2) |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Row |
| Kimlik alanı | `timeframe_group_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::timeframes::validate_timeframes()` içinde:
```
timeframe_group_id boş
→ TFR_001 (satır bazlı notice)
```
- Kapsam birimi: satır (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Yok; boş değer kesin yakalanır.
- **Yanlış pozitif:** Yok.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TFR_001** | timeframe_group_id boş | zorunluluk | KRİTİK · Spec · K2 |
| **TFR_002** | service_id takvimde yok | FK | KRİTİK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** timeframe_group_id eksik
- **Çözüm önerisi:** Her zaman dilimi kaydı için bir `timeframe_group_id` girin.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"timeframe_group_id zorunludur."*

## Ölçülen alan / değer
- **Dosya/alan:** `timeframes.txt` → `timeframe_group_id`.
- **Gözlenen değer:** Boş.
- **Beklenen değer:** Dolu `timeframe_group_id`.

## Kod değiştirirken minimum test matrisi
- Boş `timeframe_group_id` → TFR_001 çıkmalı.
- Dolu değer → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** Spec + KRİTİK → yayın engelleyici.
- **R5 genel skor:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `missing_required_field` (`timeframes.txt::timeframe_group_id`). GTFS-Fares v2 kapsamı araçlar arasında değişebilir.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS `timeframes.txt::timeframe_group_id` — zorunlu alan.
- 🔗 [GTFS Reference — timeframes.txt](https://gtfs.org/documentation/schedule/reference/#timeframestxt)

### Kod referansı
- Emit: [k2/timeframes.rs:32](../../../crates/pipeline/src/k2/timeframes.rs#L32) (`validate_timeframes`)

# TFR_005 — Aynı grup ve service_id içinde örtüşen zaman aralıkları

> **Karar:** Aynı (`timeframe_group_id`, `service_id`) içindeki iki zaman aralığı örtüşüyor; bir ana hangi tarifenin geçerli olduğu belirsiz. ORTA, Spec. **Düzeltme:** Aynı grup ve service_id içindeki zaman aralıklarının örtüşmediğinden emin olun.
> ⚠️ Satır bazlı (Row); aralıklar (group, service) bazında gruplanıp başlangıca göre sıralanır, ardışık örtüşme aranır.
> 🔀 **SINIF DEĞİŞTİ (#96, 2026-08-09): Interop → Spec.** Sert hüküm `Pa586fe5b`'i ölçen TEK kural buydu ve kanıt sınıfı hükmün otoritesiyle uyuşmuyordu: Spec'e filtreleyen kullanıcı ihlali hiç görmüyordu. Spec metni: *"There must not be overlapping time intervals for the same timeframe_group_id and service_id values."* 
> ⚠️ **Yayın davranışı DEĞİŞMEDİ:** R1 kapısı `Spec ∧ Kritik` ve çift yönlüdür; bu kuralın önemi ORTA olduğu için hiçbir feed'i yayına engellemez. **R5 skorunda ağırlık Interop (%30)'den Spec'e (%40) geçer.**

| Alan | Değer |
|---|---|
| Grup | TFR — Timeframes (Fares v2) |
| Önem | Orta |
| Sınıf | Spec |
| Otorite kaynağı | GTFS_SPEC |
| Aşama | K2 |
| Varlık | Row |
| Kimlik alanı | `timeframe_group_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::timeframes::validate_timeframes()` içinde:
```
(timeframe_group_id, service_id) bazında aralıklar toplanır
başlangıca göre sıralanır
ardışık aralıkta cur_start < prev_end (örtüşme)
→ TFR_005 (satır bazlı, örtüşen satır referanslı)
```
- Kapsam birimi: satır (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Zamanı parse edilemeyen satırlar gruplamaya girmez.
- **Yanlış pozitif:** Bitiş = sonraki başlangıç (sınır teması) örtüşme sayılmaz (`<` kullanılır) — beklenen.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TFR_005** | Aynı grup/service'te örtüşen aralıklar | örtüşme | ORTA · Spec · K2 |
| **TFR_004** | end_time ≤ start_time | tek aralık sıralaması | ORTA · Spec · K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** Aynı grup ve service_id içinde örtüşen zaman aralıkları
- **Çözüm önerisi:** Aynı grup ve service_id içindeki zaman aralıklarının örtüşmediğinden emin olun.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"timeframe_group_id '{group}' service_id '{service}': {prev}–{prev_end}s ile {cur}s aralıkları örtüşüyor."*

## Ölçülen alan / değer
- **Dosya/alan:** `timeframes.txt` → `timeframe_group_id`, `service_id`, `start_time`/`end_time`.
- **Gözlenen değer:** Örtüşen satır çifti.
- **Beklenen değer:** Grup+service içinde ayrık aralıklar.

## Kod değiştirirken minimum test matrisi
- Aynı grup/service'te örtüşen iki aralık → TFR_005 çıkmalı.
- Ardışık ama temas eden (prev_end == cur_start) → notice yok.
- Farklı grup veya service → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 2.0** (ORTA).
- **R1 blocker değil:** Spec sınıfı ama önem ORTA; R1 yalnız `Spec ∧ Kritik` üretir.
- **R5 genel skor:** Spec sınıf penaltısına girer (ağırlık 0.4; #96'dan önce Interop/0.3 idi).

### Hangi raporlarda görünür
`report_views = VI = [R2, R5, R8, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-07 doğrulandı:** MobilityData'da **`timeframe_overlap` (ERROR)** birebir karşılığı VAR → **Interop** (MD-parite). Resmi GTFS Spec'te açık norm yok ama MD tüketici davranışıyla uyum sinyali.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS `timeframes.txt` — aynı grup/servis içinde zaman aralıkları örtüşmemelidir.
- 🔗 [GTFS Reference — timeframes.txt](https://gtfs.org/documentation/schedule/reference/#timeframestxt)

### Kod referansı
- Emit: [k2/timeframes.rs:159](../../../crates/pipeline/src/k2/timeframes.rs#L159) (`validate_timeframes`)

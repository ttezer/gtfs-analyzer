# FMD_001 — fare_media_id yineleniyor

> **Karar:** `fare_media.txt` içinde aynı `fare_media_id` birden fazla satırda kullanılmış. KRİTİK, spec hatası. **Düzeltme:** Her ödeme aracına benzersiz bir `fare_media_id` verin.
> ⚠️ Bu kural K3 entity map kurulurken çalışır; yinelenen ödeme aracı kimliği daha sonra `fare_products.txt.fare_media_id` referanslarını belirsizleştirir.

| Alan | Değer |
| --- | --- |
| Grup | FMD — Fare Media |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K3 |
| Varlık | Row |
| Kimlik alanı | `fare_media_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | `FPD_004` |

## Tetikleme koşulu (kesin mantık)
`build_fares_v2` içinde `fare_media_id` daha önce `map.fare_media_ids` içine alınmışsa aynı id'nin sonraki satırı için notice üretilir.

Kaynak: [`crates/pipeline/src/k3_entity_graph.rs`](../../../crates/pipeline/src/k3_entity_graph.rs)

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Boş `fare_media_id` bu K3 tekillik kontrolünde atlandığı için FMD_001 üretmez; boş kimlik ayrı zorunlu alan kontrollerine bırakılır.
- **Yanlış pozitif:** Aynı `fare_media_id` iki satırda bilinçli verilmiş olsa bile GTFS primary key açısından geçerli değildir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **FPD_004** | `fare_products.txt.fare_media_id` değerinin `fare_media.txt` içinde bulunmaması | Referans | Kritik · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** fare_media_id yineleniyor

### R2 mesajı/mesajları
- `'<fare_media_id>' ödeme aracı tekrarlanıyor (ilk görünüm: satır <satır>).`
- **Çözüm önerisi:** `Her ödeme aracına benzersiz bir fare_media_id atayın.`

## Ölçülen alan / değer
- **Dosya/alan:** `fare_media.txt` → `fare_media_id`
- **Gözlenen değer:** Tekrarlanan `fare_media_id`
- **Beklenen değer:** Her ödeme aracı için tekil `fare_media_id`

## Kod değiştirirken minimum test matrisi
- Aynı `fare_media_id` iki satırda verildiğinde FMD_001 üretmeli.
- Farklı `fare_media_id` değerlerinde FMD_001 üretmemeli.
- Boş `fare_media_id` bu kuralı üretmemeli.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1/R2/R5/R9 etkisi:** Spec sınıfı ve `VS_K` görünürlüğüyle kritik bulgu olarak raporlanır.

### Hangi raporlarda görünür
`report_views = VS_K` → R1, R2, R5, R9.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `fare_media_id` boşsa FMD_001 çalışmaz; boş kimlik ayrı zorunlu alan/root-cause kontrollerine bırakılır.

### Dış araç eşleşmesi
2026-06-03 doğrulandı: MobilityData kuralları sayfasında birebir FMD_001 adı görünmedi; GTFS Fares v2 dokümantasyonu `fare_media_id` alanını fare media primary key'i olarak tanımlar.

Kaynaklar: [MobilityData GTFS Validator Rules](https://gtfs-validator.mobilitydata.org/rules.html), [GTFS Fares Feature Guide](https://gtfs.org/getting-started/features/fares/)

### GTFS spec referansı
[GTFS Schedule Reference - fare_media.txt](https://gtfs.org/documentation/schedule/reference/#fare_mediatxt): `fare_media_id`, fare media kaydını tanımlayan unique ID alanıdır.

### Kod referansı
- Emit: [`crates/pipeline/src/k3_entity_graph.rs`](../../../crates/pipeline/src/k3_entity_graph.rs)

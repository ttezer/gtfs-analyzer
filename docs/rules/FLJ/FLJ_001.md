# FLJ_001 — from_network_id eksik veya bulunamadı

> **Karar:** `fare_leg_join_rules.txt.from_network_id` **Required**'dır ve `networks.txt` ya da `routes.network_id` içinde tanımlı olmalıdır. KRİTİK, Spec.
> 📌 `fare_leg_join_rules.txt` 2026-08-02'ye kadar pipeline'a **hiç tanıtılmamıştı** (issue #59): `KNOWN_FILES`'ta yoktu — yani dosyayı gönderen feed "bilinmeyen dosya" uyarısı alıyordu — parser'ı yoktu, kuralı yoktu. UI'ın dosya haritası ise düğümü zaten çiziyordu.

| Alan | Değer |
|---|---|
| Grup | FLJ — Fare Leg Join Rules |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Row |
| Kimlik alanı | `from_network_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
```text
K4: fare_leg_join_rules.txt satırı var
VE ( from_network_id boş                       → FLJ_001
   VEYA ağ kodu hiçbir kaynakta tanımlı DEĞİL  → FLJ_001 )
```

⚠️ Ağ alanlarının hedefi **birleşik** kümedir (`EntityMap::network_ids`) — `NET_002`'nin aksine
burada bu doğrudur, çünkü spec alanı *"referencing routes.network_id **or**
networks.network_id"* diye tanımlar. `FLG_002` ile aynı hedef.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Dosya hiç yoksa kural çalışmaz.
- **Yanlış pozitif:** Bilinmiyor; alan koşulsuz Required'dır.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **FLJ_001** | `from_network_id` eksik/çözülemiyor | ön-aktarma ağı | KRİTİK · Spec · K4 |
| FLJ_002 | `to_network_id` eksik/çözülemiyor | aktarma sonrası ağ | KRİTİK · Spec · K4 |
| FLJ_003 | `from_stop_id` eksik/çözülemiyor/durak değil | ön-aktarma durağı | KRİTİK · Spec · K4 |
| FLJ_004 | `to_stop_id` eksik/çözülemiyor/durak değil | aktarma sonrası durak | KRİTİK · Spec · K4 |
| **FLG_002** | `fare_leg_rules.network_id` çözülemiyor | ücret kuralı ağı | KRİTİK · Spec · K4 |
| **NET_002** | `route_networks.network_id` çözülemiyor | yalnız networks.txt hedefli | KRİTİK · Spec · K4 |
| **DQ_021** | dört alanlık bileşik anahtar yineleniyor | birincil anahtar | YÜKSEK · Spec · K6 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** from_network_id eksik veya bulunamadı
- **Çözüm önerisi:** `networks.txt` veya `routes.network_id` içinde tanımlı bir ağ kodu kullanın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"fare_leg_join_rules.txt satırında from_network_id zorunludur."*
- **Ayrıntılı bulgu mesajı:** *"'<network_id>' ağ kodu networks.txt/routes.txt içinde tanımlı değil."*

## Ölçülen alan / değer
- **Dosya/alan:** `fare_leg_join_rules.txt` → `from_network_id`
- **Gözlenen değer:** boş dize veya çözülemeyen ağ kodu
- **Beklenen değer:** tanımlı bir `network_id`

## Kod değiştirirken minimum test matrisi
- `from_network_id` boş → FLJ_001 çıkmalı.
- Tanımsız ağ kodu → FLJ_001 çıkmalı.
- `routes.network_id` ile tanımlı ağ → FLJ_001 çıkmamalı (birleşik küme).
- `to_network_id` bozuk → FLJ_001 çıkmamalı (o FLJ_002).

---
## Teknik ek

### Dış araç eşleşmesi
- **2026-08-02:** MobilityData tarafında yakın kategoriler `foreign_key_violation` ve
  `missing_required_field`'dır. Bu dosya için birebir doğrulanmış bir notice bulunamadı.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- `fare_leg_join_rules.txt` → `from_network_id`: **Foreign ID referencing `routes.network_id` or `networks.network_id`**, Presence **Required**.
- Dosya: *"File: Optional. Primary Key (from_network_id, to_network_id, from_stop_id, to_stop_id)."*
- 🔗 https://gtfs.org/documentation/schedule/reference/#fare_leg_join_rulestxt

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** `VS_K` görünümü R1'i içerir.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Kod referansı
- Emit: [k4_cross_ref.rs](../../../crates/pipeline/src/k4_cross_ref.rs)
- Parser: [fare_leg_join_rules.rs](../../../crates/pipeline/src/k2/fare_leg_join_rules.rs)
- Registry: [registry.rs](../../../crates/rules/src/registry.rs)

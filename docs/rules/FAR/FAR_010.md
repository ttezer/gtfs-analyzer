# FAR_010 — Çakışan ücret kuralları

> **Karar:** `fare_rules.txt` içinde aynı `(route_id, origin_id, destination_id, contains_id)` kombinasyonu birden fazla farklı `fare_id` için tanımlanmışsa FAR_010 üretilir. **Düzeltme:** Her `(route_id, origin_id, destination_id, contains_id)` kombinasyonu için yalnızca bir ücret tarifesi tanımlayın.
> ⚠️ Notice `fare_rules.txt` satırından gelir ama grup FAR'dır; çünkü çakışma farklı ücret tarifelerinin aynı koşula uygulanmasından kaynaklanır.

| Alan | Değer |
| --- | --- |
| Grup | FAR — Fare Attributes |
| Önem | Orta |
| Sınıf | Quality |
| Aşama | K4 |
| Varlık | Feed |
| Kimlik alanı | - |
| Skor tabanı | 2 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
K4 `fare_rules.txt` satırlarını `(route_id, origin_id, destination_id, contains_id)` anahtarıyla gruplayarak gezer. Aynı anahtar daha önce farklı bir `fare_id` ile görülmüşse FAR_010 notice'ı üretir.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Kuralın bağlı olduğu alan boş, parse edilemez veya önceki aşamada elenmişse bu notice çalışmayabilir.
- **Yanlış pozitif:** Kod koşulu sağlanıyorsa veri modeli açısından sorun kabul edilir; özel işletme yorumu varsa veri tarafında ayrıca modellenmelidir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **FAR_009** | ücretin hiç `fare_rules.txt` kaydı yoksa uyarır. | Komşu kontrol | - |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** Çakışan ücret kuralları

### R2 mesajı/mesajları
- `Ücret tarifesi '<fare_id>' için tanımlanan kural, '<önceki_fare_id>' ile çakışıyor — aynı koşul kümesine birden fazla tarife uygulanıyor.`
- **Çözüm önerisi:** Her `(route_id, origin_id, destination_id, contains_id)` kombinasyonu için yalnızca bir ücret tarifesi tanımlayın.

## Ölçülen alan / değer
- Ölçülen alanlar: `route_id`, `origin_id`, `destination_id`, `contains_id`, `fare_id`.

## Kod değiştirirken minimum test matrisi
- Temel pozitif örnek: koşul sağlandığında notice üretilmeli.
- Temel negatif örnek: geçerli satırda notice üretilmemeli.
- Sınır değer: boş, geçersiz veya en küçük/geçerli değer ayrı denenmeli.
- İlgili komşu kural ile karışmama testi yapılmalı.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 2.0** (Orta).
- **Sınıf/Aşama:** Quality · K4.

### Hangi raporlarda görünür
`report_views = VS` → R2, R5, R9.

### Bağımlılık (bunu maskeleyen root cause'lar)
- Belirgin `blocks` ilişkisi yok; önce aynı satırdaki zorunlu kimlik ve parse hataları çözülmelidir.

### Dış araç eşleşmesi
2026-06-03 doğrulandı: MobilityData kuralları sayfasında birebir FAR_010 adı görünmedi; GTFS Fares v1 rehberleri `fare_rules.txt` koşullarının ücret uygulama alanını belirlediğini açıklar.

### GTFS spec referansı
[GTFS Schedule Reference - fare_rules.txt](https://gtfs.org/documentation/schedule/reference/#fare_rulestxt): `fare_rules.txt`, ücret sınıfının rota/origin/destination/zone koşullarıyla nasıl eşleştiğini tanımlar.

### Kod referansı
- Emit: [kaynak](../../../crates/pipeline/src/k4_cross_ref.rs)

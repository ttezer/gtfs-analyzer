# FAR_002 — price eksik veya geçersiz

> **Karar:** Mevcut K2 kodunda FAR_002, `price` alanı sayısal okunamadığında veya negatif olduğunda üretilir. Registry başlığı “eksik veya geçersiz” dese de boş `price` bu fonksiyonda FAR_002 üretmez. **Düzeltme:** `price` alanını sıfır veya pozitif sayısal değere ayarlayın.
> ⚠️ Kod davranışı başlıktan daha dar: boş `price` için bu kural değil, varsa genel zorunlu alan/kolon kontrolleri beklenmelidir.

| Alan | Değer |
| --- | --- |
| Grup | FAR — Fare Attributes |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `fare_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`parse_f64(row, "price")` hata döndürürse veya parse edilen değer `0` altındaysa notice eklenir.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Kuralın bağlı olduğu alan boş, parse edilemez veya önceki aşamada elenmişse bu notice çalışmayabilir.
- **Yanlış pozitif:** Kod koşulu sağlanıyorsa veri modeli açısından sorun kabul edilir; özel işletme yorumu varsa veri tarafında ayrıca modellenmelidir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **FAR_003** | para birimi kodu eksik/geçersizse düşer. | Komşu kontrol | - |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** price eksik veya geçersiz

### R2 mesajı/mesajları
- `price negatif olamaz.`
- **Çözüm önerisi:** `price` alanını sıfır veya pozitif sayısal değere ayarlayın.

## Ölçülen alan / değer
- Ölçülen alan: `price`.

## Kod değiştirirken minimum test matrisi
- Temel pozitif örnek: koşul sağlandığında notice üretilmeli.
- Temel negatif örnek: geçerli satırda notice üretilmemeli.
- Sınır değer: boş, geçersiz veya en küçük/geçerli değer ayrı denenmeli.
- İlgili komşu kural ile karışmama testi yapılmalı.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (Kritik). R1 yayın engelleyici.
- **Sınıf/Aşama:** Spec · K2.

### Hangi raporlarda görünür
`report_views = VS_K` → R1, R2, R5, R9.

### Bağımlılık (bunu maskeleyen root cause'lar)
- Belirgin `blocks` ilişkisi yok; önce aynı satırdaki zorunlu kimlik ve parse hataları çözülmelidir.

### Dış araç eşleşmesi
2026-06-03 doğrulandı: MobilityData kuralları sayfasında birebir FAR_002 adı görünmedi; Google Transit fare rehberi `fare_attributes.txt` içinde `price`, `currency_type` ve `payment_method` alanlarının ücret tanımının çekirdeği olduğunu belirtir.

### GTFS spec referansı
[GTFS Schedule Reference - fare_attributes.txt](https://gtfs.org/documentation/schedule/reference/#fare_attributestxt): `price`, `currency_type` biriminde non-negative float ve zorunlu alandır.

### Kod referansı
- Emit: [kaynak](../../../crates/pipeline/src/k2/fare_attributes.rs)

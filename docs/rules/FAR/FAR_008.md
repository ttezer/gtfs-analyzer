# FAR_008 — agency_id bulunamadı

> **Karar:** `fare_attributes.txt` içinde `agency_id` dolu olduğu halde `agency.txt` içinde karşılığı yoksa FAR_008 üretilir. **Düzeltme:** `agency.txt` içinde tanımlı geçerli bir `agency_id` kullanın.
> ⚠️ Bu kural eksik `agency_id` değerini yakalamaz; çoklu agency durumunda eksik `agency_id` mevcut kodda `FIN_013` olarak raporlanır.

| Alan | Değer |
| --- | --- |
| Grup | FAR — Fare Attributes |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Entity |
| Kimlik alanı | `fare_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
K4 `check_fare_attributes` içinde `agency_id` doluysa `map.agencies` içinde aranır. Bulunamazsa FAR_008 notice'ı üretilir.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Kuralın bağlı olduğu alan boş, parse edilemez veya önceki aşamada elenmişse bu notice çalışmayabilir.
- **Yanlış pozitif:** Kod koşulu sağlanıyorsa veri modeli açısından sorun kabul edilir; özel işletme yorumu varsa veri tarafında ayrıca modellenmelidir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **AGN_002** | `agency_id` tekilliğini kontrol eder. | Komşu kontrol | - |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** agency_id bulunamadı

### R2 mesajı/mesajları
- `'<agency_id>' işletici kodu agency.txt'te tanımlı değil.`
- **Çözüm önerisi:** `agency.txt` içinde tanımlı geçerli bir `agency_id` kullanın.

## Ölçülen alan / değer
- Ölçülen alan: `agency_id`.

## Kod değiştirirken minimum test matrisi
- Temel pozitif örnek: koşul sağlandığında notice üretilmeli.
- Temel negatif örnek: geçerli satırda notice üretilmemeli.
- Sınır değer: boş, geçersiz veya en küçük/geçerli değer ayrı denenmeli.
- İlgili komşu kural ile karışmama testi yapılmalı.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (Kritik).
- **Sınıf/Aşama:** Spec · K4.

### Hangi raporlarda görünür
`report_views = VS_K` → R1, R2, R5, R9.

### Bağımlılık (bunu maskeleyen root cause'lar)
- Belirgin `blocks` ilişkisi yok; önce aynı satırdaki zorunlu kimlik ve parse hataları çözülmelidir.

### Dış araç eşleşmesi
2026-06-03 doğrulandı: MobilityData kuralları sayfasında birebir FAR_008 adı görünmedi; GTFS consumer rehberleri `agency_id` alanını çoklu işletmeci ücret bağlamı için kullanır.

### GTFS spec referansı
[GTFS Schedule Reference - fare_attributes.txt](https://gtfs.org/documentation/schedule/reference/#fare_attributestxt): `agency_id`, `agency.agency_id` değerine foreign ID olarak referans verir ve birden fazla agency olduğunda koşullu zorunludur.

### Kod referansı
- Emit: [kaynak](../../../crates/pipeline/src/k4_cross_ref.rs)

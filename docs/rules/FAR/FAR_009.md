# FAR_009 — Ücrete ait hat kuralı yok

> **Karar:** `fare_attributes.txt` içinde tanımlı bir `fare_id` için `fare_rules.txt` içinde hiç kayıt yoksa FAR_009 üretilir. **Düzeltme:** `fare_rules.txt` içine bu `fare_id` için en az bir kural ekleyin.
> ⚠️ Bu kural kalite seviyesindedir; ücret tanımı var ama hangi hat/zone/koşul kümesine uygulanacağı belirsizdir.

| Alan | Değer |
| --- | --- |
| Grup | FAR — Fare Attributes |
| Önem | Düşük |
| Sınıf | Quality |
| Aşama | K4 |
| Varlık | Entity |
| Kimlik alanı | `fare_id` |
| Skor tabanı | 3 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
K4 önce `fare_rules.txt` içindeki `fare_id` kümesini çıkarır. `fare_attributes.txt` içindeki dolu bir `fare_id` bu kümede yoksa FAR_009 üretir.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Kuralın bağlı olduğu alan boş, parse edilemez veya önceki aşamada elenmişse bu notice çalışmayabilir.
- **Yanlış pozitif:** Kod koşulu sağlanıyorsa veri modeli açısından sorun kabul edilir; özel işletme yorumu varsa veri tarafında ayrıca modellenmelidir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **FAR_001** | yinelenen `fare_id`, bu ilişkiyi bozabilir. | Komşu kontrol | - |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** Ücrete ait hat kuralı yok

### R2 mesajı/mesajları
- `Ücret tarifesi '<fare_id>' için fare_rules.txt'te hiç kural tanımlanmamış — hangi hatlara uygulanacağı belli değil.`
- **Çözüm önerisi:** `fare_rules.txt` içine bu `fare_id` için en az bir kural ekleyin.

## Ölçülen alan / değer
- `fare_rules.txt` tamamen boşsa her dolu `fare_id` için FAR_009 üretilebilir.

## Kod değiştirirken minimum test matrisi
- Temel pozitif örnek: koşul sağlandığında notice üretilmeli.
- Temel negatif örnek: geçerli satırda notice üretilmemeli.
- Sınır değer: boş, geçersiz veya en küçük/geçerli değer ayrı denenmeli.
- İlgili komşu kural ile karışmama testi yapılmalı.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 1.0** (Düşük).
- **Sınıf/Aşama:** Quality · K4.

### Hangi raporlarda görünür
`report_views = VS` → R2, R5, R9.

### Bağımlılık (bunu maskeleyen root cause'lar)
- Belirgin `blocks` ilişkisi yok; önce aynı satırdaki zorunlu kimlik ve parse hataları çözülmelidir.

### Dış araç eşleşmesi
2026-06-03 doğrulandı: MobilityData kuralları sayfasında birebir FAR_009 adı görünmedi; GTFS Fares v1 rehberleri `fare_attributes.txt` ve `fare_rules.txt` ikilisinin birlikte ücret uygulama modelini oluşturduğunu belirtir.

### GTFS spec referansı
[GTFS Schedule Reference - fare_rules.txt](https://gtfs.org/documentation/schedule/reference/#fare_rulestxt): `fare_rules.txt`, `fare_attributes.txt` içindeki ücretlerin bir güzergaha/zone koşuluna nasıl uygulanacağını belirtir.

### Kod referansı
- Emit: [kaynak](../../../crates/pipeline/src/k4_cross_ref.rs)

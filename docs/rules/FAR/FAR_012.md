# FAR_012 — fare_id eksik

> **Karar:** `fare_attributes.txt` satırında `fare_id` boş. KRİTİK. Sütun başlıkta hiç yoksa ARC_025 devralır.

| Alan | Değer |
|---|---|
| Grup | FAR — Fare Attributes |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `fare_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::fare_attributes` içinde `fare_id` sütunu başlıkta VAR ve değer boş → FAR_012. Sütun yoksa ARC_025.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **FAR_012** | `fare_id` boş | alan zorunluluğu | KRİTİK · Spec · k2 |
| **ARC_025** | `fare_id` sütunu yok | sütun varlığı | KRİTİK · Spec · k1 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** fare_id eksik
- **Çözüm önerisi:** Her ücret tanımına benzersiz bir `fare_id` verin.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"fare_id zorunludur."*

---
## Teknik ek
### Dış araç eşleşmesi
- **Eşleşme:** `missing_required_field` · **Tür:** MD-parite · **Bilinçli:** evet.
### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
### Hangi raporlarda görünür
`VS_K = [R1, R2, R5, R9]`.
### Kod referansı
- [`registry.rs`](../../../crates/rules/src/registry.rs)
- [`fare_attributes.rs`](../../../crates/pipeline/src/k2/fare_attributes.rs)

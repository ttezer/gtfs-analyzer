# FAR_001 — fare_id yineleniyor

> **Karar:** `fare_attributes.txt` içinde aynı `fare_id` birden fazla satırda kullanılmışsa FAR_001 üretilir. `fare_id`, dosyanın birincil anahtarıdır ve tekil olmalıdır. **Düzeltme:** Her tarife kaydına benzersiz bir `fare_id` atayın.
> ⚠️ Bu kural K3 entity map kurulurken çalışır; ilk görülen kayıt haritaya alınır, sonraki aynı `fare_id` satırları hata olarak işaretlenir.

| Alan | Değer |
| --- | --- |
| Grup | FAR — Fare Attributes |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K3 |
| Varlık | Entity |
| Kimlik alanı | `fare_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | `FRL_001`, `FAR_009` |

## Tetikleme koşulu (kesin mantık)
`build_fare_attrs` aynı `fare_id` değerinin daha önce map'e eklendiğini görürse notice üretir.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Kuralın bağlı olduğu alan boş, parse edilemez veya önceki aşamada elenmişse bu notice çalışmayabilir.
- **Yanlış pozitif:** Kod koşulu sağlanıyorsa veri modeli açısından sorun kabul edilir; özel işletme yorumu varsa veri tarafında ayrıca modellenmelidir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **FRL_001** | `fare_rules.txt` içindeki `fare_id` referansını kontrol eder. | Komşu kontrol | - |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** fare_id yineleniyor

### R2 mesajı/mesajları
- `'<fare_id>' ücret tarifesi tekrarlanıyor (ilk görünüm: satır <satır>).`
- **Çözüm önerisi:** Her tarife kaydına benzersiz bir `fare_id` atayın.

## Ölçülen alan / değer
- Boş `fare_id` bu K3 kontrolünde atlanır; boş zorunlu alanlar K1/K2 tarafındaki genel kontrollerle ele alınır.

## Kod değiştirirken minimum test matrisi
- Temel pozitif örnek: koşul sağlandığında notice üretilmeli.
- Temel negatif örnek: geçerli satırda notice üretilmemeli.
- Sınır değer: boş, geçersiz veya en küçük/geçerli değer ayrı denenmeli.
- İlgili komşu kural ile karışmama testi yapılmalı.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (Kritik).
- **Sınıf/Aşama:** Spec · K3.

### Hangi raporlarda görünür
`report_views = VS_K` → R1, R2, R5, R9.

### Bağımlılık (bunu maskeleyen root cause'lar)
- Bu kuralın bloke ettiği kurallar: FRL_001, FAR_009.

### Dış araç eşleşmesi
2026-06-03 doğrulandı: MobilityData validator kuralları sayfasında bu proje ID'siyle birebir FAR_001 karşılığı görünmedi; eşleşme GTFS spec birincil anahtar kuralı üzerinden yapılır.

### GTFS spec referansı
[GTFS Schedule Reference - fare_attributes.txt](https://gtfs.org/documentation/schedule/reference/#fare_attributestxt): `fare_id`, `fare_attributes.txt` için birincil anahtardır.

### Kod referansı
- Emit: [kaynak](../../../crates/pipeline/src/k3_entity_graph.rs)

# FAR_011 — payment_method eksik

> **Karar:** `fare_attributes.txt` satırında `payment_method` boş. KRİTİK, yayın engelleyici. **Düzeltme:** `payment_method` = 0 (araçta ödeme) veya 1 (önceden ödeme) girin.
> ⚠️ Yalnızca alanın boş olmasını kontrol eder; sütun başlıkta hiç yoksa ARC_025 devralır. Geçersiz enum değeri FAR_004'ün konusudur.

| Alan | Değer |
|---|---|
| Grup | FAR — Fare Attributes |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `payment_method` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::fare_attributes::validate_fare_attributes()` içinde: `payment_method` sütunu başlıkta VAR ve değer boş → FAR_011. Sütun hiç yoksa ARC_025.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış pozitif:** Yok; `fare_attributes.payment_method` GTFS için zorunludur.
- **Yanlış negatif:** Geçersiz (0/1 dışı) değer FAR_004'te (ayrı) yakalanır.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **FAR_011** | `payment_method` boş | alan zorunluluğu | KRİTİK · Spec · k2 |
| **FAR_004** | `payment_method` geçersiz enum | format | — · — · k2 |
| **ARC_025** | `payment_method` sütunu başlıkta yok | sütun varlığı | KRİTİK · Spec · k1 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** payment_method eksik
- **Çözüm önerisi:** `payment_method` değerini 0 (peşin) veya 1 (önceden) olarak girin.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"payment_method zorunludur."*

## Ölçülen alan / değer
- **Dosya/alan:** `fare_attributes.txt` → `payment_method`
- **Gözlenen değer:** boş string. **Beklenen:** 0 veya 1.

## Kod değiştirirken minimum test matrisi
- `payment_method=""` (sütun var) → FAR_011 çıkmalı.
- `payment_method="0"` → FAR_011 çıkmamalı.
- `payment_method` sütunu yok → ARC_025 beklenmeli.

---
## Teknik ek
### Dış araç eşleşmesi
- **Eşleşme:** `missing_required_field` · **Tür:** MD-parite · **Bilinçli:** evet — zorunlu alan boşluğu (sütun-varlığı ARC_025).
### GTFS spec referansı
- GTFS Schedule Reference: `fare_attributes.payment_method` Required. 🔗 https://gtfs.org/documentation/schedule/reference/
### Skora katkı
- **severity.weight() = 4.0** (KRİTİK). Yayın engelleyici.
### Hangi raporlarda görünür
`VS_K = [R1, R2, R5, R9]`.
### Kod referansı
- [`registry.rs`](../../../crates/rules/src/registry.rs)
- [`fare_attributes.rs`](../../../crates/pipeline/src/k2/fare_attributes.rs)

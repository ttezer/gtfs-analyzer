# FAR_006 — transfer_duration geçersiz

> **Karar:** `transfer_duration` alanı dolu olduğu halde pozitif `u32` olarak okunamıyorsa veya değeri `0` ise FAR_006 üretilir. **Düzeltme:** `transfer_duration` değerini pozitif tam sayıya ayarlayın veya boş bırakın.
> ⚠️ GTFS spec `transfer_duration` için non-negative integer der; mevcut kod ise `0` değerini de geçersiz sayarak `> 0` bekler. Bu kart repo davranışını esas alır.

| Alan | Değer |
| --- | --- |
| Grup | FAR — Fare Attributes |
| Önem | Orta |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `fare_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`parse_u32(row, "transfer_duration")` hata döndürürse veya parse edilen değer `0` ise notice eklenir.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Kuralın bağlı olduğu alan boş, parse edilemez veya önceki aşamada elenmişse bu notice çalışmayabilir.
- **Yanlış pozitif:** Kod koşulu sağlanıyorsa veri modeli açısından sorun kabul edilir; özel işletme yorumu varsa veri tarafında ayrıca modellenmelidir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **FAR_005** | aktarma sayısı enum değerini kontrol eder. | Komşu kontrol | - |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** transfer_duration geçersiz

### R2 mesajı/mesajları
- `transfer_duration pozitif olmalıdır.`
- **Çözüm önerisi:** `transfer_duration` değerini pozitif tam sayıya ayarlayın veya boş bırakın.

## Ölçülen alan / değer
- Ölçülen alan: `transfer_duration`.

## Kod değiştirirken minimum test matrisi
- Temel pozitif örnek: koşul sağlandığında notice üretilmeli.
- Temel negatif örnek: geçerli satırda notice üretilmemeli.
- Sınır değer: boş, geçersiz veya en küçük/geçerli değer ayrı denenmeli.
- İlgili komşu kural ile karışmama testi yapılmalı.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 2.0** (Orta).
- **Sınıf/Aşama:** Spec · K2.

### Hangi raporlarda görünür
`report_views = VS` → R2, R5, R9.

### Bağımlılık (bunu maskeleyen root cause'lar)
- Belirgin `blocks` ilişkisi yok; önce aynı satırdaki zorunlu kimlik ve parse hataları çözülmelidir.

### Dış araç eşleşmesi
2026-06-03 doğrulandı: MobilityData kuralları sayfasında birebir FAR_006 adı görünmedi; Google Transit fare rehberi `transfer_duration` alanını aktarma geçerliliği için kullanır.

### GTFS spec referansı
[GTFS Schedule Reference - fare_attributes.txt](https://gtfs.org/documentation/schedule/reference/#fare_attributestxt): `transfer_duration`, saniye cinsinden non-negative integer ve opsiyonel alandır.

### Kod referansı
- Emit: [kaynak](../../../crates/pipeline/src/k2/fare_attributes.rs)

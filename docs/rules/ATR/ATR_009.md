# ATR_009 — attribution hedef alanları birlikte kullanılmış

> **Karar:** K4 kodu ATR_009'u `agency_id`, `route_id`, `trip_id` alanlarından birden fazlası aynı satırda doluyken üretir; bir attribution kaydı tek bir hedefe (agency VEYA route VEYA trip) uygulanmalıdır. YÜKSEK, Quality.
> ⚠️ Tarihsel not: eski registry başlığı `attribution_phone geçersiz` idi (2026-07-11 düzeltildi); `attribution_phone` alanı K2'de parse edilmiyor, ATR_009 telefon kuralı DEĞİL.

| Alan | Değer |
|---|---|
| Grup | ATR — Attributions |
| Önem | Yüksek |
| Sınıf | Quality |
| Otorite kaynağı | PROJECT_QUALITY |
| Aşama | K4 |
| Varlık | Entity |
| Kimlik alanı | `attribution_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
K4 `agency_id`, `route_id`, `trip_id` alanlarından kaçının dolu olduğunu sayar. Dolu alan sayısı `> 1` ise ATR_009 üretilir.

```text
filled([agency_id, route_id, trip_id]) > 1
→ ATR_009
```

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Telefon alanı gerçekten geçersiz olsa bile mevcut kod ATR_009 üretmez.
- **Yanlış pozitif:** Kullanıcı attribution'ı hem agency hem route seviyesine bağlamak isteyebilir; GTFS attribution hedefi bu üç alandan en fazla birini bekler.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **ATR_009** | `agency_id/route_id/trip_id` birlikte kullanılmış | hedef kapsamı | YÜKSEK · Quality · K4 |
| **ATR_010** | `agency_id` agency.txt'te yok | agency referansı | DÜŞÜK · Spec · K4 |
| **ATR_011/012** | `route_id`/`trip_id` bulunamadı | route/trip referansı | DÜŞÜK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** attribution hedef alanları (agency/route/trip) birlikte kullanılmış
- **Çözüm önerisi:** `agency_id`, `route_id`, `trip_id` alanlarından yalnız birini doldurun (attribution tek bir hedefe uygulanmalı).

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"agency_id, route_id ve trip_id aynı satırda birlikte kullanılamaz."*
- **Ayrıntılı çözüm:** *"Bu alanlardan yalnızca birini doldurun."*

## Ölçülen alan / değer
- **Dosya/alan:** `attributions.txt` → `agency_id|route_id|trip_id`
- **Gözlenen değer:** birden fazla hedef alan dolu
- **Beklenen değer:** en fazla biri dolu

## Kod değiştirirken minimum test matrisi
- yalnız `agency_id` dolu → ATR_009 çıkmamalı.
- `agency_id` ve `route_id` dolu → ATR_009 çıkmalı.
- `route_id` ve `trip_id` dolu → ATR_009 çıkmalı.
- geçersiz telefon değeri → mevcut kodda ATR_009 beklenmemeli.

---
## Teknik ek

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData tarafında yakın kategori attribution hedef alanları için conditional/mutually-exclusive alan doğrulamasıdır; registry başlığındaki telefon semantiğiyle birebir eşleşme yoktur.
- **2026-07-11 sınıf adjudication:** Quality korundu. attributions.txt'in "agency_id/route_id/trip_id'den en fazla biri tanımlı olmalı" açık normatif hükmü doğrulanamadı (gtfs.org reference + raw markdown attributions bölümü, dosya-sırasında sonda olduğu için WebFetch/arama ile çekilemiyor — 4 kaynak denendi) ve birebir MD paritesi yok → Spec/Interop kanıtsız, proje kalite kuralı. Tam reference metni bulunursa açık hüküm varsa Spec'e yükseltilebilir (bkz. docs/audits/class-authority-postphase5.md, Bulgu B).
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- **Doğrudan normatif karşılık yok:** Mevcut ATR_009 davranışı telefon formatını değil attribution hedef alanlarının birlikte kullanımını denetler; bu proje-özel kalite kuralıdır (2026-07 otorite denetimi).
- 🔗 https://gtfs.org/documentation/schedule/reference/#attributionstxt

### Skora katkı
- **severity.weight() = 3.0** (YÜKSEK).
- **R1 blocker değil:** Quality sınıfı R1 üretmez (R1 yalnız `GtfsSpec` + Spec + Kritik).
- **R5 genel skor:** Quality sınıf penaltısına girer (ağırlık 0.2).

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Kod referansı
- Emit: [k4_cross_ref.rs](../../../crates/pipeline/src/k4_cross_ref.rs)
- Registry: [registry.rs](../../../crates/rules/src/registry.rs)

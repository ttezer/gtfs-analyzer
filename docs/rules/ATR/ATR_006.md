# ATR_006 — is_authority geçersiz

> **Karar:** `attributions.txt.is_authority` dolduğunda değeri `0` veya `1` olmalıdır. KRİTİK, Spec.
> 📌 2026-08-01'e kadar bu ID K4'te `route_id` referans hatası için de üretiliyordu; o yol artık [ATR_011](ATR_011.md). Kural artık tek olgu ölçer.

| Alan | Değer |
|---|---|
| Grup | ATR — Attributions |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `attribution_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
```text
K2: is_authority alanı dolu
VE değer 0/1 olarak parse edilemiyor
→ ATR_006
```

`parse_role_enum` üç rol alanının üçünde de aynı biçimde çalışır: `is_producer`→ATR_004,
`is_operator`→ATR_005, `is_authority`→ATR_006.

### ✅ Gerçek feed mutasyonuyla ölçüldü (2026-08-15, issue #131)
`mdb-2931` üzerinde `is_authority = 9` → `ATR_006` emit etti (`real_feed_mutation.rs`). Yan etki yok.
Korpustaki sessizlik doğal: bu hatayı taşıyan feed yayımlanmıyor. Kardeş kontrolü dosyanın
okunduğunu zaten kanıtlıyordu (`ATR_001` 8 feed · `ATR_009` 89 bulgu).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Boş `is_authority` geçersiz enum sayılmaz; hiçbir rolün 1 olmaması ATR_003 konusudur.
- **Yanlış pozitif:** Bilinen yok; spec değer kümesini `0` ve `1` ile sınırlar.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **ATR_006** | `is_authority` 0/1 dışında | rol enumu | KRİTİK · Spec · K2 |
| **ATR_004** | `is_producer` 0/1 dışında | rol enumu | KRİTİK · Spec · K2 |
| **ATR_005** | `is_operator` 0/1 dışında | rol enumu | KRİTİK · Spec · K2 |
| **ATR_003** | hiçbir rol 1 değil | rol eksikliği | YÜKSEK · Spec · K2 |
| **ATR_011** | attribution `route_id` routes.txt'te yok | route referansı | DÜŞÜK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** is_authority geçersiz
- **Çözüm önerisi:** `is_authority` değerini `0` veya `1` yapın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"is_authority alanı 0 veya 1 olmalıdır."*
- **Ayrıntılı bulgu mesajı:** *"<parse_u32 hata mesajı>"*
- **Ayrıntılı çözüm:** *"Her attribution rol alanını 0 veya 1 olarak ayarlayın."*

## Ölçülen alan / değer
- **Dosya/alan:** `attributions.txt` → `is_authority`
- **Gözlenen değer:** geçersiz rol değeri
- **Beklenen değer:** `0` veya `1`

## Kod değiştirirken minimum test matrisi
- `is_authority=2` → ATR_006 çıkmalı.
- `is_authority=1` → ATR_006 çıkmamalı.
- `is_authority` boş → ATR_006 çıkmamalı.
- `route_id` bozuk → ATR_006 **çıkmamalı** (artık ATR_011).

---
## Teknik ek

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData tarafında yakın kategori boolean/enum doğrulamasıdır.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- `attributions.txt` → `is_authority`: *"Enum. 0 or empty - Organization doesn't have this role. 1 - Organization does have this role."*
- 🔗 https://gtfs.org/documentation/schedule/reference/#attributionstxt

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** `VS_K` görünümü R1'i içerir.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Kod referansı
- Emit: [attributions.rs](../../../crates/pipeline/src/k2/attributions.rs)
- Registry: [registry.rs](../../../crates/rules/src/registry.rs)

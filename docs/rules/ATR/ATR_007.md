# ATR_007 — attribution_url geçersiz

> **Karar:** `attributions.txt.attribution_url` dolduğunda geçerli bir `http/https` URL olmalıdır. KRİTİK, Spec.
> 📌 2026-08-01'e kadar bu ID K4'te `trip_id` referans hatası için de üretiliyordu; o yol artık [ATR_012](ATR_012.md). Kural artık tek olgu ölçer.

| Alan | Değer |
|---|---|
| Grup | ATR — Attributions |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `attribution_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
```text
K2: attribution_url dolu
VE looks_like_url(url) false
→ ATR_007
```


> ℹ️ **`looks_like_url` 2026-08-03'te daraltıldı.** Spec `URL` tipini *"a fully qualified URL
> that includes http:// or https://"* diye tanımlar; kontrol daha önce yalnız
> `Url::parse(v).is_ok()` idi ve `mailto:`, `ftp://`, `file:///`, `javascript:` gibi her
> şemayı geçiriyordu. Artık **`http://` veya `https://` şeması zorunlu** (şema adı
> büyük/küçük harfe duyarsız). Korpus etkisi ölçüldü: 1 feed, 116 satır; hiçbir feed'in
> yayın kararı değişmedi.

### ✅ Gerçek feed mutasyonuyla ölçüldü (2026-08-15, issue #131)
`mdb-2931` üzerinde geçersiz `attribution_url` → `ATR_007` emit etti (`real_feed_mutation.rs`). ⚠️ Satır DOLULUK ORANIYLA seçildi: 18 satırın yalnız 8'inde `attribution_url` dolu, boş hücreye mutasyon hiçbir şey sınamazdı. 🚪 Çürütüldü: `looks_like_url` kontrolü kapatılınca test kırmızı oluyor.
Korpustaki sessizlik doğal: bu hatayı taşıyan feed yayımlanmıyor. Kardeş kontrolü dosyanın
okunduğunu zaten kanıtlıyordu (`ATR_001` 8 feed · `ATR_009` 89 bulgu).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Boş `attribution_url` hata değildir (alan Optional).
- **Yanlış pozitif:** URL biçimsel olarak doğrulanır; gerçekten açılıp açılmadığı kontrol edilmez — ağ isteği yapılmaz.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **ATR_007** | `attribution_url` URL biçiminde değil | URL biçimi | KRİTİK · Spec · K2 |
| **ATR_008** | `attribution_email` e-posta biçiminde değil | iletişim alanı | DÜŞÜK · Spec · K2 |
| **ATR_012** | attribution `trip_id` trips.txt'te yok | trip referansı | DÜŞÜK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** attribution_url geçersiz
- **Çözüm önerisi:** `attribution_url` için geçerli bir `http/https` URL kullanın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"attribution_url geçerli bir URL değil."*
- **Ayrıntılı çözüm:** *"attribution_url için geçerli bir http/https URL'si kullanın."*

## Ölçülen alan / değer
- **Dosya/alan:** `attributions.txt` → `attribution_url`
- **Gözlenen değer:** geçersiz URL metni
- **Beklenen değer:** `http://` veya `https://` ile başlayan geçerli URL

## Kod değiştirirken minimum test matrisi
- `attribution_url=https://example.com` → ATR_007 çıkmamalı.
- `attribution_url=example` → ATR_007 çıkmalı.
- `attribution_url` boş → ATR_007 çıkmamalı.
- `trip_id` bozuk → ATR_007 **çıkmamalı** (artık ATR_012).

---
## Teknik ek

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData tarafında yakın kategori `invalid_url` notice'ıdır.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- `attributions.txt` → `attribution_url`: Field Type **URL**, Presence **Optional**.
- 🔗 https://gtfs.org/documentation/schedule/reference/#attributionstxt

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** `VS_K` görünümü R1'i içerir.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Kod referansı
- Emit: [attributions.rs](../../../crates/pipeline/src/k2/attributions.rs)
- Registry: [registry.rs](../../../crates/rules/src/registry.rs)

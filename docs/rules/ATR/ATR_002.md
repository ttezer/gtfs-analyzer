# ATR_002 — organization_name eksik

> **Karar:** Attribution kaydında `organization_name` boş olamaz. KRİTİK, Spec. **Düzeltme:** Atıf yapılacak kuruluş adını doldurun.
> ⚠️ Bu kural `attribution_id` olsa da olmasa da çalışır; kimlik yoksa notice entity id boş kalabilir.

| Alan | Değer |
|---|---|
| Grup | ATR — Attributions |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `attribution_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
K2 `organization_name` alanını trim eder. Sonuç boşsa ATR_002 üretilir.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Kuruluş adı dolu ama anlamsız/kısaltma ise bu kural yakalamaz.
- **Yanlış pozitif:** Attribution satırı bilinçli placeholder ise bile GTFS atıf kaydı için kuruluş adı gereklidir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **ATR_002** | `organization_name` boş | kuruluş adı | KRİTİK · Spec · K2 |
| **ATR_001** | `attribution_id` boş | attribution kimliği | YÜKSEK · Spec · K2 |
| **DQ_020** | önerilen alan eksik/boş | genel kalite | Quality · K6 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** organization_name eksik
- **Çözüm önerisi:** `organization_name` alanını atıf yapılacak kuruluş adıyla doldurun.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"organization_name zorunludur."*
- **Ayrıntılı çözüm:** *"organization_name alanını doldurun."*

## Ölçülen alan / değer
- **Dosya/alan:** `attributions.txt` → `organization_name`
- **Gözlenen değer:** boş
- **Beklenen değer:** kuruluş adı

## Kod değiştirirken minimum test matrisi
- `organization_name` dolu → ATR_002 çıkmamalı.
- `organization_name` boş → ATR_002 çıkmalı.
- `attribution_id` boş, `organization_name` boş → ATR_001 ve ATR_002 çıkmalı.

---
## Teknik ek

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData tarafında yakın kategori `missing_required_field` notice'ıdır.
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- `attributions.txt` → `organization_name`.
- 🔗 https://gtfs.org/documentation/schedule/reference/#attributionstxt

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** `VS_K` görünümü R1'i içerir.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Kod referansı
- Emit: [attributions.rs](../../../crates/pipeline/src/k2/attributions.rs)
- Registry: [registry.rs](../../../crates/rules/src/registry.rs)

# CAL_010 — Serviste aktif gün sayısı çok az

> **Karar:** Bir servisin türetilmiş aktif gün sayısı `service_gap_days` eşiğine eşit veya daha az. ORTA, operasyonel analiz uyarısı. **Düzeltme:** Servis takvimini genişletin veya kısa servis olduğunu belgeleyin.
> ⚠️ Bu kontrol aktif tarih bitmap'ine bakar; ham `calendar.txt` aralığı uzun olsa bile aktif gün sayısı azsa kural çıkabilir.
> 📅 **Feed penceresine göre ölçek:** Mutlak gün eşiği yalnız feed **≥30 günlük** bir dönem yayınlıyorsa uygulanır. Daha kısa yayınlarda eşik ORANA döner: `aktif gün / yayın penceresi < %20`. 17 günlük bir yayında 4-6 gün çalışan bir tren (Vangölü Ekspresi deseni) seyrek değil NORMALdir; mutlak 7-gün eşiği orada neredeyse her servisi işaretliyordu. Pencere, feed'deki TÜM servislerin min–max aktif tarih aralığıdır.

| Alan | Değer |
|---|---|
| Grup | CAL — Calendar |
| Önem | Orta |
| Sınıf | Analytics |
| Aşama | K6 |
| Varlık | Entity |
| Kimlik alanı | `service_id` |
| Skor tabanı | 2 |
| Görünürlük | VA (`R2`, `R3`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k6_analytics::check_calendar_analytics()` içinde:
```
active_dates boş değil
VE active_dates.len() <= service_gap_days
→ CAL_010
```
- Eşik config tarafındaki `service_gap_days` değeridir.
- Boş active_dates seti atlanır; bu kural "aktif günü yok" kontrolü değildir.

## Değişiklik notu (2026-07-23)
Eski OPR_018 (servis dönemi < 3 aktif gün) bu kuralla birleştirildi/kaldırıldı. OPR_018 `dates.len() < 3` (yani ≤ 2) ile CAL_010'un saf alt kümesiydi: CAL_010 eşiği `service_gap_days` config min **3** olduğundan, 1-2 aktif günlü her servis zaten CAL_010'u tetikliyordu. Aynı severity/sınıf/skor/kimlik-alanı/mesaj → duplikat. İkisinin de MD paritesi doğrulanmamıştı, parite riski yok.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış pozitif:** Özel etkinlik, tatil servisi veya kısa kampanya servisi bilinçli olarak az gün çalışabilir.
- **Yanlış negatif:** Aktif tarih bitmap'i eksik/hatalı üretilirse gerçek kısa servis yanlış değerlendirilebilir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **CAL_010** | Aktif gün sayısı az | toplam aktif gün | ORTA · Analytics · k6 |
| **CAL_007** | Aktif günler arasında boşluk | aralık boşluğu | ORTA · Analytics · k6 |
| **TRP_026** | Seferin aktif tarihi yok | trip-service kullanımı | ORTA · Analytics · k6 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** Serviste aktif gün sayısı çok az
- **Çözüm önerisi:** Servis takvimini genişletin ya da bu servisin kısa olduğunu belgeleyin.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"'{service_id}' takviminde yalnızca {total_days} aktif gün var."*

## Ölçülen alan / değer
- **Dosya/alan:** `calendar.txt` + `calendar_dates.txt` etkisi → türetilmiş aktif tarih seti.
- **Gözlenen değer:** `{total_days} gün`.
- **Beklenen değer:** `> {service_gap_days} gün`.

## Kod değiştirirken minimum test matrisi
- Aktif gün sayısı `service_gap_days` → CAL_010 çıkmalı.
- Aktif gün sayısı `service_gap_days + 1` → CAL_010 çıkmamalı.
- Active_dates boş → CAL_010 çıkmamalı.
- Kısa ama aralıksız servis → CAL_010 çıkmalı, CAL_007 çıkmamalı.
- ≥30 günlük pencerede 3 aktif günlü servis → çıkmalı (mutlak yol).
- 17 günlük pencerede 5 aktif gün (%29) → notice YOK; 2 aktif gün (%12) → çıkmalı (oran yolu).

---
## Teknik ek

### Dış araç eşleşmesi
- Henüz tarih damgalı, doğrulanmış dış araç eşleşmesi eklenmedi.

### GTFS spec referansı
- Henüz doğrudan spec maddesi eşleşmesi eklenmedi.

### Skora katkı
- **severity.weight() = 2.0** (ORTA).
- **Yayın engelleyici değil:** Analytics sınıfı R1 blocker değildir.
- **R5 genel skor:** Analytics sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VA = [R2, R3, R5, R9]` → R2, R3, R5, R9.

### Kod referansı
- Emit: [k6_analytics.rs:1824](../../../crates/pipeline/src/k6_analytics.rs#L1824) (`check_calendar_analytics`) — notice [k6_analytics.rs:1824](../../../crates/pipeline/src/k6_analytics.rs#L1824)
- Test: [k6_analytics.rs:10155](../../../crates/pipeline/src/k6_analytics.rs#L10155)


# CAL_006 — Haftalık bazda tüm günler pasif

> **Karar:** `calendar.txt` satırındaki tüm tanımlı hafta günleri `0`; servis haftalık şablona göre hiç çalışmıyor. YÜKSEK, kalite sorunu. **Düzeltme:** En az bir gün alanını `1` yapın veya servisi kaldırın.
> ⚠️ Bu kural `calendar_dates.txt` ile eklenmiş aktif günleri dikkate almaz; bu yüzden override kullanılan feed'lerde dikkatli yorumlanmalıdır.

| Alan | Değer |
|---|---|
| Grup | CAL — Calendar |
| Önem | Yüksek |
| Sınıf | Quality |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `service_id` |
| Skor tabanı | 2 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::calendar::validate_calendar()` içinde:
```
tüm parse edilen gün alanları 0
VE gün alanlarından en az biri tanımlı
→ CAL_006
```
- `all_zero` başlangıçta true gelir; herhangi bir gün `1` ise false olur.
- `days.iter().any(|d| d.is_some())` şartı, tamamen boş gün alanlarında CAL_006 üretmemek için vardır.
- Geçersiz gün alanları CAL_002 üretir; CAL_006 yine de parse edilebilen gün değerlerine göre değerlendirilebilir.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış pozitif:** Servis haftalık olarak pasif tutulup `calendar_dates.txt` ile tekil günlerde aktif ediliyorsa CAL_006 çıkar; kart bunu "override mümkün" diye işaretler.
- **Yanlış negatif:** Tüm gün alanları boşsa CAL_006 çıkmaz.
- **Yanlış negatif:** Haftalık günlerden biri `1`, ama pratikte bütün aktif günler `calendar_dates` ile kaldırılmışsa CAL_006 bunu yakalamaz.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **CAL_002** | Gün alanı 0/1 dışında | alan değeri | KRİTİK · Spec · k2 |
| **CAL_006** | Haftalık günlerin tamamı 0 | weekly pattern | YÜKSEK · Quality · k2 |
| **CAL_018** | Tüm günler pasif ve calendar_dates aktif override yok | gerçek aktiflik | DÜŞÜK · Quality · k4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** Haftalık bazda tüm günler pasif (calendar_dates ile override mümkün)
- **Çözüm önerisi:** En az bir gün alanını `1` yapın veya bu servisi kaldırın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"'{service_id}' takviminde tüm günler 0 — bu takvim hiç çalışmıyor."*

## Ölçülen alan / değer
- **Dosya/alan:** `calendar.txt` → `monday`-`sunday`
- **Gözlenen değer:** notice'ta spesifik observed value gönderilmiyor.
- **Beklenen değer:** notice'ta spesifik expected value gönderilmiyor; kavramsal beklenti en az bir aktif gün veya bilinçli override.

## Kod değiştirirken minimum test matrisi
- Tüm günler `0` → CAL_006 çıkmalı.
- En az bir gün `1` → CAL_006 çıkmamalı.
- Tüm gün alanları boş → mevcut davranışta CAL_006 çıkmamalı.
- Tüm günler `0`, `calendar_dates` aktif gün ekliyor → mevcut davranışta CAL_006 çıkar; bu bilinçli sınırlamadır.

---
## Teknik ek

### Dış araç eşleşmesi
- Henüz tarih damgalı, doğrulanmış dış araç eşleşmesi eklenmedi.

### GTFS spec referansı
- Henüz doğrudan spec maddesi eşleşmesi eklenmedi.

### Skora katkı
- **severity.weight() = 3.0** (YÜKSEK).
- **Yayın engelleyici değil:** YÜKSEK + Quality, R1 blocker değildir.
- **R5 kalite skoru:** Quality sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]` → R2, R5, R9.

### Bağımlılık (bunu maskeleyen root cause'lar)
CAL_006 kendi registry satırında başka kuralları maskelemez. Ancak CAL_002 varsa gün alanlarının anlamı güvenilmez hale gelir.

### Kod referansı
- Emit: [calendar.rs:83](../../../crates/pipeline/src/k2/calendar.rs#L83) (`validate_calendar`) — notice [calendar.rs:83](../../../crates/pipeline/src/k2/calendar.rs#L83)
- Test: [calendar.rs:220](../../../crates/pipeline/src/k2/calendar.rs#L220) (`all_zero_days_produces_cal_006`)


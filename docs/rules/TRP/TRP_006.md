# TRP_006 — wheelchair_accessible geçersiz

> **Karar:** `trips.txt::wheelchair_accessible` 0, 1 veya 2 dışında bir değer içeriyor. DÜŞÜK, Spec. **Düzeltme:** Değeri 0 (bilgi yok), 1 (erişilebilir) veya 2 (erişilemez) olarak ayarlayın.
> ⚠️ Yalnızca dolu ve >2 sayısal değerler tetikler; parse edilemeyen değerler sessizce `None` sayılır (notice üretmez).

| Alan | Değer |
|---|---|
| Grup | TRP — Trips |
| Önem | Düşük |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `trip_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::trips::validate()` içinde:
```
wheelchair_accessible parse → değer > 2
→ TRP_006 (trip bazlı notice)
```
- Geçerli değerler: 0, 1, 2.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Sayıya çevrilemeyen değer `None` olarak ele alınır ve notice üretmez (yalnızca >2 sayılar yakalanır).
- **Yanlış pozitif:** Yok.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRP_006** | wheelchair_accessible enum dışı değer | enum değeri | DÜŞÜK · Spec · K2 |
| **TRP_028** | Bazı seferlerde wheelchair bilgisi eksik | kapsam (feed) | ORTA · Quality · K6 |
| **TRP_029** | Hiçbir seferde wheelchair bilgisi yok | kapsam (feed) | BİLGİ · Quality · K6 |
| **TRP_025** | Eksik wheelchair oranı >%80 | kapsam oranı | BİLGİ · Quality · K6 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** wheelchair_accessible geçersiz
- **Çözüm önerisi:** `wheelchair_accessible` değerini 0 (bilgi yok), 1 (erişilebilir) veya 2 (erişilemez) olarak ayarlayın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"wheelchair_accessible 0, 1 veya 2 olmalıdır."*

## Ölçülen alan / değer
- **Dosya/alan:** `trips.txt` → `wheelchair_accessible`.
- **Gözlenen değer:** `>2` sayısal değer.
- **Beklenen değer:** `0, 1 veya 2`.

## Kod değiştirirken minimum test matrisi
- `wheelchair_accessible=3` → TRP_006 çıkmalı.
- `wheelchair_accessible=0/1/2` → notice yok.
- Boş değer → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 1.0** (DÜŞÜK).
- **R1 blocker değil:** Spec + DÜŞÜK.
- **R5 genel skor:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `unexpected_enum_value` (`trips.txt::wheelchair_accessible`).
  - https://gtfs-validator.mobilitydata.org/rules.html
- GTFS Guru tarafında bu kontrolün canlı doğrulanmış birebir eşleşmesi tespit edilmedi.

### GTFS spec referansı
- GTFS `trips.txt::wheelchair_accessible` — geçerli değerler 0, 1, 2.
- 🔗 [GTFS Reference — trips.txt](https://gtfs.org/documentation/schedule/reference/#tripstxt)

### Kod referansı
- Emit: [k2/trips.rs:283](../../../crates/pipeline/src/k2/trips.rs#L283)
- Test: [k2/trips.rs:596](../../../crates/pipeline/src/k2/trips.rs#L596)

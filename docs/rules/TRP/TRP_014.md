# TRP_014 — trip_short_name çok uzun

> **Karar:** `trips.txt::trip_short_name` 20 karakterden uzun. BİLGİ, Quality — yolcu arayüzünde taşma riski. **Düzeltme:** `trip_short_name`'i kısaltın.
> ⚠️ Eşik proje-özeldir (>20 karakter); GTFS spec'te sabit bir uzunluk sınırı yoktur.

| Alan | Değer |
|---|---|
| Grup | TRP — Trips |
| Önem | Bilgi |
| Sınıf | Quality |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `trip_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::trips::validate()` içinde:
```
trip_short_name dolu VE uzunluk > 20 karakter
→ TRP_014 (trip bazlı notice)
```
- Eşik: **>20 karakter**.

> ℹ️ Eşik **karakter** sayar (`chars().count()`), bayt değil — çok baytlı alfabelerde
> (Türkçe, Japonca) `str::len()` eşiği erken tetikliyordu (2026-07-29'da düzeltildi).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `trip_short_name` boşsa atlanır.
- **Yanlış pozitif:** Bazı sistemler uzun kısa adları tolere edebilir; bilgi notudur.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRP_014** | trip_short_name >20 karakter | uzunluk | BİLGİ · Quality · K2 |
| **TRP_011** | trip_headsign ve kısa ad ikisi de yok | eksiklik | YÜKSEK · Quality · K6 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** trip_short_name çok uzun
- **Çözüm önerisi:** `trip_short_name`'i kısaltın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"trip_short_name {n} karakter; 20'yi aşmamalıdır."*

## Ölçülen alan / değer
- **Dosya/alan:** `trips.txt` → `trip_short_name`.
- **Gözlenen değer:** Karakter sayısı (>20).
- **Beklenen değer:** `≤20` karakter.

## Kod değiştirirken minimum test matrisi
- 21 karakterlik `trip_short_name` → TRP_014 çıkmalı.
- ≤20 karakter → notice yok.
- Boş `trip_short_name` → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 0.0** (BİLGİ).
- **R1 blocker değil:** Quality + BİLGİ.
- **R5 genel skor:** Quality sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData ve GTFS Guru tarafında `trip_short_name` uzunluk sınırı için doğrulanmış birebir notice karşılığı tespit edilmedi; proje-özel Quality kuralıdır (>20 karakter eşiği).
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS `trips.txt::trip_short_name` — kısa, yolcuya yönelik tanımlayıcı (sabit uzunluk sınırı yoktur).
- 🔗 [GTFS Reference — trips.txt](https://gtfs.org/documentation/schedule/reference/#tripstxt)

### Kod referansı
- Emit: [k2/trips.rs:248](../../../crates/pipeline/src/k2/trips.rs#L248)

# TRP_024 — Block içinde tutarsız rota tipi

> **Karar:** Aynı `block_id`'deki seferler farklı `route_type` değerlerine ait; tek araç bloğu içinde tür değişimi olağandışı. DÜŞÜK, Quality. **Düzeltme:** Aynı block içindeki tüm seferlerin aynı rota tipine sahip olmasını sağlayın.
> ⚠️ İlk görülen route_type referans alınır; bloktaki sonraki farklı türler tetikler.

| Alan | Değer |
|---|---|
| Grup | TRP — Trips |
| Önem | Düşük |
| Sınıf | Quality |
| Otorite kaynağı | PROJECT_QUALITY |
| Aşama | K6 |
| Varlık | Entity |
| Kimlik alanı | `trip_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k6_analytics::check_operational_analytics()` içinde:
```
block_id → ilk görülen (route_type, trip_id) saklanır
aynı blokta route_type farklı bir sefer görülürse
→ TRP_024 (trip bazlı notice)
```
- Kapsam birimi: trip (entity).

## Yanlış pozitif / negatif (asıl tasarım riski)
- 🔴 **YANLIŞ NEGATİF — KİMLİK BOŞLUĞU (#169, 2026-08-19).** Kural `route_id`'yi `routes.txt`'ten HAM okur ve `trips.txt`'in ham değeriyle karşılaştırır. `routes.txt` sabit genişliğe doldurulmuşsa (`"10T0001C1  "`) ve `trips.txt` doldurulmamışsa (`"10T0001C1"`) arama ıskalar, kural **hiçbir şey bulmaz ve hiçbir şey söylemez.** `mdb-2653`'te 854 hattın 838'i böyle ve 1.502 gerçek tutarsızlık kaçtı.
  - **Bu bir hata değil, KARARDIR.** GTFS kimlikleri ham bayt olarak karşılaştırılır (#85; her varlık ayrıştırıcısı böyle okur) ve spec suçu üreticiye verir: *"Extra spaces between fields or field names should be removed."* Kırpmak, sessizce başka bir feed'in iki farklı kimliğini birleştirme riski taşır.
  - **Sessizliği artık BEYAN EDİLİR:** anahtar sütununda boşluk varsa `DQ_016` `identity_whitespace_fields` ve `cross_file_analysis: unreliable` yazar. Bu kural o dosyada susuyorsa sebebi orada görünür.
  - ⚠️ **Bu kurala özel değildir.** `k6_analytics.rs`'te 116 çağrı yeri haritasını ham kimlik alanından kurar; hepsi aynı şekilde davranır. Birini kırpıp diğerlerini bırakmak, `recoverable_stop_coord`'un 13 çağrı yerini atlaması hatasının tekrarı olur.

- **Yanlış negatif:** route_type'ı tanımsız hatlara ait seferler değerlendirilmez.
- **Yanlış pozitif:** Çok modlu (ör. otobüs+vapur aktarmalı) bloklar nadiren meşru olabilir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRP_024** | Block içinde farklı route_type | rota tipi | DÜŞÜK · Quality · K6 |
| **TRP_022** | Block içinde çakışan saatler | zaman | YÜKSEK · Spec · K6 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** Block içinde tutarsız rota tipi
- **Çözüm önerisi:** Aynı block içindeki tüm seferlerin aynı rota tipine sahip olmasını sağlayın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"block_id '{block_id}' içinde farklı rota tipleri: '{trip_a}' tip-{t1} ve '{trip_b}' tip-{t2}."*

## Ölçülen alan / değer
- **Dosya/alan:** `trips.txt` → `block_id`; `routes.txt` → `route_type`.
- **Gözlenen değer:** `route_type={t}` (referanstan farklı).
- **Beklenen değer:** Blok genelinde tek `route_type`.

## Kod değiştirirken minimum test matrisi
- Aynı blokta farklı route_type'lı seferler → TRP_024 çıkmalı.
- Aynı blokta aynı route_type → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 1.0** (DÜŞÜK).
- **R1/R8:** Quality sınıfı R1 veya R8 üretmez.
- **R5 genel skor:** Quality sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-07-10 doğrulandı:** MobilityData'da "block içinde farklı route_type" için birebir notice karşılığı YOKTUR; TRP_024 proje-özel kalite kuralıdır (block homojenliği sezgisi).
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS `trips.txt::block_id` — aynı araca atanan seferleri gruplar; tutarlı modalite beklenir.
- 🔗 [GTFS Reference — trips.txt](https://gtfs.org/documentation/schedule/reference/#tripstxt)

### Kod referansı
- Emit: [k6_analytics.rs:3357](../../../crates/pipeline/src/k6_analytics.rs#L3357) (`check_operational_analytics`)

# TRP_005 — direction_id geçersiz

> **Karar:** `trips.txt::direction_id` 0 veya 1 dışında bir değer (ya da sayı olmayan) içeriyor. ORTA, Spec. **Düzeltme:** `direction_id` değerini 0 (gidiş) veya 1 (dönüş) olarak ayarlayın.
> ⚠️ Yalnızca dolu değerler kontrol edilir; >1 sayısal değer veya parse edilemeyen değer tetikler.

| Alan | Değer |
|---|---|
| Grup | TRP — Trips |
| Önem | Orta |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `trip_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::trips::validate()` içinde:
```
direction_id parse → değer > 1 VEYA sayıya çevrilemiyor
→ TRP_005 (trip bazlı notice)
```
- Geçerli değerler: yalnızca 0 ve 1.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `direction_id` boşsa atlanır (eksiklik TRP_012 kapsamında).
- **Yanlış pozitif:** Yok; enum dışı değer kesin Spec ihlalidir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRP_005** | direction_id 0/1 dışında | enum değeri | ORTA · Spec · K2 |
| **TRP_012** | Çift yönlü rotada direction_id eksik | eksiklik | DÜŞÜK · Quality · K6 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** direction_id geçersiz
- **Çözüm önerisi:** `direction_id` değerini 0 (gidiş) veya 1 (dönüş) olarak ayarlayın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"direction_id {değer} geçersiz; 0 veya 1 olmalıdır."*

## Ölçülen alan / değer
- **Dosya/alan:** `trips.txt` → `direction_id`.
- **Gözlenen değer:** Geçersiz değer (ör. `2`, `abc`).
- **Beklenen değer:** `0 veya 1`.

## Kod değiştirirken minimum test matrisi
- `direction_id=2` → TRP_005 çıkmalı.
- `direction_id=abc` → TRP_005 çıkmalı.
- `direction_id=0/1` → notice yok.
- `direction_id` boş → TRP_005 çıkmamalı.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 2.0** (ORTA).
- **R1 blocker değil:** Spec + ORTA.
- **R5 genel skor:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `unexpected_enum_value` (`trips.txt::direction_id`).
  - https://gtfs-validator.mobilitydata.org/rules.html
- GTFS Guru tarafında bu kontrolün canlı doğrulanmış birebir eşleşmesi tespit edilmedi.

### GTFS spec referansı
- GTFS `trips.txt::direction_id` — geçerli değerler 0 ve 1.
- 🔗 [GTFS Reference — trips.txt](https://gtfs.org/documentation/schedule/reference/#tripstxt)

### Kod referansı
- Emit: [k2/trips.rs:250](../../../crates/pipeline/src/k2/trips.rs#L250)
- Test: [k2/trips.rs:525](../../../crates/pipeline/src/k2/trips.rs#L525)

# TRP_002 — route_id bulunamadı

> **Karar:** Bir seferin `route_id`'si `routes.txt`'te tanımlı değil. KRİTİK, Spec — sefer bir hatta bağlanamaz. **Düzeltme:** Geçerli bir `route_id` kullanın.
> ⚠️ Satır bazlı (per-trip) FK kontrolü; yalnızca dolu `route_id` değerleri kontrol edilir.

| Alan | Değer |
|---|---|
| Grup | TRP — Trips |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Entity |
| Kimlik alanı | `trip_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_trips()` içinde:
```
trips.route_id boş değil
VE map.routes route_id'yi İÇERMİYOR
→ TRP_002 (trip bazlı notice)
```
- Kapsam birimi: trip (entity); scope_key = `trip_id`.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `route_id` boşsa atlanır (zorunluluk başka kuralda).
- **Yanlış pozitif:** Yok; routes.txt mevcut ama hat eksikse doğru tetiklenir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRP_002** | Seferin route_id'si routes.txt'te yok | FK (route) | KRİTİK · Spec · K4 |
| **TRP_003** | Seferin service_id'si takvimde yok | FK (service) | KRİTİK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** route_id bulunamadı
- **Çözüm önerisi:** Geçerli bir `route_id` kullanın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"'{route_id}' hattı routes.txt'te tanımlı değil."*

## Ölçülen alan / değer
- **Dosya/alan:** `trips.txt` → `route_id`; karşılaştırma `routes.txt` → `route_id`.
- **Gözlenen değer:** Tanımsız `route_id`.
- **Beklenen değer:** `routes.txt`'te kayıtlı `route_id`.

## Kod değiştirirken minimum test matrisi
- routes.txt'te olmayan `route_id` → TRP_002 çıkmalı.
- `route_id` boş → TRP_002 çıkmamalı.
- Geçerli `route_id` → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** Spec + KRİTİK → yayın engelleyici.
- **R5 genel skor:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `foreign_key_violation` (child=`trips.txt::route_id` → parent=`routes.txt::route_id`).
  - https://gtfs-validator.mobilitydata.org/rules.html
- GTFS Guru tarafında bu kontrolün canlı doğrulanmış birebir eşleşmesi tespit edilmedi.

### GTFS spec referansı
- GTFS `trips.txt::route_id` → `routes.txt::route_id` foreign key kuralı.
- 🔗 [GTFS Reference — trips.txt](https://gtfs.org/documentation/schedule/reference/#tripstxt) · [routes.txt](https://gtfs.org/documentation/schedule/reference/#routestxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:1103](../../../crates/pipeline/src/k4_cross_ref.rs#L1103) (`check_trips`)
- Test: [k4_cross_ref.rs:4947](../../../crates/pipeline/src/k4_cross_ref.rs#L4947)

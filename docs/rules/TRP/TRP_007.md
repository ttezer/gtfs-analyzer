# TRP_007 — bikes_allowed geçersiz

<!-- md-parity: code=unexpected_enum_value -->

> **Karar:** `trips.txt::bikes_allowed` 0, 1 veya 2 dışında bir değer içeriyor. DÜŞÜK, Spec. **Düzeltme:** Değeri 0 (bilgi yok), 1 (izinli) veya 2 (izinsiz) olarak ayarlayın.
> ⚠️ Yalnızca dolu ve >2 sayısal değerler tetikler; parse edilemeyen değerler sessizce `None` sayılır.

| Alan | Değer |
|---|---|
| Grup | TRP — Trips |
| Önem | Düşük |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `trip_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::trips::validate()` içinde:
```
bikes_allowed parse → değer > 2
→ TRP_007 (trip bazlı notice)
```
- Geçerli değerler: 0, 1, 2.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Sayıya çevrilemeyen değer `None` sayılır, notice üretmez; eksik değer ise TRP_021 kapsamındadır.
- **Yanlış pozitif:** Yok.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRP_007** | bikes_allowed enum dışı değer | enum değeri | DÜŞÜK · Spec · K2 |
| **TRP_021** | bikes_allowed belirtilmemiş (eksik) | kapsam | BİLGİ · Quality · K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** bikes_allowed geçersiz
- **Çözüm önerisi:** `bikes_allowed` değerini 0 (bilgi yok), 1 (izinli) veya 2 (izinsiz) olarak ayarlayın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"bikes_allowed 0, 1 veya 2 olmalıdır."*

## Ölçülen alan / değer
- **Dosya/alan:** `trips.txt` → `bikes_allowed`.
- **Gözlenen değer:** `>2` sayısal değer.
- **Beklenen değer:** `0, 1 veya 2`.

## Kod değiştirirken minimum test matrisi
- `bikes_allowed=3` → TRP_007 çıkmalı.
- `bikes_allowed=0/1/2` → notice yok.
- Boş değer → TRP_007 çıkmamalı (TRP_021 değerlendirilir).

---
## Teknik ek

### Skora katkı
- **severity.weight() = 1.0** (DÜŞÜK).
- **R1 blocker değil:** Spec + DÜŞÜK.
- **R5 genel skor:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `unexpected_enum_value` (`trips.txt::bikes_allowed`).
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS `trips.txt::bikes_allowed` — geçerli değerler 0, 1, 2.
- 🔗 [GTFS Reference — trips.txt](https://gtfs.org/documentation/schedule/reference/#tripstxt)

### Kod referansı
- Emit: [k2/trips.rs:337](../../../crates/pipeline/src/k2/trips.rs#L337)

# TRP_017 — Frekans tabanlı sefer stop_times'ta eksik

> **Karar:** `frequencies.txt`'te geçen bir `trip_id`'nin `stop_times.txt`'te durağı tanımlanmamış; frekans şablonu uygulanabileceği bir durak dizisi yok. ORTA, Spec. **Düzeltme:** `stop_times.txt`'e bu sefer için durak saatlerini ekleyin.
> ⚠️ `check_frequencies` içinde, frekans satırının `trip_id`'si `trips_in_stm` setinde yoksa tetiklenir; FRQ_001 (trip_id FK) ile aynı döngüde değerlendirilir.

| Alan | Değer |
|---|---|
| Grup | TRP — Trips |
| Önem | Orta |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Entity |
| Kimlik alanı | `trip_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_frequencies()` içinde:
```
frequencies.trip_id trips_in_stm (stop_times'lı trip seti) içinde DEĞİL
→ TRP_017 (frequencies bazlı, trip kimlikli notice)
```
- Kapsam birimi: trip (entity).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Frekans satırının `trip_id`'si trips.txt'te de yoksa FRQ_001 (FK) öne çıkar.
- **Yanlış pozitif:** Yok; frekans tabanlı sefer için stop_times zorunludur.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRP_017** | Frekans trip'inin stop_times'ı yok | stop_times yokluğu | ORTA · Spec · K4 |
| **FRQ_001** | frequencies→trip FK yok | FK | — · Spec · K4 |
| **XFL_002** | Herhangi trip'in stop_times'ı yok | stop_times yokluğu (genel) | YÜKSEK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** Frekans tabanlı sefer stop_times'ta eksik
- **Çözüm önerisi:** `stop_times.txt`'e bu sefer için durak saatlerini ekleyin.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"'{trip_id}' frekans tabanlı seferin stop_times.txt'te durağı tanımlanmamış."*

## Ölçülen alan / değer
- **Dosya/alan:** `frequencies.txt` → `trip_id`; karşılaştırma `stop_times.txt`'teki trip seti.
- **Gözlenen değer:** stop_times'sız frekans `trip_id`'si.
- **Beklenen değer:** Frekans seferinin stop_times durak dizisi.

## Kod değiştirirken minimum test matrisi
- frequencies trip_id'si stop_times'ta yok → TRP_017 çıkmalı.
- frequencies trip_id'si stop_times'ta var → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 2.0** (ORTA).
- **R1 blocker değil:** Spec + ORTA.
- **R5 kalite skoru:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData ve GTFS Guru tarafında "frekans seferinin stop_times'ı yok" için doğrulanmış birebir notice karşılığı tespit edilmedi; proje-özel Spec kuralıdır (MobilityData `unusable_trip` ile dolaylı ilişkili).
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS `frequencies.txt` — referans verilen sefer `stop_times.txt`'te tanımlı durak dizisine sahip olmalıdır.
- 🔗 [GTFS Reference — frequencies.txt](https://gtfs.org/documentation/schedule/reference/#frequenciestxt) · [stop_times.txt](https://gtfs.org/documentation/schedule/reference/#stop_timestxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:1188](../../../crates/pipeline/src/k4_cross_ref.rs#L1188) (`check_frequencies`)

# TRP_017 — Frekans tabanlı sefer stop_times'ta eksik

> **Karar:** `frequencies.txt`'te geçen bir `trip_id`'nin `stop_times.txt`'te durağı tanımlanmamış; frekans şablonu uygulanabileceği bir durak dizisi yok. ORTA, Quality. **Düzeltme:** `stop_times.txt`'e bu sefer için durak saatlerini ekleyin.
> `check_frequencies` içinde yalnız `trip_id` trips.txt'te mevcut olduğu halde `trips_in_stm` setinde yoksa tetiklenir. trips.txt'te olmayan kimlik FRQ_001 kök FK hatasıdır ve TRP_017 üretilmez.

| Alan | Değer |
|---|---|
| Grup | TRP — Trips |
| Önem | Orta |
| Sınıf | Quality |
| Otorite kaynağı | PROJECT_QUALITY |
| Aşama | K4 |
| Varlık | Entity |
| Kimlik alanı | `trip_id` |
| Skor tabanı | 1 |
| Görünürlük | VS (`R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_frequencies()` içinde:
```
frequencies.trip_id trips.txt içinde VAR
VE frequencies.trip_id trips_in_stm (stop_times'lı trip seti) içinde DEĞİL
→ TRP_017 (frequencies bazlı, trip kimlikli notice)
```
- Kapsam birimi: trip (entity).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** Bilinen trip stop_times'ta yoksa TRP_017 yine üretilir.
- **Yanlış pozitif:** trips.txt'te olmayan kimlik için ayrıca TRP_017 üretmek FRQ_001'in türeviydi; full-catalog audit sonrası bu yol bloke edildi.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **TRP_017** | Frekans trip'inin stop_times'ı yok | stop_times yokluğu | ORTA · Quality · K4 |
| **FRQ_001** | frequencies→trip FK yok | FK | — · Spec · K4 |
| **XFL_002** | Herhangi trip'in stop_times'ı yok | stop_times yokluğu (genel) | YÜKSEK · Interop · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** Frekans tabanlı sefer stop_times'ta eksik
- **Çözüm önerisi:** `stop_times.txt`'e bu sefer için durak saatlerini ekleyin.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"'{trip_id}' frekans tabanlı seferin stop_times.txt'te durağı tanımlanmamış."*

## Ölçülen alan / değer
- **Dosya/alan:** `frequencies.txt` → `trip_id`; karşılaştırma `stop_times.txt`'teki trip seti.
- **Gözlenen değer:** stop_times'sız frekans `trip_id`'si.
- **Beklenen değer:** Frekans seferinin stop_times durak dizisi.

## Kod değiştirirken minimum test matrisi
- trips.txt'te mevcut frequency trip_id'si stop_times'ta yok → TRP_017 çıkmalı.
- frequency trip_id trips.txt'te yok → FRQ_001 çıkar, TRP_017 çıkmaz.
- frequency trip_id stop_times'ta var → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 2.0** (ORTA).
- **R1 blocker değil:** Quality sınıfı R1 üretmez (R1 yalnız `GtfsSpec` + Spec + Kritik).
- **R5 genel skor:** Quality sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS = [R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- **FRQ_001 kök nedeni:** `frequencies.trip_id` trips.txt'te yoksa TRP_017 çalışmaz; önce FK düzeltilir.

### Dış araç eşleşmesi
- **2026-07-11 yeniden değerlendirildi (Quality'e reclass):** MobilityData'da `unused_trip`
  **farklı bir koşuldur** (hiç kullanılmayan trip; TRP_017 ise frekans'ın referans verdiği ama
  stop_times'ı olmayan trip). Birebir (exact-notice) parite YOK; TRP_017 bu durumu proje-özel
  olarak AYRICA raporlar → Interop değil **Quality** (bkz. docs/audits/class-authority-postphase5.md, Bulgu B).
  - https://gtfs-validator.mobilitydata.org/rules.html

### GTFS spec referansı
- GTFS `frequencies.txt` — referans verilen sefer `stop_times.txt`'te tanımlı durak dizisine sahip olmalıdır.
- 🔗 [GTFS Reference — frequencies.txt](https://gtfs.org/documentation/schedule/reference/#frequenciestxt) · [stop_times.txt](https://gtfs.org/documentation/schedule/reference/#stop_timestxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:1548](../../../crates/pipeline/src/k4_cross_ref.rs#L1548) (`check_frequencies`)

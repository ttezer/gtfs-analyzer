# LVL_008 — level_id eksik

> **Karar:** `levels.txt` satırında `level_id` boş. KRİTİK. Sütun başlıkta hiç yoksa ARC_025 devralır.

| Alan | Değer |
|---|---|
| Grup | LVL — Levels |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K2 |
| Varlık | Entity |
| Kimlik alanı | `level_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k2::levels` içinde `level_id` sütunu başlıkta VAR ve değer boş → LVL_008. Sütun yoksa ARC_025.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `level_id` sütunu başlıkta hiç yoksa bu kural değil ARC_025 devreye girer.
- **Yanlış pozitif:** Yok denecek kadar az — `level_id` zorunlu birincil anahtardır; yalnız-boşluk değer trim sonrası boş sayılır (doğru).

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **LVL_008** | `level_id` boş | alan zorunluluğu | KRİTİK · Spec · k2 |
| **ARC_025** | `level_id` sütunu yok | sütun varlığı | KRİTİK · Spec · k1 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** level_id eksik
- **Çözüm önerisi:** Her kata benzersiz bir `level_id` verin.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"level_id zorunludur."*

## Ölçülen alan / değer
- `file`: `levels.txt`
- `field`: `level_id`
- `line`: kat satırı
- `observed`: - (boş değer)

## Kod değiştirirken minimum test matrisi
| Senaryo | Beklenen |
|---|---|
| `level_id` dolu | `LVL_008` yok |
| `level_id` boş (sütun var) | `LVL_008` |
| `level_id` sütunu yok | ARC_025 (LVL_008 yok) |

---
## Teknik ek
### Dış araç eşleşmesi
- **Eşleşme:** `missing_required_field` · **Tür:** MD-parite · **Bilinçli:** evet.
### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
### Hangi raporlarda görünür
`VS_K = [R1, R2, R5, R9]`.
### GTFS spec referansı
- GTFS Schedule Reference — [levels.txt](https://gtfs.org/documentation/schedule/reference/#levelstxt) ilgili alan/dosya tanımını verir.

### Kod referansı
- [`registry.rs`](../../../crates/rules/src/registry.rs)
- [`levels.rs`](../../../crates/pipeline/src/k2/levels.rs)

# SAR_001 — area_id bulunamadı

> **Karar:** `stop_areas.txt::area_id` `areas.txt`'te tanımlı değil. KRİTİK, Spec — durak-alan ilişkisi bağlanamaz. **Düzeltme:** Geçerli bir `area_id` kullanın.
> ⚠️ Satır bazlı (Row) FK; GTFS-Fares v2 alan (area) modeli.

| Alan | Değer |
|---|---|
| Grup | SAR — Stop Areas (Fares v2) |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Row |
| Kimlik alanı | `area_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_fares_v2()` içinde:
```
stop_areas.area_id boş değil VE map.areas İÇERMİYOR
→ SAR_001 (satır bazlı notice)
```
- Kapsam birimi: satır (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `area_id` boşsa atlanır.
- **Yanlış pozitif:** Yok; areas.txt mevcut ama kayıt eksikse doğru tetiklenir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **SAR_001** | stop_areas→area FK yok | FK (area) | KRİTİK · Spec · K4 |
| **SAR_002** | stop_areas→stop FK yok | FK (stop) | KRİTİK · Spec · K4 |
| **ARS_001** | areas.txt area_id yineleniyor | kimlik tekilliği | KRİTİK · Spec · K3 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** area_id bulunamadı
- **Çözüm önerisi:** Geçerli bir `area_id` kullanın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"'{area_id}' alan kodu areas.txt'te tanımlı değil."*

## Ölçülen alan / değer
- **Dosya/alan:** `stop_areas.txt` → `area_id`; karşılaştırma `areas.txt`.
- **Gözlenen değer:** Tanımsız `area_id`.
- **Beklenen değer:** `areas.txt`'te kayıtlı `area_id`.

## Kod değiştirirken minimum test matrisi
- areas.txt'te olmayan `area_id` → SAR_001 çıkmalı.
- `area_id` boş → notice yok.
- Geçerli `area_id` → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** Spec + KRİTİK → yayın engelleyici.
- **R5 genel skor:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok. (`ARS_001` area_id tekrarı bu kuralı dolaylı tetikleyebilir.)

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `foreign_key_violation` (`stop_areas.txt::area_id` → `areas.txt::area_id`).
  - https://gtfs-validator.mobilitydata.org/rules.html
- GTFS Guru tarafında bu kontrolün canlı doğrulanmış birebir eşleşmesi tespit edilmedi.

### GTFS spec referansı
- GTFS-Fares v2 `stop_areas.txt::area_id` → `areas.txt::area_id` foreign key kuralı.
- 🔗 [GTFS Reference — stop_areas.txt](https://gtfs.org/documentation/schedule/reference/#stop_areastxt) · [areas.txt](https://gtfs.org/documentation/schedule/reference/#areastxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:2299](../../../crates/pipeline/src/k4_cross_ref.rs#L2299) (`check_fares_v2`)

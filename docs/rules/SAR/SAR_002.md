# SAR_002 — stop_id bulunamadı

> **Karar:** `stop_areas.txt::stop_id` `stops.txt`'te tanımlı değil. KRİTİK, Spec — alan bir durağa bağlanamaz. **Düzeltme:** Geçerli bir `stop_id` kullanın.
> ⚠️ Satır bazlı (Row) FK; GTFS-Fares v2 alan (area) modeli.

| Alan | Değer |
|---|---|
| Grup | SAR — Stop Areas (Fares v2) |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Row |
| Kimlik alanı | `stop_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_fares_v2()` içinde:
```
stop_areas.stop_id boş değil VE map.stops İÇERMİYOR
→ SAR_002 (satır bazlı notice)
```
- Kapsam birimi: satır (Row).

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** `stop_id` boşsa atlanır.
- **Yanlış pozitif:** Yok; stops.txt mevcut ama durak eksikse doğru tetiklenir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **SAR_002** | stop_areas→stop FK yok | FK (stop) | KRİTİK · Spec · K4 |
| **SAR_001** | stop_areas→area FK yok | FK (area) | KRİTİK · Spec · K4 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** stop_id bulunamadı
- **Çözüm önerisi:** Geçerli bir `stop_id` kullanın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"'{stop_id}' durak stops.txt'te tanımlı değil."*

## Ölçülen alan / değer
- **Dosya/alan:** `stop_areas.txt` → `stop_id`; karşılaştırma `stops.txt`.
- **Gözlenen değer:** Tanımsız `stop_id`.
- **Beklenen değer:** `stops.txt`'te kayıtlı `stop_id`.

## Kod değiştirirken minimum test matrisi
- stops.txt'te olmayan `stop_id` → SAR_002 çıkmalı.
- `stop_id` boş → notice yok.
- Geçerli `stop_id` → notice yok.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **R1 blocker:** Spec + KRİTİK → yayın engelleyici.
- **R5 kalite skoru:** Spec sınıf penaltısına girer.

### Hangi raporlarda görünür
`report_views = VS_K = [R1, R2, R5, R9]`.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `blocks` yok.

### Dış araç eşleşmesi
- **2026-06-03 doğrulandı:** MobilityData'da yakın karşılık `foreign_key_violation` (`stop_areas.txt::stop_id` → `stops.txt::stop_id`).
  - https://gtfs-validator.mobilitydata.org/rules.html
- GTFS Guru tarafında bu kontrolün canlı doğrulanmış birebir eşleşmesi tespit edilmedi.

### GTFS spec referansı
- GTFS-Fares v2 `stop_areas.txt::stop_id` → `stops.txt::stop_id` foreign key kuralı.
- 🔗 [GTFS Reference — stop_areas.txt](https://gtfs.org/documentation/schedule/reference/#stop_areastxt) · [stops.txt](https://gtfs.org/documentation/schedule/reference/#stopstxt)

### Kod referansı
- Emit: [k4_cross_ref.rs:2005](../../../crates/pipeline/src/k4_cross_ref.rs#L2005) (`check_fares_v2`)

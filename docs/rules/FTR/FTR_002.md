# FTR_002 — from_leg_group_id bulunamadı

> **Karar:** from_leg_group_id dolu ama fare_leg_rules leg_group_id kümesinde yok. KRİTİK, Spec kuralı. **Düzeltme:** Geçerli bir `from_leg_group_id` kullanın.
> ⚠️ FTR grubu Fares v2 `fare_transfer_rules.txt` kayıtlarını denetler; transfer tipi, süre limiti, transfer sayısı ve bacak/ürün referansları ayrı rule id'lerle raporlanır.

| Alan | Değer |
|---|---|
| Grup | FTR — Fare Transfer Rules |
| Önem | Kritik |
| Sınıf | Spec |
| Aşama | K4 |
| Varlık | Row |
| Kimlik alanı | `from_leg_group_id` |
| Skor tabanı | 1 |
| Görünürlük | VS_K (`R1`, `R2`, `R5`, `R9`) |
| Bloke ettiği kurallar | - |

## Tetikleme koşulu (kesin mantık)
`k4_cross_ref::check_fares_v2()` içinde:
```
from_leg_group_id dolu ama fare_leg_rules leg_group_id kümesinde yok
→ FTR_002
```
- Kapsam birimi `fare_transfer_rules.txt` satırıdır.
- Entity id bazı kontrollerde `from_leg_group_id` üzerinden gelir.

## Yanlış pozitif / negatif (asıl tasarım riski)
- **Yanlış negatif:** İlgili alan boşsa bazı referans/değer kontrolleri çalışmaz; boşluk semantiği ayrıca spec bağlamıyla yorumlanmalıdır.
- **Yanlış pozitif:** Kod koşulu sağlanıyorsa spec açısından sorun kabul edilir; özel ücret politikası veri modelinde açık ifade edilmelidir.

## Karışan komşular (ayrım)
| Kural | Ne yakalar | Eksen | Önem · Sınıf · Aşama |
|---|---|---|---|
| **FTR_001** | fare transfer tipi enum zorunluluğu | Transfer tipi | KRİTİK · Spec · K2 |
| **FLG_007** | leg_group_id satır bağlamı ve rule priority | Bacak grubu | ORTA · Spec · K2 |
| **FTR_008** | transfer_count değer aralığı | Transfer sayısı | ORTA · Spec · K2 |

## Mesaj & çözüm önerisi
### R9 Kural Mesajı
- **Kural mesajı:** from_leg_group_id bulunamadı
- **Çözüm önerisi:** Geçerli bir `from_leg_group_id` kullanın.

### R2 mesajı/mesajları
- **Ayrıntılı bulgu mesajı:** *"<from_leg_group_id> bacak grubu fare_leg_rules.txt te tanimli degil."*
- **Çözüm önerisi:** Gecerli bir from_leg_group_id kullanin.

## Ölçülen alan / değer
- **Dosya/alan:** fare_transfer_rules.txt → from_leg_group_id
- **Gözlenen değer:** Notice observed value veya boş/geçersiz alan.
- **Beklenen değer:** Geçerli bir `from_leg_group_id` kullanın.

## Kod değiştirirken minimum test matrisi
- Temel pozitif örnek: koşul sağlandığında FTR_002 çıkmalı.
- Temel negatif örnek: geçerli satırda FTR_002 çıkmamalı.
- Sınır değer: boş, 0, -1 veya enum dışı değer denenmeli.
- Komşu FTR/FLG/FPD kuralıyla rule id karışmamalı.

---
## Teknik ek

### Skora katkı
- **severity.weight() = 4.0** (KRİTİK).
- **Sınıf/Aşama:** Spec · K4.

### Hangi raporlarda görünür
`report_views = VS_K` → R1, R2, R5, R9.

### Bağımlılık (bunu maskeleyen root cause'lar)
- `fare_leg_rules.txt` ve `fare_products.txt` kimlik haritaları oluşmadan K4 referans kontrolleri güvenilir yorumlanamaz.

### Dış araç eşleşmesi
2026-06-03 doğrulandı: MobilityData kuralları sayfasında birebir FTR_002 adı görünmedi; GTFS Fares v2 dokümantasyonu `fare_transfer_rules.txt` alan ailesini tanımlar.

Kaynak: [MobilityData GTFS Validator Rules](https://gtfs-validator.mobilitydata.org/rules.html)

### GTFS spec referansı
[GTFS Schedule Reference - fare_transfer_rules.txt](https://gtfs.org/documentation/schedule/reference/#fare_transfer_rulestxt): `fare_transfer_type`, `from_leg_group_id`, `to_leg_group_id`, `duration_limit`, `duration_limit_type`, `transfer_count` ve `fare_product_id` alanları transfer ücret kurallarını tanımlar.

### Kod referansı
- Emit: [kaynak](../../../crates/pipeline/src/k4_cross_ref.rs)
